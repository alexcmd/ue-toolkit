# UE Python API — PixelCapture Module

**2 types** from the `PixelCapture` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `PixelCaptureMediaCapture`, `PixelCaptureMediaOuput`

---

## Classes

### `unreal.PixelCaptureMediaCapture`
Inherits: `MediaCapture` | Header: `PixelCaptureCapturerMediaCapture.h`

Pixel Capture Media Capture

### `unreal.PixelCaptureMediaOuput`
Inherits: `MediaOutput` | Header: `PixelCaptureCapturerMediaCapture.h`

Pixel Capture Media Ouput
