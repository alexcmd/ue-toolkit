# UE Python API — SparseVolumeTexture Module

**1 types** from the `SparseVolumeTexture` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SparseVolumeTextureFactory`

---

## Classes

### `unreal.SparseVolumeTextureFactory`
Inherits: `Factory` | Header: `SparseVolumeTextureFactory.h`

Responsible for creating and importing Sparse Volume Texture objects
