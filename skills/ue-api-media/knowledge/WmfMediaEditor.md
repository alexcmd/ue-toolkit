# UE Python API — WmfMediaEditor Module

**1 types** from the `WmfMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `WmfFileMediaSourceFactory`

---

## Classes

### `unreal.WmfFileMediaSourceFactory`
Inherits: `Factory` | Header: `WmfFileMediaSourceFactory.h`

Implements a factory for UFileMediaSource objects.
