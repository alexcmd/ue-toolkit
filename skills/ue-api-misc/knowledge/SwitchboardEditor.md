# UE Python API — SwitchboardEditor Module

**4 types** from the `SwitchboardEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `DisplayClusterRootActorReference`, `SwitchboardNewConfigUserOptions`, `SwitchboardEditorSettings`, `SwitchboardProjectSettings`

---

## Classes

### `unreal.DisplayClusterRootActorReference`
Inherits: `StructBase` | Header: `SwitchboardTypes.h`

Structure used to hold a reference of an nDisplay actor for detail customization purposes Note: The reason for not simply using <ADisplayClusterRootActor> is to avoid depending on the nDisplay plugin

### `unreal.SwitchboardNewConfigUserOptions`
Inherits: `StructBase` | Header: `SwitchboardTypes.h`

Structure used to collect New Config options from the user

**Properties** (5):
  - `auto_connect`: `bool` — [Read-Write] Indicates that Switchboard should auto-connect to the listeners of each created device ...
  - `config_name`: `str` — [Read-Write] Optional name of the new config. If left empty, a name will be autogenerated. (str)
  - `dcra`: `DisplayClusterRootActorReference` — [Read-Write] Indicates which nDisplay actor to use (DisplayClusterRootActorReference)
  - `num_editor_devices`: `int` — [Read-Write] Number of Editor devices that it should create (int32)
  - `use_localhost`: `bool` — [Read-Write] Indicates that Switchboard should override IP addresses with localhost (bool)

### `unreal.SwitchboardEditorSettings`
Inherits: `Object` | Header: `SwitchboardEditorSettings.h`

Switchboard Editor Settings

**Methods** (1):
  - `get_switchboard_editor_settings()` -> `SwitchboardEditorSettings` [classmethod] — Get Editor Settings object for Switchboard

### `unreal.SwitchboardProjectSettings`
Inherits: `Object` | Header: `SwitchboardProjectSettings.h`

Switchboard Project Settings

**Methods** (1):
  - `get_switchboard_project_settings()` -> `SwitchboardProjectSettings` [classmethod] — Get Project Settings object for Switchboard
