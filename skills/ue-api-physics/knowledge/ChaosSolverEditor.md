# UE Python API — ChaosSolverEditor Module

**1 types** from the `ChaosSolverEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ChaosSolverFactory`

---

## Classes

### `unreal.ChaosSolverFactory`
Inherits: `Factory` | Header: `ChaosSolverFactory.h`

Factory for Simple Cube
