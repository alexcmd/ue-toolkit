# UE Python API — NDIMediaEditor Module

**2 types** from the `NDIMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `NDIMediaOutputFactory`, `NDIMediaSourceFactory`

---

## Classes

### `unreal.NDIMediaOutputFactory`
Inherits: `Factory` | Header: `NDIMediaOutputFactory.h`

Implements a factory for UNDIMediaOutputFactory objects.

### `unreal.NDIMediaSourceFactory`
Inherits: `Factory` | Header: `NDIMediaSourceFactory.h`

Implements a factory for UNDIMediaSource objects.
