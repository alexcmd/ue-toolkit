# UE Python API — RivermaxMediaEditor Module

**2 types** from the `RivermaxMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `RivermaxMediaOutputFactoryNew`, `RivermaxMediaSourceFactoryNew`

---

## Classes

### `unreal.RivermaxMediaOutputFactoryNew`
Inherits: `Factory` | Header: `RivermaxMediaOutputFactoryNew.h`

Implements a factory for URivermaxMediaOutput objects.

### `unreal.RivermaxMediaSourceFactoryNew`
Inherits: `Factory` | Header: `RivermaxMediaSourceFactoryNew.h`

Implements a factory for URivermaxMediaSource objects.
