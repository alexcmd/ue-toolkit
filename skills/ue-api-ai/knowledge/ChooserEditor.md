# UE Python API — ChooserEditor Module

**2 types** from the `ChooserEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ChooserTableFactory`, `FloatAutoPopulatorBlueprint`

---

## Classes

### `unreal.ChooserTableFactory`
Inherits: `Factory` | Header: `ChooserFactory.h`

Chooser Table Factory

### `unreal.FloatAutoPopulatorBlueprint`
Inherits: `FloatAutoPopulator` | Header: `FloatDistanceColumnEditor.h`

Float Auto Populator Blueprint

**Methods** (1):
  - `auto_populate(value=float)` — Auto Populate
