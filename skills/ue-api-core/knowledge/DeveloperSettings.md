# UE Python API — DeveloperSettings Module

**3 types** from the `DeveloperSettings` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `PerPlatformSettings`, `DeveloperSettings`, `PlatformSettings`

---

## Classes

### `unreal.PerPlatformSettings`
Inherits: `StructBase` | Header: `PlatformSettings.h`

Per Platform Settings

### `unreal.DeveloperSettings`
Inherits: `Object` | Header: `DeveloperSettings.h`

The base class of any auto discovered settings object.

### `unreal.PlatformSettings`
Inherits: `Object` | Header: `PlatformSettings.h`

The base class of any per platform settings. The pattern for using these is as follows.
