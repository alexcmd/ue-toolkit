# UE Python API — BlackmagicMediaEditor Module

**2 types** from the `BlackmagicMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `BlackmagicMediaOutputFactoryNew`, `BlackmagicMediaSourceFactoryNew`

---

## Classes

### `unreal.BlackmagicMediaOutputFactoryNew`
Inherits: `Factory` | Header: `BlackmagicMediaOutputFactoryNew.h`

Implements a factory for objects.

### `unreal.BlackmagicMediaSourceFactoryNew`
Inherits: `Factory` | Header: `BlackmagicMediaSourceFactoryNew.h`

Implements a factory for objects.
