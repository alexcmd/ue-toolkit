# UE Python API — ColorGradingEditor Module

**1 types** from the `ColorGradingEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ColorGradingMixerObjectFilter`

---

## Classes

### `unreal.ColorGradingMixerObjectFilter`
Inherits: `ObjectMixerObjectFilter` | Header: `ColorGradingMixerObjectFilter.h`

Color Grading Mixer Object Filter
