# UE Python API — MfMediaEditor Module

**1 types** from the `MfMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MfFileMediaSourceFactory`

---

## Classes

### `unreal.MfFileMediaSourceFactory`
Inherits: `Factory` | Header: `MfFileMediaSourceFactory.h`

Implements a factory for UFileMediaSource objects.
