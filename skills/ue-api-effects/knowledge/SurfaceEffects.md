# UE Python API — SurfaceEffects Module

**2 types** from the `SurfaceEffects` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SurfaceEffectTableRow`, `SurfaceEffectRule`

---

## Classes

### `unreal.SurfaceEffectTableRow`
Inherits: `TableRowBase` | Header: `SurfaceEffectsSubsystem.h`

Data Table Row that effectively wraps the Surface Effect Rule

### `unreal.SurfaceEffectRule`
Inherits: `DataAsset` | Header: `SurfaceEffectsSubsystem.h`

Base data asset used to store what conditions result in a specific surface being returned
