# UE Python API — MovieScenePoseSearchTracks Module

**2 types** from the `MovieScenePoseSearchTracks` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MovieSceneStitchAnimSection`, `MovieSceneStitchAnimTrack`

---

## Classes

### `unreal.MovieSceneStitchAnimSection`
Inherits: `MovieSceneSection` | Header: `MovieSceneStitchAnimSection.h`

Handles generating and playing back transitional skeletal animations from a stitch database.

### `unreal.MovieSceneStitchAnimTrack`
Inherits: `MovieSceneNameableTrack` | Header: `MovieSceneStitchAnimTrack.h`

Handles generating and playing back transitional skeletal animations from a stitch database.
