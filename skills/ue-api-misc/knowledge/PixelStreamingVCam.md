# UE Python API — PixelStreamingVCam Module

**1 types** from the `PixelStreamingVCam` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `PixelStreamingMediaOutput`

---

## Classes

### `unreal.PixelStreamingMediaOutput`
Inherits: `MediaOutput` | Header: `PixelStreamingMediaOutput.h`

Pixel Streaming Media Output
