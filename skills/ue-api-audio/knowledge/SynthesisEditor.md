# UE Python API — SynthesisEditor Module

**3 types** from the `SynthesisEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AudioImpulseResponseFactory`, `ModularSynthPresetBankFactory`, `MonoWaveTableSynthPresetFactory`

---

## Classes

### `unreal.AudioImpulseResponseFactory`
Inherits: `Factory` | Header: `AudioImpulseResponseAsset.h`

Audio Impulse Response Factory

### `unreal.ModularSynthPresetBankFactory`
Inherits: `Factory` | Header: `EpicSynth1PresetBank.h`

Modular Synth Preset Bank Factory

### `unreal.MonoWaveTableSynthPresetFactory`
Inherits: `Factory` | Header: `MonoWaveTablePresetBank.h`

Mono Wave Table Synth Preset Factory
