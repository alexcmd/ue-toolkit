# UE Python API — MutableRuntime Module

**1 types** from the `MutableRuntime` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `FaceCullStrategy`

---

## Classes

### `unreal.FaceCullStrategy`
Inherits: `EnumBase` | Header: `Types.h`

This type is used in some mesh clip operations. Warning: this type is used compiled COs. Any relevant change to the option order requires and update in the CustomizableObjectPrivate::ECustomizableObje...

**Properties** (2):
  - `ALL_VERTICES_CULLED`: `FaceCullStrategy = Ellipsis` — 0
  - `ONE_VERTEX_CULLED`: `FaceCullStrategy = Ellipsis` — 1
