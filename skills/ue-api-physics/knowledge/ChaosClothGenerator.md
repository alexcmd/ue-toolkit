# UE Python API — ChaosClothGenerator Module

**1 types** from the `ChaosClothGenerator` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ClothGeneratorComponent`

---

## Classes

### `unreal.ClothGeneratorComponent`
Inherits: `ChaosClothComponent` | Header: `ClothGeneratorComponent.h`

Cloth data generation component.
