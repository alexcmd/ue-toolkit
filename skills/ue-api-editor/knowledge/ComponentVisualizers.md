# UE Python API — ComponentVisualizers Module

**1 types** from the `ComponentVisualizers` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `Manipulator`

---

## Classes

### `unreal.Manipulator`
Inherits: `Actor` | Header: `Manipulator.h`
