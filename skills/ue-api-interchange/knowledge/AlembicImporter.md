# UE Python API — AlembicImporter Module

**1 types** from the `AlembicImporter` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AlembicImportFactory`

---

## Classes

### `unreal.AlembicImportFactory`
Inherits: `Factory` | Header: `AlembicImportFactory.h`

Alembic Import Factory
