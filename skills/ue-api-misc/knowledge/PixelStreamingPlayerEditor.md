# UE Python API — PixelStreamingPlayerEditor Module

**1 types** from the `PixelStreamingPlayerEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `PixelStreamingMediaTextureFactory`

---

## Classes

### `unreal.PixelStreamingMediaTextureFactory`
Inherits: `Factory` | Header: `PixelStreamingMediaTextureFactory.h`

Pixel Streaming Media Texture Factory
