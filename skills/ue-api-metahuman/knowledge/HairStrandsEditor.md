# UE Python API — HairStrandsEditor Module

**5 types** from the `HairStrandsEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `GroomBindingFactory`, `GroomFactory`, `HairStrandsFactory`, `ReimportGroomCacheFactory`, `ReimportHairStrandsFactory`

---

## Classes

### `unreal.GroomBindingFactory`
Inherits: `Factory` | Header: `GroomBindingFactory.h`

Groom Binding Factory

### `unreal.GroomFactory`
Inherits: `Factory` | Header: `GroomFactory.h`

Groom Factory

### `unreal.HairStrandsFactory`
Inherits: `Factory` | Header: `HairStrandsFactory.h`

Implements a factory for UHairStrands objects.

### `unreal.ReimportGroomCacheFactory`
Inherits: `HairStrandsFactory` | Header: `ReimportGroomCacheFactory.h`

Reimport Groom Cache Factory

### `unreal.ReimportHairStrandsFactory`
Inherits: `HairStrandsFactory` | Header: `ReimportHairStrandsFactory.h`

Reimport Hair Strands Factory
