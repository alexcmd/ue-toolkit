# UE Python API — WebMMediaEditor Module

**1 types** from the `WebMMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `WebMPlatFileMediaSourceFactory`

---

## Classes

### `unreal.WebMPlatFileMediaSourceFactory`
Inherits: `Factory` | Header: `WebMFileMediaSourceFactory.h`

Implements a factory for UFileMediaSource objects.
