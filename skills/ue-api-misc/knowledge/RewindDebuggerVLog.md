# UE Python API — RewindDebuggerVLog Module

**1 types** from the `RewindDebuggerVLog` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `VLogRenderingActor`

---

## Classes

### `unreal.VLogRenderingActor`
Inherits: `VisualLoggerRenderingActorBase` | Header: `VLogRenderingActor.h`

Transient actor used to draw visual logger data on level
