# UE Python API — RemoteControlComponents Module

**1 types** from the `RemoteControlComponents` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `RemoteControlTrackerComponent`

---

## Classes

### `unreal.RemoteControlTrackerComponent`
Inherits: `ActorComponent` | Header: `RemoteControlTrackerComponent.h`

A component keeping track of properties currently exposed to Remote Control. Supports auto-exposing of properties upon duplication. e.g. duplicating an Actor with this component will result in the sam...
