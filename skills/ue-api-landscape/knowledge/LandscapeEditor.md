# UE Python API — LandscapeEditor Module

**1 types** from the `LandscapeEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `LandscapePlaceholder`

---

## Classes

### `unreal.LandscapePlaceholder`
Inherits: `Actor` | Header: `LandscapePlaceholder.h`

Landscape Placeholder
