# UE Python API — LightMixer Module

**1 types** from the `LightMixer` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `LightMixerObjectFilter`

---

## Classes

### `unreal.LightMixerObjectFilter`
Inherits: `ObjectMixerObjectFilter` | Header: `LightMixerObjectFilter.h`

Light Mixer Object Filter
