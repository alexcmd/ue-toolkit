# UE Python API — PSDImporterEditor Module

**1 types** from the `PSDImporterEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `PSDDocumentImportFactory`

---

## Classes

### `unreal.PSDDocumentImportFactory`
Inherits: `Factory` | Header: `PSDDocumentImportFactory.h`

PSDDocument Import Factory
