# UE Python API — USDStageEditor Module

**1 types** from the `USDStageEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `UsdStageEditorLibrary`

---

## Classes

### `unreal.UsdStageEditorLibrary`
Inherits: `BlueprintFunctionLibrary` | Header: `USDStageEditorBlueprintLibrary.h`

Library of functions that can be used from scripting to interact with the USD Stage Editor UI

**Methods** (24):
  - `actions_import(output_content_folder, options)` -> `None` [classmethod] — Imports the currently opened USD Stage into persistent UE assets, actors and components on the level. Corresponds to the...
  - `close_stage_editor()` -> `bool` [classmethod] — Closes the USD Stage Editor window if it is opened. Does nothing in case it is already closed.
  - `export_selected_layers(output_directory)` -> `None` [classmethod] — Exports the currently selected layers on the USD Stage Editor to brand new files in a new location. Corresponds to right...
  - `file_close()` -> `None` [classmethod] — Closes the currently opened USD Stage (by clearing the attached Stage Actorâs RootLayer property). Corresponds to the ...
  - `file_export_all_layers(output_directory)` -> `None` [classmethod] — Exports all layers of the currently opened USD Stage to brand new files in a new location. Corresponds to the âFile ->...
  - `file_export_flattened_layer_stack(output_layer)` -> `None` [classmethod] — Exports the currently opened USD Stage to a single âflattenedâ USD layer. Corresponds to the âFile -> Export -> Fl...
  - `file_export_flattened_stage(output_layer)` -> `None` [classmethod] — Exports the currently opened USD Stage to a single âflattenedâ USD layer. Corresponds to the âFile -> Export -> Fl...
  - `file_new()` -> `None` [classmethod] — Creates a new memory-only layer and opens an USD Stage with that layer as its root. Corresponds to the âFile -> Newâ...
  - `file_open(file_path)` -> `None` [classmethod] — Opens an USD Stage from a file on disk. Corresponds to the âFile -> Openâ action on the USD Stage Editor menu bar.
  - `file_reload()` -> `None` [classmethod] — Reloads all layers of the current USD Stage. Corresponds to the âFile -> Reloadâ action on the USD Stage Editor menu...
  - `file_reset()` -> `None` [classmethod] — Resets the state of the current USD Stage (which layers are muted, the current edit target, etc.). Corresponds to the â...
  - `file_save(output_file_path_if_unsaved)` -> `None` [classmethod] — Saves the currently opened USD Stage back to disk, or to a new file in case it hasnât been saved yet. Corresponds to t...
  - `get_attached_stage_actor()` -> `UsdStageActor` [classmethod] — Gets which actor is currently attached to the USD Stage Editor, if any. May return nullptr in case no actor is attached.
  - `get_selected_layer_identifiers()` -> `Array [ str ]` [classmethod] — Returns the full identifiers of all layers that are currently selected on the USD Stage Editor.
  - `get_selected_prim_paths()` -> `Array [ str ]` [classmethod] — Returns the full paths to all prims currently selected on the USD Stage Editor.
  - `get_selected_property_metadata_names()` -> `Array [ str ]` [classmethod] — Returns the names of the currently selected property metadata entries on the right panel of the USD Stage Editor.
  - `get_selected_property_names()` -> `Array [ str ]` [classmethod] — Returns the names of the currently selected properties on the right panel of the USD Stage Editor.
  - `is_stage_editor_opened()` -> `bool` [classmethod] — Checks to see if an USD Stage Editor window is currently opened.
  - `open_stage_editor()` -> `bool` [classmethod] — Opens the the USD Stage Editor window, or focus it in case it is already open.
  - `set_attached_stage_actor(new_actor)` -> `bool` [classmethod] — Sets which actor is currently attached to the USD Stage Editor. Provide None/nullptr to clear the attached stage actor.
  - `set_selected_layer_identifiers(new_selection)` -> `None` [classmethod] — Sets the USD Stage Editor layer selection to all occurences of the layers with identifiers in NewSelection. Provide an e...
  - `set_selected_prim_paths(new_selection)` -> `None` [classmethod] — Sets the USD Stage Editor prim selection to the prims with paths contained in NewSelection. Provide an empty array to cl...
  - `set_selected_property_metadata_names(new_selection)` -> `None` [classmethod] — Sets the USD Stage Editor property metadata selection to the entries with names contained in NewSelection. Provide an em...
  - `set_selected_property_names(new_selection)` -> `None` [classmethod] — Sets the USD Stage Editor property selection to the properties with names contained in NewSelection. Provide an empty ar...
