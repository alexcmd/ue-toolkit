# UE Python API — IOSReplayKit Module

**1 types** from the `IOSReplayKit` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `IOSReplayKitControl`

---

## Classes

### `unreal.IOSReplayKitControl`
Inherits: `BlueprintFunctionLibrary` | Header: `IOSReplayKitControl.h`

IOSReplay Kit Control

**Methods** (4):
  - `start_capture_to_file(microphone_enabled = True)` -> `None` [classmethod] — Start Capture to File
  - `start_recording(microphone_enabled = True)` -> `None` [classmethod] — Start Recording
  - `stop_capture()` -> `None` [classmethod] — Stop Capture
  - `stop_recording()` -> `None` [classmethod] — Stop Recording
