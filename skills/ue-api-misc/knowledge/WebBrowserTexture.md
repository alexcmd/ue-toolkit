# UE Python API — WebBrowserTexture Module

**1 types** from the `WebBrowserTexture` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `WebBrowserTexture`

---

## Classes

### `unreal.WebBrowserTexture`
Inherits: `Texture` | Header: `WebBrowserTexture.h`

Implements a texture asset for rendering webbrowser output for Android.
