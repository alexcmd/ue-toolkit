# UE Python API — AxFImporter Module

**1 types** from the `AxFImporter` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AxFImporterFactory`

---

## Classes

### `unreal.AxFImporterFactory`
Inherits: `Factory` | Header: `AxFImporterFactory.h`

Ax FImporter Factory
