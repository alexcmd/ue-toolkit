# UE Python API — AvalancheSequencer Module

**3 types** from the `AvalancheSequencer` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AvaSequencerDisplayRate`, `AvaEaseCurve`, `AvaSequenceExporter`

---

## Classes

### `unreal.AvaSequencerDisplayRate`
Inherits: `StructBase` | Header: `AvaSequencerDisplayRate.h`

Ava Sequencer Display Rate

### `unreal.AvaEaseCurve`
Inherits: `CurveFloat` | Header: `AvaEaseCurve.h`

Ava Ease Curve

### `unreal.AvaSequenceExporter`
Inherits: `Exporter` | Header: `AvaSequenceExporter.h`

Ava Sequence Exporter
