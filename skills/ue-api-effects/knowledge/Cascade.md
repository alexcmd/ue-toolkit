# UE Python API — Cascade Module

**1 types** from the `Cascade` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `CascadeParticleSystemComponent`

---

## Classes

### `unreal.CascadeParticleSystemComponent`
Inherits: `ParticleSystemComponent` | Header: `CascadeParticleSystemComponent.h`

Used to provide an extended particle system component to allow collision to function in the preview window.
