# UE Python API — OverlayEditor Module

**4 types** from the `OverlayEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `BasicOverlaysFactory`, `BasicOverlaysFactoryNew`, `LocalizedOverlaysFactoryNew`, `ReimportBasicOverlaysFactory`

---

## Classes

### `unreal.BasicOverlaysFactory`
Inherits: `Factory` | Header: `BasicOverlaysFactory.h`

Implements a factory for UBasicOverlays objects.

### `unreal.BasicOverlaysFactoryNew`
Inherits: `Factory` | Header: `BasicOverlaysFactoryNew.h`

Implements a factory for new UBasicOverlays objects.

### `unreal.LocalizedOverlaysFactoryNew`
Inherits: `Factory` | Header: `LocalizedOverlaysFactoryNew.h`

Implements a factory for new ULocalizedOverlays objects.

### `unreal.ReimportBasicOverlaysFactory`
Inherits: `BasicOverlaysFactory` | Header: `ReimportBasicOverlaysFactory.h`

A factory for reimporting basic overlays
