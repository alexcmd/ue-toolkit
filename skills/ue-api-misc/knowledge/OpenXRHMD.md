# UE Python API — OpenXRHMD Module

**1 types** from the `OpenXRHMD` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `OpenXRBlueprintFunctionLibrary`

---

## Classes

### `unreal.OpenXRBlueprintFunctionLibrary`
Inherits: `BlueprintFunctionLibrary` | Header: `OpenXRBlueprintFunctionLibrary.h`

Open XRBlueprint Function Library

**Methods** (1):
  - `set_environment_blend_mode(new_blend_mode)` -> `None` [classmethod] — Allows to change the environment blend mode at runtime.
