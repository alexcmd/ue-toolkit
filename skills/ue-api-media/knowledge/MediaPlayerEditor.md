# UE Python API — MediaPlayerEditor Module

**7 types** from the `MediaPlayerEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `FileMediaSourceFactoryNew`, `MediaPlayerEditorMediaContext`, `MediaPlayerFactoryNew`, `MediaPlaylistFactoryNew`, `MediaTextureFactoryNew`, `PlatformMediaSourceFactoryNew`, `StreamMediaSourceFactoryNew`

---

## Classes

### `unreal.FileMediaSourceFactoryNew`
Inherits: `Factory` | Header: `FileMediaSourceFactoryNew.h`

Implements a factory for UFileMediaSource objects.

### `unreal.MediaPlayerEditorMediaContext`
Inherits: `ToolMenuContextBase` | Header: `MediaPlayerEditorMediaContext.h`

Media Player Editor Media Context

**Properties** (2):
  - `selected_asset`: `Object` — [Read-Write] (Object)
  - `style_set_name`: `Name` — [Read-Write] (Name)

### `unreal.MediaPlayerFactoryNew`
Inherits: `Factory` | Header: `MediaPlayerFactoryNew.h`

Implements a factory for UMediaPlayer objects.

### `unreal.MediaPlaylistFactoryNew`
Inherits: `Factory` | Header: `MediaPlaylistFactoryNew.h`

Implements a factory for UMediaPlaylist objects.

### `unreal.MediaTextureFactoryNew`
Inherits: `Factory` | Header: `MediaTextureFactoryNew.h`

Implements a factory for UMediaTexture objects.

### `unreal.PlatformMediaSourceFactoryNew`
Inherits: `Factory` | Header: `PlatformMediaSourceFactoryNew.h`

Implements a factory for UPlatformMediaSource objects.

### `unreal.StreamMediaSourceFactoryNew`
Inherits: `Factory` | Header: `StreamMediaSourceFactoryNew.h`

Implements a factory for UStreamMediaSource objects.
