# UE Python API — DMXControlConsoleEditor Module

**1 types** from the `DMXControlConsoleEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `DMXControlConsoleFactory`

---

## Classes

### `unreal.DMXControlConsoleFactory`
Inherits: `Factory` | Header: `DMXControlConsoleFactory.h`

Factory for DMX Control Console
