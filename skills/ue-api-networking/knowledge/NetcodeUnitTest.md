# UE Python API — NetcodeUnitTest Module

**2 types** from the `NetcodeUnitTest` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `NUTActor`, `ExecuteOnServer`

---

## Classes

### `unreal.NUTActor`
Inherits: `Actor` | Header: `NUTActor.h`

### `unreal.ExecuteOnServer`
Inherits: `DelegateBase` | Header: `NUTActor.h`

Delegate for executing a unit test function on the server
