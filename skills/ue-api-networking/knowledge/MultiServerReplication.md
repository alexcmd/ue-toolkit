# UE Python API — MultiServerReplication Module

**3 types** from the `MultiServerReplication` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MultiServerBeaconClient`, `MultiServerBeaconHost`, `MultiServerBeaconHostObject`

---

## Classes

### `unreal.MultiServerBeaconClient`
Inherits: `OnlineBeaconClient` | Header: `MultiServerBeaconClient.h`

An online beacon that helps manage connecting to MultiServer Networks, and replicating metadata about the MultiServer Network.

### `unreal.MultiServerBeaconHost`
Inherits: `OnlineBeaconHost` | Header: `MultiServerBeaconHost.h`

An online beacon that helps manage connecting to MultiServer networks, and replicating metadata about the MultiServer network.

### `unreal.MultiServerBeaconHostObject`
Inherits: `OnlineBeaconHostObject` | Header: `MultiServerBeaconHostObject.h`

Multi Server Beacon Host Object
