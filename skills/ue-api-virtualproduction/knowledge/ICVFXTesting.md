# UE Python API — ICVFXTesting Module

**1 types** from the `ICVFXTesting` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ICVFXTestLocation`

---

## Classes

### `unreal.ICVFXTestLocation`
Inherits: `CameraActor` | Header: `ICVFXTestLocation.h`

Actor used when running ICVFX perf tests. For every test location, the display cluster root actor will be moved to this location and perf data will be connected for a minute.
