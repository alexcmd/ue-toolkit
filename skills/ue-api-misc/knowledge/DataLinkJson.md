# UE Python API — DataLinkJson Module

**2 types** from the `DataLinkJson` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `DataLinkJsonStructMappingConfig`, `DataLinkJsonStructMapping`

---

## Classes

### `unreal.DataLinkJsonStructMappingConfig`
Inherits: `StructBase` | Header: `DataLinkJsonToStruct.h`

Data Link Json Struct Mapping Config

### `unreal.DataLinkJsonStructMapping`
Inherits: `Object` | Header: `DataLinkJsonStructMapping.h`

Base class for any mapping logic from a json object to a given struct
