# UE Python API — CQTestTests Module

**4 types** from the `CQTestTests` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `CQGameInstanceClass`, `CQTestGameMode`, `TestActorWithProperties`, `TestReplicatedActor`

---

## Classes

### `unreal.CQGameInstanceClass`
Inherits: `GameInstance` | Header: `CQTestGameInstance.h`

Basic GameInstance with a test value.

### `unreal.CQTestGameMode`
Inherits: `GameModeBase` | Header: `CQTestGameMode.h`

Basic GameMode with a test value.

### `unreal.TestActorWithProperties`
Inherits: `Actor` | Header: `TestActorWithProperties.h`

Test Actor with Properties

### `unreal.TestReplicatedActor`
Inherits: `Actor` | Header: `TestReplicatedActor.h`

Test Replicated Actor
