# UE Python API — PropertyAnimatorCoreEditor Module

**1 types** from the `PropertyAnimatorCoreEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `PropertyAnimatorCorePresetFactory`

---

## Classes

### `unreal.PropertyAnimatorCorePresetFactory`
Inherits: `Factory` | Header: `PropertyAnimatorCorePresetFactory.h`

Property Animator Core Preset Factory
