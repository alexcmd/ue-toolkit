# UE Python API — ChaosOutfitAssetEngine Module

**3 types** from the `ChaosOutfitAssetEngine` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ChaosSizedOutfitSource`, `ChaosOutfit`, `ChaosOutfitAsset`

---

## Classes

### `unreal.ChaosSizedOutfitSource`
Inherits: `StructBase` | Header: `SizedOutfitSource.h`

### `unreal.ChaosOutfit`
Inherits: `Object` | Header: `Outfit.h`

Outfit class to handle the assembly of an outfit asset.

### `unreal.ChaosOutfitAsset`
Inherits: `ChaosClothAssetBase` | Header: `OutfitAsset.h`

Outfit asset for character clothing and simulation.
