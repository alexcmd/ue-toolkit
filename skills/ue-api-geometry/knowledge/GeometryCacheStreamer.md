# UE Python API — GeometryCacheStreamer Module

**1 types** from the `GeometryCacheStreamer` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `GeometryCacheStreamerSettings`

---

## Classes

### `unreal.GeometryCacheStreamerSettings`
Inherits: `DeveloperSettings` | Header: `GeometryCacheStreamerSettings.h`

Settings for the GeometryCache streamer

**Properties** (2):
  - `look_ahead_buffer`: `float` — [Read-Write] The amount of animation (in seconds) to stream ahead of time (per stream) (float)
  - `max_memory_allowed`: `float` — [Read-Write] The maximum total amount of streamed data allowed in memory (for all streams) (float)
