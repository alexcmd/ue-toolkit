# UE Python API — SkeletonTemplateFrameworkEditor Module

**2 types** from the `SkeletonTemplateFrameworkEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SkeletonBindingFactory`, `SkeletonTemplateFactory`

---

## Classes

### `unreal.SkeletonBindingFactory`
Inherits: `Factory` | Header: `SkeletonBindingFactory.h`

Skeleton Binding Factory

### `unreal.SkeletonTemplateFactory`
Inherits: `Factory` | Header: `SkeletonTemplateFactory.h`

Skeleton Template Factory
