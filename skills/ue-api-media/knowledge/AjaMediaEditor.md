# UE Python API — AjaMediaEditor Module

**2 types** from the `AjaMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AjaMediaOutputFactoryNew`, `AjaMediaSourceFactoryNew`

---

## Classes

### `unreal.AjaMediaOutputFactoryNew`
Inherits: `Factory` | Header: `AjaMediaOutputFactoryNew.h`

Implements a factory for UAjaMediaOutput objects.

### `unreal.AjaMediaSourceFactoryNew`
Inherits: `Factory` | Header: `AjaMediaSourceFactoryNew.h`

Implements a factory for UAjaMediaSource objects.
