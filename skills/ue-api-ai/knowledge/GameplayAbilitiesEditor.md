# UE Python API — GameplayAbilitiesEditor Module

**2 types** from the `GameplayAbilitiesEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `GameplayAbilityAuditRow`, `GameplayAbilitiesBlueprintFactory`

---

## Classes

### `unreal.GameplayAbilityAuditRow`
Inherits: `TableRowBase` | Header: `GameplayAbilityAudit.h`

This contains all of the data we gather about a Blueprint during the Audit process

### `unreal.GameplayAbilitiesBlueprintFactory`
Inherits: `Factory` | Header: `GameplayAbilitiesBlueprintFactory.h`

Gameplay Abilities Blueprint Factory
