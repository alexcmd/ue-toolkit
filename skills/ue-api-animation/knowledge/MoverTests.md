# UE Python API — MoverTests Module

**1 types** from the `MoverTests` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `TestCustomLayeredMove`

---

## Classes

### `unreal.TestCustomLayeredMove`
Inherits: `LayeredMoveBase` | Header: `TestCustomLayeredMoves.h`

Custom Layered move for testing purposes - acts the same as the Launch layered move

**Properties** (2):
  - `force_movement_mode`: `Name` — [Read-Write] Optional movement mode name to force the actor into before applying the impulse velocit...
  - `launch_velocity`: `Vector` — [Read-Write] Velocity to apply to the actor. Could be additive or overriding depending on MixMode se...
