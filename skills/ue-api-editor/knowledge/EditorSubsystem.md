# UE Python API — EditorSubsystem Module

**1 types** from the `EditorSubsystem` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `EditorSubsystem`

---

## Classes

### `unreal.EditorSubsystem`
Inherits: `DynamicSubsystem` | Header: `EditorSubsystem.h`

UEditorSubsystem Base class for auto instanced and initialized systems that share the lifetime of the Editor
