# UE Python API — HarmonixMidiEditor Module

**1 types** from the `HarmonixMidiEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MidiFileFactory`

---

## Classes

### `unreal.MidiFileFactory`
Inherits: `Factory` | Header: `MidiFileFactory.h`

Imports a standard midi file
