# UE Python API — USDClassesEditor Module

**1 types** from the `USDClassesEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `UsdAssetCacheFactory`

---

## Classes

### `unreal.UsdAssetCacheFactory`
Inherits: `Factory` | Header: `USDAssetCacheFactory.h`

Usd Asset Cache Factory
