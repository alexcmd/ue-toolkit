# UE Python API — MotoSynthEditor Module

**2 types** from the `MotoSynthEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MotoSynthPresetFactory`, `MotoSynthSourceFactory`

---

## Classes

### `unreal.MotoSynthPresetFactory`
Inherits: `Factory` | Header: `MotoSynthSourceFactory.h`

Moto Synth Preset Factory

### `unreal.MotoSynthSourceFactory`
Inherits: `Factory` | Header: `MotoSynthSourceFactory.h`

Moto Synth Source Factory
