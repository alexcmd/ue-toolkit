# UE Python API — RemoteSession Module

**2 types** from the `RemoteSession` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `RemoteSessionMediaCapture`, `RemoteSessionMediaOutput`

---

## Classes

### `unreal.RemoteSessionMediaCapture`
Inherits: `MediaCapture` | Header: `RemoteSessionMediaOutput.h`

Remote Session Media Capture

### `unreal.RemoteSessionMediaOutput`
Inherits: `MediaOutput` | Header: `RemoteSessionMediaOutput.h`

Remote Session Media Output
