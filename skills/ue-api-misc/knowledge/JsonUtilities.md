# UE Python API — JsonUtilities Module

**1 types** from the `JsonUtilities` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `JsonObjectWrapper`

---

## Classes

### `unreal.JsonObjectWrapper`
Inherits: `StructBase` | Header: `JsonObjectWrapper.h`

UStruct that holds a JsonObject, can be used by structs passed to JsonObjectConverter to pass through JsonObjects directly
