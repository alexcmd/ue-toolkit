# UE Python API — DisplayClusterLightCardExtender Module

**1 types** from the `DisplayClusterLightCardExtender` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `DisplayClusterPositionalParams`

---

## Classes

### `unreal.DisplayClusterPositionalParams`
Inherits: `StructBase` | Header: `DisplayClusterPositionalParams.h`

Positional location and rotation parameters for use with nDisplay stage actors. Note that the origin point is purposely not included as these parameters are meant to be shared between actors with diff...

**Properties** (8):
  - `distance_from_center`: `float` — [Read-Write] (double)
  - `latitude`: `float` — [Read-Write] (double)
  - `longitude`: `float` — [Read-Write] (double)
  - `pitch`: `float` — [Read-Write] (double)
  - `radial_offset`: `float` — [Read-Write] (double)
  - `scale`: `Vector2D` — [Read-Write] (Vector2D)
  - `spin`: `float` — [Read-Write] (double)
  - `yaw`: `float` — [Read-Write] (double)
