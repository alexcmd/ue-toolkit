# UE Python API — LiveLinkFaceImporter Module

**1 types** from the `LiveLinkFaceImporter` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `LiveLinkFaceImporterFactory`

---

## Classes

### `unreal.LiveLinkFaceImporterFactory`
Inherits: `Factory` | Header: `LiveLinkFaceImporterFactory.h`

Imports a CSV file created by the Live Link Face iOS app when recording facial animation.
