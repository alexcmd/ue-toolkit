# UE Python API — MegascansPlugin Module

**1 types** from the `MegascansPlugin` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `VersionInfoHandler`

---

## Classes

### `unreal.VersionInfoHandler`
Inherits: `Object` | Header: `VersionInfoHandler.h`

Version Info Handler

**Methods** (1):
  - `get()` -> `VersionInfoHandler` [classmethod] — Get
