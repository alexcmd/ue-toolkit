# UE Python API — RemoteControlUI Module

**1 types** from the `RemoteControlUI` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `RemoteControlPresetFactory`

---

## Classes

### `unreal.RemoteControlPresetFactory`
Inherits: `Factory` | Header: `RemoteControlPresetFactory.h`

Implements a factory for URemoteControlPreset objects.
