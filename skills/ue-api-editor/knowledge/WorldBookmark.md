# UE Python API — WorldBookmark Module

**1 types** from the `WorldBookmark` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `WorldBookmarkFactory`

---

## Classes

### `unreal.WorldBookmarkFactory`
Inherits: `Factory` | Header: `WorldBookmarkFactory.h`

World Bookmark Factory
