# UE Python API — CodeEditor Module

**1 types** from the `CodeEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `CodeProjectFactory`

---

## Classes

### `unreal.CodeProjectFactory`
Inherits: `Factory` | Header: `CodeProjectFactory.h`

Code Project Factory
