# UE Python API — LocalizableMessage Module

**1 types** from the `LocalizableMessage` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `LocalizableMessage`

---

## Classes

### `unreal.LocalizableMessage`
Inherits: `StructBase` | Header: `LocalizableMessage.h`

Localizable Message
