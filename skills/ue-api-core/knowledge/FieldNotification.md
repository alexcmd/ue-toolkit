# UE Python API — FieldNotification Module

**2 types** from the `FieldNotification` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `FieldNotificationId`, `FieldValueChangedDynamicDelegate`

---

## Classes

### `unreal.FieldNotificationId`
Inherits: `StructBase` | Header: `FieldNotificationId.h`

namespace

**Properties** (1):
  - `field_name`: `Name` — [Read-Write] (Name)

### `unreal.FieldValueChangedDynamicDelegate`
Inherits: `DelegateBase` | Header: `INotifyFieldValueChanged.h`

Field Value Changed Dynamic Delegate Delegate Signature
