# UE Python API — GameplayCamerasEditor Module

**5 types** from the `GameplayCamerasEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `CameraAssetFactory`, `CameraRigAssetFactory`, `CameraRigProxyAssetFactory`, `CameraShakeAssetFactory`, `CameraVariableCollectionFactory`

---

## Classes

### `unreal.CameraAssetFactory`
Inherits: `Factory` | Header: `CameraAssetFactory.h`

Implements a factory for UCameraAsset objects.

### `unreal.CameraRigAssetFactory`
Inherits: `Factory` | Header: `CameraRigAssetFactory.h`

Implements a factory for UCameraRigAsset objects.

### `unreal.CameraRigProxyAssetFactory`
Inherits: `Factory` | Header: `CameraRigProxyAssetFactory.h`

Implements a factory for UCameraRigProxyAsset objects.

### `unreal.CameraShakeAssetFactory`
Inherits: `Factory` | Header: `CameraShakeAssetFactory.h`

Implements a factory for UCameraShakeAsset objects.

### `unreal.CameraVariableCollectionFactory`
Inherits: `Factory` | Header: `CameraVariableCollectionFactory.h`

Implements a factory for camera variable collections.
