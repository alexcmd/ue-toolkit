# UE Python API — VPSettings Module

**1 types** from the `VPSettings` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `VPSettings`

---

## Classes

### `unreal.VPSettings`
Inherits: `Object` | Header: `VPSettings.h`

**Methods** (1):
  - `get_vp_settings()` -> `VPSettings` [classmethod] — Get VPSettings

**Properties** (6):
  - `aperture_presets`: `None` — [Read-Write] Default Apertures for for Virtual Camera (Array[float])
  - `default_iso_presets`: `None` — [Read-Write] Default ISOs for Virtual Camera (Array[float])
  - `default_shutter_speed_presets`: `None` — [Read-Write] Default Shutter Speeds (1/s) for Virtual Camera (Array[float])
  - `director_name`: `str` — [Read-Write] Default Name for Director (str)
  - `focal_length_presets`: `None` — [Read-Write] Default Kit of Focal Lengths for Virtual Camera (Array[float])
  - `show_name`: `str` — [Read-Write] Default Name for Project (str)
