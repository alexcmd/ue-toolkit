# UE Python API — SubtitlesAndClosedCaptionsEditor Module

**1 types** from the `SubtitlesAndClosedCaptionsEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SubtitleFactory`

---

## Classes

### `unreal.SubtitleFactory`
Inherits: `Factory` | Header: `SubtitleFactory.h`

Subtitle Factory
