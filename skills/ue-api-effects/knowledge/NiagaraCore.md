# UE Python API — NiagaraCore Module

**2 types** from the `NiagaraCore` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `NiagaraDataInterfaceBase`, `NiagaraMergeable`

---

## Classes

### `unreal.NiagaraDataInterfaceBase`
Inherits: `NiagaraMergeable` | Header: `NiagaraDataInterfaceBase.h`

Base class for all Niagara data interfaces.

### `unreal.NiagaraMergeable`
Inherits: `Object` | Header: `NiagaraMergeable.h`

Niagara Mergeable
