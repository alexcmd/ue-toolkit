# UE Python API — AndroidMediaEditor Module

**1 types** from the `AndroidMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AndroidFileMediaSourceFactory`

---

## Classes

### `unreal.AndroidFileMediaSourceFactory`
Inherits: `Factory` | Header: `AndroidFileMediaSourceFactory.h`

Implements a factory for UFileMediaSource objects.
