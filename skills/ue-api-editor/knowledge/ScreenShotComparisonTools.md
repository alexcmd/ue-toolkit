# UE Python API — ScreenShotComparisonTools Module

**2 types** from the `ScreenShotComparisonTools` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ImageTolerance`, `ImageTolerancePreset`

---

## Classes

### `unreal.ImageTolerance`
Inherits: `StructBase` | Header: `ImageComparer.h`

Image Tolerance

### `unreal.ImageTolerancePreset`
Inherits: `EnumBase` | Header: `ImageComparer.h`

EImage Tolerance Preset

**Properties** (5):
  - `CUSTOM`: `ImageTolerancePreset = Ellipsis` — 4
  - `IGNORE_ANTI_ALIASING`: `ImageTolerancePreset = Ellipsis` — 2
  - `IGNORE_COLORS`: `ImageTolerancePreset = Ellipsis` — 3
  - `IGNORE_LESS`: `ImageTolerancePreset = Ellipsis` — 1
  - `IGNORE_NOTHING`: `ImageTolerancePreset = Ellipsis` — 0
