# UE Python API — CQTest Module

**1 types** from the `CQTest` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `TestGameInstance`

---

## Classes

### `unreal.TestGameInstance`
Inherits: `GameInstance` | Header: `TestGameInstance.h`

Test Game Instance
