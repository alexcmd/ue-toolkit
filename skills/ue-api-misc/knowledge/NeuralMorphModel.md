# UE Python API — NeuralMorphModel Module

**3 types** from the `NeuralMorphModel` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `NeuralMorphMaskInfo`, `NeuralMorphModel`, `NeuralMorphMode`

---

## Classes

### `unreal.NeuralMorphMaskInfo`
Inherits: `StructBase` | Header: `NeuralMorphTypes.h`

Information needed to generate the mask for a specific bone. This includes a list of bone names. Each bone has a skinning influence mask. The final mask will be a merge of all the bone masks of bones ...

### `unreal.NeuralMorphModel`
Inherits: `MLDeformerMorphModel` | Header: `NeuralMorphModel.h`

The neural morph model. This generates a set of highly compressed morph targets to approximate a target deformation based on bone rotations and/or curve inputs. During inference, the neural network in...

**Properties** (15):
  - `batch_size`: `int` — [Read-Write] The number of frames per batch when training the model. (int32)
  - `bone_group_mask_infos`: `None` — [Read-Write]
deprecated: This property has been deprecated, please use BoneGroupMaskInfoMap instead....
  - `bone_mask_infos`: `None` — [Read-Write]
deprecated: This property has been deprecated, please use BoneMaskInfoMap instead. (Map...
  - `enable_bone_masks`: `bool` — [Read-Write] Enable the use of per bone and bone group masks.
When enabled, an influence mask is gen...
  - `global_num_hidden_layers`: `int` — [Read-Write] The number of hidden layers that the neural network model will have.nHigher numbers wil...
  - `global_num_morph_targets`: `int` — [Read-Write] The number of morph targets to generate in total.
Higher numbers result in better appro...
  - `global_num_neurons_per_layer`: `int` — [Read-Write] The number of units/neurons per hidden layer. Higher numbers will slow down performance...
  - `learning_rate`: `float` — [Read-Write] The learning rate used during the model training. (float)
  - `local_num_hidden_layers`: `int` — [Read-Write] The number of hidden layers that the neural network model will have.
Higher numbers wil...
  - `local_num_morph_targets_per_bone`: `int` — [Read-Write] The number of morph targets to generate per bone, curve or group.
Higher numbers result...
  - `local_num_neurons_per_layer`: `int` — [Read-Write] The number of units/neurons per hidden layer.
Higher numbers will slow down performance...
  - `mode`: `NeuralMorphMode` — [Read-Write] The mode that the neural network will operate in.
Local mode means there is one tiny ne...
  - `num_iterations`: `int` — [Read-Write] The number of iterations to train the model for.
If you are quickly iterating then arou...
  - `regularization_factor`: `float` — [Read-Write] The regularization factor. Higher values can help generate more sparse morph targets, b...
  - `smooth_loss_beta`: `float` — [Read-Write] The beta parameter in the smooth L1 loss function, which describes below which absolute...

### `unreal.NeuralMorphMode`
Inherits: `EnumBase` | Header: `NeuralMorphTypes.h`

The mode of the model, either local or global. In local mode the network contains a super simple neural network for each bone, while in global mode all bones and curves are input to one larger fully c...

**Properties** (2):
  - `GLOBAL`: `NeuralMorphMode = Ellipsis` — There is one fully connected neural network that generates a set of morph targets.
This has a slight...
  - `LOCAL`: `NeuralMorphMode = Ellipsis` — Each bone creates a set of morph targets and has its own small neural network.
The local mode can al...
