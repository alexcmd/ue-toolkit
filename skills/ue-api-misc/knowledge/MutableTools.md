# UE Python API — MutableTools Module

**1 types** from the `MutableTools` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MutableMultipleTagPolicy`

---

## Classes

### `unreal.MutableMultipleTagPolicy`
Inherits: `EnumBase` | Header: `NodeModifier.h`

Despite being an UEnum, this is not always version-serialized (in MutableTools). Beware of changing the enum options or order.

**Properties** (2):
  - `ALL_REQUIRED`: `MutableMultipleTagPolicy = Ellipsis` — 1
  - `ONLY_ONE_REQUIRED`: `MutableMultipleTagPolicy = Ellipsis` — 0
