# UE Python API — MovieRenderPipelineCore Module

**186 types** from the `MovieRenderPipelineCore` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MovieGraphActorQueryEntry`, `MovieGraphBranch`, `MovieGraphEvaluationResult`, `MovieGraphFilenameResolveParams`, `MovieGraphImagePreviewData`, `MovieGraphInitConfig`, `MovieGraphMetadataAttribute`, `MovieGraphNamedResolution`, `MovieGraphPinProperties`, `MovieGraphPostRenderImageSequencePlayOptions`, `MovieGraphPostRenderSettings`, `MovieGraphPostRenderVideoPlayOptions`, `MovieGraphPropertyInfo`, `MovieGraphRenderDataIdentifier`, `MovieGraphRenderLayerOutputData`, `MovieGraphRenderOutputData`, `MovieGraphResolveArgs`, `MovieGraphSequencePlaybackRangeBound`, `MovieGraphStringArray`, `MovieGraphTimeStepData`, ... (186 total)

---

## Classes

### `unreal.MovieGraphActorQueryEntry`
Inherits: `StructBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Contains the actors and components to match within the Actor condition group query.

**Properties** (2):
  - `actor_to_match`: `Actor` — [Read-Write] The query must match this actor in order to be a match. If these are editor actors, the...
  - `components_to_match`: `None` — [Read-Write] The query must match these components in order to be a match. These must be contained w...

### `unreal.MovieGraphBranch`
Inherits: `StructBase` | Header: `MovieGraphTraversalContext.h`

Movie Graph Branch

**Properties** (1):
  - `branch_name`: `Name` — [Read-Write] (Name)

### `unreal.MovieGraphEvaluationResult`
Inherits: `StructBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Provides the actors/components returned by a collection or condition group evaluation.

**Properties** (2):
  - `matching_actors`: `None` [Read-Only] — [Read-Only] The actors that were matched during evaluation. (Set[Actor])
  - `matching_components`: `None` [Read-Only] — [Read-Only] The components that were matched during evaluation. (Set[ActorComponent])

### `unreal.MovieGraphFilenameResolveParams`
Inherits: `StructBase` | Header: `MovieGraphFilenameResolveParams.h`

This data structure contains all of the information needed to resolve a filename format string from the Movie Graph. During runtime the values are generated by the actual rendering and then placed int...

**Properties** (19):
  - `default_frame_rate`: `FrameRate` — [Read-Write] What is the default Frame Rate? This will be overwritten by the Output Setting from the...
  - `ensure_absolute_path`: `bool` — [Read-Write] If true, If the format string is a relative path, then the resulting resolved path will...
  - `evaluated_config`: `MovieGraphEvaluatedConfig` — [Read-Write] Optional. If specified, used to resolve some tokens related to settings in the shot (su...
  - `file_metadata`: `None` — [Read-Write] A key/value pair that maps metadata names to their values. Output is only supported in ...
  - `file_name_format_overrides`: `None` — [Read-Write] A map between â{format}â tokens and their values. These are applied after the auto-...
  - `file_name_override`: `str` — [Read-Write] If specified, this is the filename that will be used instead of building it from the Ou...
  - `force_relative_frame_numbers`: `bool` — [Read-Write] If true, force format strings (like {frame_number}) to resolve using the relative versi...
  - `frame_number_offset`: `int` — [Read-Write] Added onto the various FrameNumber settings automatically. Can be used to offset values...
  - `initialization_time`: `DateTime` — [Read-Write] The initialization time for this job. Used to resolve time-based format arguments. This...
  - `initialization_time_offset`: `Timespan` — [Read-Write] What offset should be applied to InitializationTime when generating {time} related file...
  - `job`: `MoviePipelineExecutorJob` — [Read-Write] Optional. If specified, used to resolve some tokens related to the job. (MoviePipelineE...
  - `render_data_identifier`: `MovieGraphRenderDataIdentifier` — [Read-Write] This is used to fill out tokens related to the render data that the file represents.
Us...
  - `root_frame_number`: `int` — If the Sequence PlaybackRange starts on 50, this value would be 50 on the first frame.)
Used to fill...
  - `root_frame_number_rel`: `int` — if your sequence started at 35, this would be 35 as well, but supports time dilation because it will...
  - `shot`: `MoviePipelineExecutorShot` — [Read-Write] Optional. If specified, used to resolve the shot name. No settings from this shot are e...
  - `shot_frame_number`: `int` — [Read-Write] Frame Number for the Shot (matching what you would see in Sequencer at the sub-sequence...
  - `shot_frame_number_rel`: `int` — if your shot started at 35, this would be 35 as well, but supports time dilation because it will cou...
  - `version`: `int` — [Read-Write] Used to resolve {version} format arguments. (int32)
  - `zero_pad_frame_number_count`: `int` — 5 => 0005 with a count of 4. (int32) [Read-Write] When converting frame numbers to strings, how many...

### `unreal.MovieGraphImagePreviewData`
Inherits: `StructBase` | Header: `MovieGraphDataTypes.h`

Movie Graph Image Preview Data

**Properties** (3):
  - `identifier`: `MovieGraphRenderDataIdentifier` [Read-Only] — [Read-Only] The identifier for the image, containing the branch name, renderer, etc. (MovieGraphRend...
  - `multiple_camera_names`: `bool` [Read-Only] — multi-camera rendering) (bool) [Read-Only] If true, then there is more than one camera name being us...
  - `texture`: `Texture` [Read-Only] — [Read-Only] The texture this preview image was rendered to. (Texture)

### `unreal.MovieGraphInitConfig`
Inherits: `StructBase` | Header: `MovieGraphDataTypes.h`

Movie Graph Init Config

**Properties** (4):
  - `audio_renderer_class`: `Class` — [Read-Write] Which class should the UMovieGraphPipeline use to generate audio. Defaults to
UMovieGra...
  - `data_source_class`: `Class` — [Read-Write] Which class should the UMovieGraphPipeline use to build time ranges from, and
during ev...
  - `render_viewport`: `bool` — [Read-Write] Should the UMovieGraphPipeline render the full player viewport? Defaults
to false (so n...
  - `renderer_class`: `Class` — [Read-Write] Which class should the UMovieGraphPipeline use to look for render layers and
request re...

### `unreal.MovieGraphMetadataAttribute`
Inherits: `StructBase` | Header: `MovieGraphSetMetadataAttributesNode.h`

Represents a metadata attribute that can be included in a file.

**Properties** (3):
  - `is_enabled`: `bool` — [Read-Write] Enable state. If disabled, this metadata attribute wonât be added to the file. (bool)
  - `name`: `str` — [Read-Write] The name of the metadata attribute. (str)
  - `value`: `str` — [Read-Write] The value of the metadata attribute. (str)

### `unreal.MovieGraphNamedResolution`
Inherits: `StructBase` | Header: `MovieGraphNamedResolution.h`

Holds information about a screen resolution to be used for rendering.

**Properties** (3):
  - `description`: `str` — [Read-Write] The description text for this screen resolution. (str)
  - `profile_name`: `Name` — [Read-Write] The name of the resolution this links to (Name)
  - `resolution`: `IntPoint` — [Read-Write] The screen resolution (in pixels). (IntPoint)

### `unreal.MovieGraphPinProperties`
Inherits: `StructBase` | Header: `MovieGraphPin.h`

Movie Graph Pin Properties

**Properties** (7):
  - `allow_multiple_connections`: `bool` — [Read-Write] Whether this pin can accept multiple connections. (bool)
  - `is_branch`: `bool` — [Read-Write] Whether this pin represents a branch. If it does not represent a branch, then it is a v...
  - `is_built_in`: `bool` — [Read-Write] Whether this pin is built-in (ie, the pin ships with the node and cannot be removed). O...
  - `is_wildcard`: `bool` — [Read-Write] Whether this pin represents a wildcard, which means it can be any type (including being...
  - `label`: `Name` — [Read-Write] The name assigned to the pin. (Name)
  - `type`: `MovieGraphValueType` — [Read-Write] The type of the pin. If the pin represents a branch, this type is ignored. (MovieGraphV...
  - `type_object`: `Object` — [Read-Write] The value type of the pin, if the type is an enum, struct, class, or object. (Object)

### `unreal.MovieGraphPostRenderImageSequencePlayOptions`
Inherits: `MovieGraphPostRenderVideoPlayOptions` | Header: `MoviePipelinePostRenderSettings.h`

Options that dictate how images should be played back after a render completes.

**Properties** (2):
  - `frame_range_notation`: `MovieGraphFrameRangeNotation` — [Read-Write] The symbol that will be used to replace the frame number in image paths when providing ...
  - `playback_range`: `MovieGraphImageSequencePlaybackRange` — [Read-Write] The range of frames to play back (only applicable to image sequences). (MovieGraphImage...

### `unreal.MovieGraphPostRenderSettings`
Inherits: `StructBase` | Header: `MoviePipelinePostRenderSettings.h`

Settings that dictate how media is played back after a render finishes.

**Properties** (6):
  - `output_type_playback`: `MovieGraphOutputTypePlayback` — [Read-Write] If set to Use Priority Order, the Output Type Priority Order list will be used to deter...
  - `output_type_priority_order`: `None` — [Read-Write] When deciding which media to play, media with extensions ranked higher in this list wil...
  - `post_render_apple_pro_res_play_options`: `MovieGraphPostRenderVideoPlayOptions` — [Read-Write] These options are used to determine how to play Apple ProRes movies. (MovieGraphPostRen...
  - `post_render_avid_d_nx_hr_play_options`: `MovieGraphPostRenderVideoPlayOptions` — [Read-Write] These options are used to determine how to play Avid DNxHR movies. (MovieGraphPostRende...
  - `post_render_image_sequence_play_options`: `MovieGraphPostRenderImageSequencePlayOptions` — [Read-Write] These options are used to determine how to play the rendered images. (MovieGraphPostRen...
  - `post_render_mp4_play_options`: `MovieGraphPostRenderVideoPlayOptions` — [Read-Write] These options are used to determine how to play H.264 MP4 movies. (MovieGraphPostRender...

### `unreal.MovieGraphPostRenderVideoPlayOptions`
Inherits: `StructBase` | Header: `MoviePipelinePostRenderSettings.h`

Options that dictate how videos should be played back after a render completes.

**Properties** (5):
  - `additional_command_line_arguments`: `str` — [Read-Write] If using a custom viewer, these command line arguments are passed along. (str)
  - `job_playback`: `MovieGraphJobPlaybackRange` — [Read-Write] The jobs that should have their media played. (MovieGraphJobPlaybackRange)
  - `playback_method`: `MovieGraphPlaybackMethod` — [Read-Write] Determines which application is used to open rendered media for playback. (MovieGraphPl...
  - `player_executable`: `FilePath` — [Read-Write] If using a custom viewer, this is the path to the executable to use. (FilePath)
  - `render_layer_playback`: `MovieGraphRenderLayerPlaybackRange` — [Read-Write] The render layers that should have their media played. (MovieGraphRenderLayerPlaybackRa...

### `unreal.MovieGraphPropertyInfo`
Inherits: `StructBase` | Header: `MovieGraphNode.h`

Information about a property that currently is (or can be) exposed on a node.

**Properties** (5):
  - `context_menu_name`: `Text` [Read-Only] — [Read-Only] The display name of the property which will be shown in the context menu. If empty, the ...
  - `is_dynamic_property`: `bool` [Read-Only] — [Read-Only] Whether this property is dynamic (ie, it does not correspond to a native UPROPERTY on th...
  - `name`: `Name` [Read-Only] — [Read-Only] The name of the property. (Name)
  - `promotion_name`: `Name` [Read-Only] — [Read-Only] If this property is promoted, this is the name of the variable that is created. If empty...
  - `value_type`: `MovieGraphValueType` [Read-Only] — [Read-Only] The type of the value pointed to by the property. (MovieGraphValueType)

### `unreal.MovieGraphRenderDataIdentifier`
Inherits: `StructBase` | Header: `MovieGraphRenderDataIdentifier.h`

This data structure can be used to identify what render data a set of pixels represents by knowing what the render layer name is, what renderer produced it, if itâs a sub-resource, and what camera i...

**Properties** (5):
  - `camera_name`: `str` — [Read-Write] The name of the camera being used for this render. (str)
  - `layer_name`: `str` — [Read-Write] The Render Layer name (as defined by the âRender Layerâ node.). This is effectively...
  - `renderer_name`: `str` — [Read-Write] Which renderer was used to produce this image (âpanoramicâ âdeferredâ âpath t...
  - `root_branch_name`: `Name` — [Read-Write] The root branch name from the Outputs node that this identifier is for. This is useful ...
  - `sub_resource_name`: `str` — âbeautyâ, âobject idâ, âdepthâ, etc.) (str) [Read-Write] A sub-resource name for the ren...

### `unreal.MovieGraphRenderLayerOutputData`
Inherits: `StructBase` | Header: `MovieGraphRenderDataIdentifier.h`

Movie Graph Render Layer Output Data

**Properties** (4):
  - `file_paths`: `None` [Read-Only] — [Read-Only] A list of file paths on disk (in order) that were generated for this particular render p...
  - `frame_templated_file_paths`: `None` [Read-Only] — [Read-Only] The âframe templatedâ file paths. This groups together a set of output frames by fra...
  - `node_type_to_file_paths`: `None` [Read-Only] — [Read-Only] Maps a node type to all of the files that the node type generated. (Map[SoftClassPath,Mo...
  - `render_layer_index`: `int` [Read-Only] — [Read-Only] The index of the render layer these files belong to. Index 0 is the first render layer r...

### `unreal.MovieGraphRenderOutputData`
Inherits: `StructBase` | Header: `MovieGraphRenderDataIdentifier.h`

Movie Graph Render Output Data

**Properties** (2):
  - `render_layer_data`: `None` [Read-Only] — [Read-Only] A mapping between render layers (such as âbeautyâ) and an array containing the files...
  - `shot`: `MoviePipelineExecutorShot` [Read-Only] — [Read-Only] Which shot is this output data for. (MoviePipelineExecutorShot)

### `unreal.MovieGraphResolveArgs`
Inherits: `StructBase` | Header: `MovieGraphFilenameResolveParams.h`

This data structure contains a list of key-value pairs (as strings) for both filename resolving, and file metadata. They are stored as two separate arrays (as metadata is often a bit more verbose/huma...

**Properties** (2):
  - `file_metadata`: `None` — [Read-Write] A set of Key/Value pairs for file metadata for file formats that support metadata. (Map...
  - `filename_arguments`: `None` — [Read-Write] A set of Key/Value pairs for output filename format strings (without {}) and their valu...

### `unreal.MovieGraphSequencePlaybackRangeBound`
Inherits: `StructBase` | Header: `MovieGraphGlobalOutputSettingNode.h`

Movie Graph Sequence Playback Range Bound

**Properties** (2):
  - `type`: `MovieGraphSequenceRangeType` — [Read-Write] By default the render will use the Playback Range Start/End frame to determine what to ...
  - `value`: `int` — [Read-Write] If Type is set to Custom, then this value is used for the Playback Range Start or End f...

### `unreal.MovieGraphStringArray`
Inherits: `StructBase` | Header: `MovieGraphRenderDataIdentifier.h`

A simple struct wrapper around a string array so it can be included in a TMap.

**Properties** (1):
  - `array`: `None` [Read-Only] — [Read-Only] (Array[str])

### `unreal.MovieGraphTimeStepData`
Inherits: `StructBase` | Header: `MovieGraphTimeStepData.h`

This data structure needs to be filled out each frame by the UMovieGraphTimeStepBase, which will eventually be passed to the renderer. It controls per-sample behavior such as the delta time, if this i...

**Properties** (22):
  - `discard_output`: `bool` — [Read-Write] Should the rendered result be discarded after a render? This will skip any
accumulators...
  - `evaluated_config`: `MovieGraphEvaluatedConfig` — [Read-Write] The evaluated config holds the configuration used for this given frame. This pointer
ca...
  - `frame_delta_time`: `float` — [Read-Write] (float)
  - `frame_rate`: `FrameRate` — [Read-Write] (FrameRate)
  - `has_relative_time_been_used`: `bool` — [Read-Write] True if time dilation was used on this frame, or any in the past, even if WorldTimeDila...
  - `is_first_temporal_sample_for_frame`: `bool` — [Read-Write] Should be set to true for the first sample of each output frame. Used to determine
if v...
  - `is_last_temporal_sample_for_frame`: `bool` — [Read-Write] Should be set to true for the last sample of each output frame. Can be true at
the same...
  - `motion_blur_fraction`: `float` — [Read-Write] (float)
  - `output_frame_number`: `int` — [Read-Write] * Relative to zero for the entire render. (int32)
  - `rendered_frame_number`: `int` — [Read-Write] (int32)
  - `requires_accumulator`: `bool` — [Read-Write] Should be set to true for every sample if there is more than one temporal sample
making...
  - `root_frame_number`: `FrameNumber` — [Read-Write] * The current frame number at the root (sequence) level.
* This is the same number as w...
  - `root_time_code`: `Timecode` — [Read-Write] The current timecode at the root (sequence) level. Note that this is adjusted according...
  - `shot_frame_number`: `FrameNumber` — [Read-Write] * The current frame number at the shot level.
* This is the same number as what is show...
  - `shot_output_frame_number`: `int` — [Read-Write] * Relative to zero for the current shot. (int32)
  - `shot_time_code`: `Timecode` — [Read-Write] The current timecode at the shot level. Note that this is adjusted according to the tim...
  - `spatial_sample_count`: `int` — [Read-Write] The maximum number of Spatial Samples this frame is expected to be able to process. (in...
  - `spatial_sample_index`: `int` — [Read-Write] Index out of SpatialSampleCount that weâre on. (int32)
  - `temporal_sample_count`: `int` — [Read-Write] * What is the maximum number of Temporal Samples this frame is expected to be able to p...
  - `temporal_sample_index`: `int` — [Read-Write] * Index out of TemporalSampleCount weâre on. No guarantee that weâll ever reach Ind...
  - `world_seconds`: `float` — [Read-Write] (float)
  - `world_time_dilation`: `float` — [Read-Write] (float)

### `unreal.MovieGraphTraversalContext`
Inherits: `StructBase` | Header: `MovieGraphTraversalContext.h`

Movie Graph Traversal Context

**Properties** (7):
  - `job`: `MoviePipelineExecutorJob` — [Read-Write] The job in the queue this traversal context is for. Needed to fetch variable values fro...
  - `render_data_identifier`: `MovieGraphRenderDataIdentifier` — [Read-Write] The name of the render resource this state was captured for. (MovieGraphRenderDataIdent...
  - `root_graph`: `MovieGraphConfig` — [Read-Write] The root graph to start our traversal from. This could be a shared config for the whole...
  - `shot`: `MoviePipelineExecutorShot` — [Read-Write] The shot in the queue this traversal context is for (if any). (MoviePipelineExecutorSho...
  - `shot_count`: `int` — [Read-Write] The total number of shots being rendered for this job. This is from the active shot lis...
  - `shot_index`: `int` — [Read-Write] Which shot (out of ShotCount) is this time step for? (int32)
  - `time`: `MovieGraphTimeStepData` — [Read-Write] The time data(output frame, delta times, etc.) (MovieGraphTimeStepData)

### `unreal.MovieGraphVersioningSettings`
Inherits: `StructBase` | Header: `MovieGraphGlobalOutputSettingNode.h`

Movie Graph Versioning Settings

**Properties** (2):
  - `auto_versioning`: `bool` — [Read-Write] If true, {version} tokens specified in the Output Directory and File Name Format proper...
  - `version_number`: `int` — [Read-Write] The value to use for the version token if versions are not automatically incremented (A...

### `unreal.MoviePipelineCameraCutInfo`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

ToDo: Rename this to segment.

### `unreal.MoviePipelineConsoleVariableEntry`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

Represents a console variable override that can be enabled/disabled.

**Properties** (3):
  - `is_enabled`: `bool` [Read-Only] — [Read-Only] Enable state. If disabled, this cvar entry will be ignored when resolving the final valu...
  - `name`: `str` [Read-Only] — [Read-Only] The name of the console variable. (str)
  - `value`: `float` [Read-Only] — [Read-Only] The value of the console variable. (float)

### `unreal.MoviePipelineFilenameResolveParams`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

Movie Pipeline Filename Resolve Params

**Properties** (17):
  - `additional_frame_number_offset`: `int` — [Read-Write] Additional offset added onto the offset provided by the Output Settings in the Job. Req...
  - `camera_name_override`: `str` — [Read-Write] Name used by the {camera_name} format tag. If specified, this will override the camera ...
  - `file_metadata`: `None` — [Read-Write] A key/value pair that maps metadata names to their values. Output is only supported in ...
  - `file_name_format_overrides`: `None` — [Read-Write] A map between â{format}â tokens and their values. These are applied after the auto-...
  - `file_name_override`: `str` — [Read-Write] Optional. If specified this is the filename that will be used instead of automatically ...
  - `force_relative_frame_numbers`: `bool` — [Read-Write] If true, force format strings (like {frame_number}) to resolve using the relative versi...
  - `frame_number`: `int` — If the Sequence PlaybackRange starts on 50, this value would be 50 on the first frame. (int32) [Read...
  - `frame_number_rel`: `int` — If sequence PlaybackRange starts on 50, this value would be 0 on the first frame. (int32) [Read-Writ...
  - `frame_number_shot`: `int` — [Read-Write] Frame Number for the Shot (matching what you would see in Sequencer at the sub-sequence...
  - `frame_number_shot_rel`: `int` — [Read-Write] Frame Number for the Shot (relative to 0, not what you would see in the Sequencer timel...
  - `initialization_time`: `DateTime` — [Read-Write] The initialization time for this job. Used to resolve time-based format arguments. (Dat...
  - `initialization_time_offset`: `Timespan` — [Read-Write] What offset should be applied to InitializationTime when generating {time} related file...
  - `initialization_version`: `int` — [Read-Write] The version for this job. Used to resolve version format arguments. (int32)
  - `job`: `MoviePipelineExecutorJob` — [Read-Write] Required. This is the job all of the settings should be pulled from. (MoviePipelineExec...
  - `shot_name_override`: `str` — [Read-Write] Name used by the {shot_name} format tag. If specified, this will override the shot name...
  - `shot_override`: `MoviePipelineExecutorShot` — [Read-Write] Optional. If specified, settings will be pulled from this shot (if overriden by the sho...
  - `zero_pad_frame_number_count`: `int` — 5 => 0005 with a count of 4. (int32) [Read-Write] When converitng frame numbers to strings, how many...

### `unreal.MoviePipelineFormatArgs`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

Movie Pipeline Format Args

**Properties** (3):
  - `file_metadata`: `None` — [Read-Write] A set of Key/Value pairs for file metadata for file formats that support metadata. (Map...
  - `filename_arguments`: `None` — [Read-Write] A set of Key/Value pairs for output filename format strings (without {}) and their valu...
  - `job`: `MoviePipelineExecutorJob` — [Read-Write] Which job is this for? Some settings are specific to the level sequence being rendered....

### `unreal.MoviePipelineOutputData`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

Contains information about the to-disk output generated by a movie pipeline. This structure is used both for per-shot work finished callbacks and for the final render finished callback. When used as a...

**Properties** (5):
  - `graph_data`: `None` [Read-Only] — [Read-Only] The file data for each shot that was rendered. If no files were written this will be emp...
  - `job`: `MoviePipelineExecutorJob` [Read-Only] — [Read-Only] Job the data is for. Job may still be in progress (if a shot callback) so be careful abo...
  - `pipeline`: `MoviePipelineBase` [Read-Only] — [Read-Only] The UMoviePipeline instance that generated this data. This is only provided as an id (in...
  - `shot_data`: `None` [Read-Only] — [Read-Only] The file data for each shot that was rendered. If no files were written this will be emp...
  - `success`: `bool` [Read-Only] — [Read-Only] Did the job succeed, or was it canceled early due to an error (such as failure to write ...

### `unreal.MoviePipelinePassIdentifier`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

Movie Pipeline Pass Identifier

**Properties** (2):
  - `camera_name`: `str` — [Read-Write] The name of the camera that this pass is for. Stored here so we can differentiate betwe...
  - `name`: `str` — [Read-Write] The name of the pass such as âFinalImageâ or âObjectIdâ, etc. (str)

### `unreal.MoviePipelineRenderPassOutputData`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

Movie Pipeline Render Pass Output Data

**Properties** (1):
  - `file_paths`: `None` [Read-Only] — [Read-Only] (Array[str])

### `unreal.MoviePipelineSegmentWorkMetrics`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

Movie Pipeline Segment Work Metrics

**Properties** (7):
  - `engine_warm_up_frame_index`: `int` [Read-Only] — [Read-Only] The index of the engine warm up frame that we are working on. No rendering samples are p...
  - `output_frame_index`: `int` [Read-Only] — [Read-Only] Index of the output frame we are working on right now. (int32)
  - `output_sub_sample_index`: `int` [Read-Only] — [Read-Only] Which temporal/spatial sub sample are we working on right now. This counts temporal, spa...
  - `segment_name`: `str` [Read-Only] — [Read-Only] The name of the segment (if any) (str)
  - `total_engine_warm_up_frame_count`: `int` [Read-Only] — [Read-Only] The total number of engine warm up frames for this segment. (int32)
  - `total_output_frame_count`: `int` [Read-Only] — [Read-Only] The number of output frames we expect to make for this segment. (int32)
  - `total_sub_sample_count`: `int` [Read-Only] — [Read-Only] The total number of samples we will have to build to render this output frame. (int32)

### `unreal.MoviePipelineShotOutputData`
Inherits: `StructBase` | Header: `MovieRenderPipelineDataTypes.h`

Movie Pipeline Shot Output Data

**Properties** (2):
  - `render_pass_data`: `None` [Read-Only] — [Read-Only] A mapping between render passes (such as âFinalImageâ) and an array containing the f...
  - `shot`: `MoviePipelineExecutorShot` [Read-Only] — [Read-Only] Which shot was this output data for? (MoviePipelineExecutorShot)

### `unreal.MoviePipelineSidecarCamera`
Inherits: `StructBase` | Header: `MoviePipelineQueue.h`

Movie Pipeline Sidecar Camera

**Properties** (2):
  - `binding_id`: `Guid` — [Read-Write] (Guid)
  - `name`: `str` — [Read-Write] (str)

### `unreal.MovieGraphApplyCVarPresetNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphApplyCVarPresetNode.h`

A node which can apply a console variable preset.

**Properties** (2):
  - `console_variable_preset`: `MovieSceneConsoleVariableTrackInterface` — [Read-Write] The console variable preset that should be applied. (MovieSceneConsoleVariableTrackInte...
  - `override_console_variable_preset`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphApplyViewportLookNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphApplyViewportLookNode.h`

A node which applies the look of the viewport to the render (show flags, view mode, OCIO, etc).

**Properties** (8):
  - `ocio`: `bool` — [Read-Write] Set to true to apply the viewportâs OCIO settings to the render. Only render nodes th...
  - `override_b_ocio`: `bool` — [Read-Write] (bool)
  - `override_b_show_flags`: `bool` — [Read-Write] (bool)
  - `override_b_view_mode`: `bool` — [Read-Write] (bool)
  - `override_b_visibility`: `bool` — [Read-Write] (bool)
  - `show_flags`: `bool` — [Read-Write] Set to true to apply the viewportâs show flags to the render. (bool)
  - `view_mode`: `bool` — [Read-Write] Set to true to apply the viewportâs view mode to the render. (bool)
  - `visibility`: `bool` — [Read-Write] Set to true to apply editor visibility to actors in the render. (bool)

### `unreal.MovieGraphAudioOutputNode`
Inherits: `MovieGraphFileOutputNode` | Header: `MovieGraphAudioOutputNode.h`

A node which outputs .wav audio after all renders have completed.

### `unreal.MovieGraphAudioRendererBase`
Inherits: `Object` | Header: `MovieGraphDataTypes.h`

Base class for generating audio while the pipeline is running.

### `unreal.MovieGraphBranchNode`
Inherits: `MovieGraphNode` | Header: `MovieGraphBranchNode.h`

A node which creates a True/False branching condition. A user Graph Variable can be plugged into the conditional pin and this will be evaluated when flattening the graph, choosing which branch path to...

### `unreal.MovieGraphBurnInNode`
Inherits: `MovieGraphWidgetRendererBaseNode` | Header: `MovieGraphBurnInNode.h`

A node which generates a widget burn-in, rendered to a standalone image or composited on top of a render layer.

**Properties** (2):
  - `burn_in_class`: `SoftClassPath` — [Read-Write] The widget that the burn-in should use. (SoftClassPath)
  - `override_burn_in_class`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphBurnInWidget`
Inherits: `UserWidget` | Header: `MovieGraphBurnInWidget.h`

Base class for graph-based level sequence burn-ins.

**Methods** (1):
  - `update_for_graph(graph_pipeline, evaluated_config, camera_index, camera_name)` -> `None` — Called on the first temporal and first spatial sample of each output frame.

### `unreal.MovieGraphCameraSettingNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphCameraNode.h`

A node which configures global camera settings that are shared among all renders.

**Properties** (6):
  - `override_b_render_all_cameras`: `bool` — [Read-Write] (bool)
  - `override_overscan_percentage`: `bool` — [Read-Write] (bool)
  - `override_shutter_timing`: `bool` — [Read-Write] (bool)
  - `overscan_percentage`: `float` — [Read-Write] Overscan percent enables rendering render additional pixels beyond the set resolution a...
  - `render_all_cameras`: `bool` — [Read-Write] * If enabled Movie Render Queue will examine your Level Sequence for additional cameras...
  - `shutter_timing`: `MoviePipelineShutterTiming` — [Read-Write] Shutter Timing allows you to bias the timing of your shutter angle to either be before,...

### `unreal.MovieGraphCollection`
Inherits: `Object` | Header: `MovieGraphRenderLayerSubsystem.h`

A group of actors generated by actor queries.

**Methods** (8):
  - `add_condition_group()` -> `MovieGraphConditionGroup` — Adds a new condition group to the collection and returns a ptr to it. The collection owns the created condition group.
  - `evaluate(world)` -> `Set [ Actor ]` — Evaluate deprecated: Please use EvaluateActorsAndComponents() instead.
  - `evaluate_actors_and_components(world)` -> `MovieGraphEvaluationResult` — Gets matching actors and components by having condition groups evaluate themselves, and performing set operations on the...
  - `get_collection_name()` -> `str` — Gets the name of the collection as seen in the UI.
  - `get_condition_groups()` -> `Array [ MovieGraphConditionGroup ]` — Gets all condition groups currently contained in the collection.
  - `move_condition_group_to_index(condition_group, new_index)` -> `bool` — Move the specified condition group to a new index within the collection. Returns false if the condition group was not fo...
  - `remove_condition_group(condition_group)` -> `bool` — Removes the specified condition group from the collection if it exists. Returns true on success, else false. Removes all...
  - `set_collection_name(name)` -> `None` — Sets the name of the collection as seen in the UI.

### `unreal.MovieGraphCollectionModifier`
Inherits: `Object` | Header: `MovieGraphRenderLayerSubsystem.h`

Base class for providing actor modification functionality via collections.

**Methods** (3):
  - `add_collection(collection)` -> `None` — Adds a collection to the existing set of collections in this modifier.
  - `get_collections()` -> `Array [ MovieGraphCollection ]` — Get Collections
  - `set_collections(collections)` -> `None` — Overwrites the existing collections with the provided array of collections.

### `unreal.MovieGraphCollectionNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphCollectionNode.h`

A collection node specifies an interface for doing dynamic scene queries for actors in the world. Collections work in tandem with UMovieGraphModifiers to select which actors the modifiers should be ru...

**Properties** (2):
  - `collection`: `MovieGraphCollection` [Read-Only] — [Read-Only] The collection is customized in the details panel, so the override should always be enab...
  - `override_collection`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphCommandLineEncoderNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphCommandLineEncoderNode.h`

A node which kicks off an encode process after all renders have completed.

**Properties** (24):
  - `audio_codec`: `str` — [Read-Write] Which audio codec should we use? Run âMovieRenderPipeline.DumpCLIEncoderCodecsâ for...
  - `audio_input_string_format`: `str` — [Read-Write] Format string used for each audio input. (str)
  - `b_override_file_name_format_override`: `bool` — âb_override_file_name_format_overrideâ was renamed to âoverride_file_name_formatâ. deprecate...
  - `command_line_format`: `str` — [Read-Write] The format string used when building the final command line argument to launch. (str)
  - `delete_source_files`: `bool` — [Read-Write] Whether the source files should be deleted on disk after encoding. (bool)
  - `encode_settings`: `str` — [Read-Write] Additional flags used for specifying encode quality. (str)
  - `file_name_format`: `str` — [Read-Write] File name format string override. If specified it will override the FileNameFormat from...
  - `file_name_format_override`: `str` — âfile_name_format_overrideâ was renamed to âfile_name_formatâ. deprecated
  - `output_file_extension`: `str` — âwebmâ. (str) [Read-Write] Extension for the output files. Many encoders use this to determine t...
  - `override_audio_codec`: `bool` — [Read-Write] (bool)
  - `override_audio_input_string_format`: `bool` — [Read-Write] (bool)
  - `override_b_delete_source_files`: `bool` — [Read-Write] (bool)
  - `override_b_retain_input_text_files`: `bool` — [Read-Write] (bool)
  - `override_b_skip_encode_on_render_canceled`: `bool` — [Read-Write] (bool)
  - `override_command_line_format`: `bool` — [Read-Write] (bool)
  - `override_encode_settings`: `bool` — [Read-Write] (bool)
  - `override_file_name_format`: `bool` — [Read-Write] (bool)
  - `override_output_file_extension`: `bool` — [Read-Write] (bool)
  - `override_video_codec`: `bool` — [Read-Write] (bool)
  - `override_video_input_string_format`: `bool` — [Read-Write] (bool)
  - `retain_input_text_files`: `bool` — [Read-Write] Retain the intermediate audio and video input text files that are passed to the encoder...
  - `skip_encode_on_render_canceled`: `bool` — [Read-Write] Whether encoding should be skipped on frames produced if rendering was canceled before ...
  - `video_codec`: `str` — [Read-Write] Which video codec should we use? Run âMovieRenderPipeline.DumpCLIEncoderCodecsâ for...
  - `video_input_string_format`: `str` — [Read-Write] Format string used for each video input. (str)

### `unreal.MovieGraphConditionGroup`
Inherits: `Object` | Header: `MovieGraphRenderLayerSubsystem.h`

A group of queries which can be added to a collection.

**Methods** (10):
  - `add_query(query_type, insert_index = -1)` -> `MovieGraphConditionGroupQueryBase` — Adds a new condition group query to the condition group and returns a ptr to it. The condition group owns the created qu...
  - `duplicate_query(query_index)` -> `MovieGraphConditionGroupQueryBase` — Duplicates the condition group query at the specified index. The duplicate is placed at the end of the query list. Retur...
  - `evaluate(world)` -> `Set [ Actor ]` — Evaluate deprecated: Please use EvaluateActorsAndComponents() instead.
  - `evaluate_actors_and_components(world)` -> `MovieGraphEvaluationResult` — Determines the actors and components that match the condition group by running the queries contained in it.
  - `get_operation_type()` -> `MovieGraphConditionGroupOpType` — Gets the condition group operation type.
  - `get_queries()` -> `Array [ MovieGraphConditionGroupQueryBase ]` — Gets all queries currently contained in the condition group.
  - `is_first_condition_group()` -> `bool` — Determines if this is the first condition group under the parent collection.
  - `move_query_to_index(query, new_index)` -> `bool` — Move the specified query to a new index within the condition group. Returns false if the query was not found or the inde...
  - `remove_query(query)` -> `bool` — Removes the specified query from the condition group if it exists. Returns true on success, else false.
  - `set_operation_type(operation_type)` -> `None` — Sets how the condition group interacts with the collection. This call is ignored for the first condition group in the co...

### `unreal.MovieGraphConditionGroupQuery_Actor`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors via an explicit actor list.

**Properties** (2):
  - `actors_and_components_to_match`: `None` — [Read-Write] The query must match one of the actors (or components on an actor) to be a match. If th...
  - `actors_to_match`: `None` — [Read-Write]
deprecated: This property is deprecated. Please use ActorsAndComponentsToMatch instead....

### `unreal.MovieGraphConditionGroupQuery_ActorLayer`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors via Actor Layers.

### `unreal.MovieGraphConditionGroupQuery_ActorName`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors via their name (label).

### `unreal.MovieGraphConditionGroupQuery_ActorTagName`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors via tags on actors.

### `unreal.MovieGraphConditionGroupQuery_ActorType`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors by type.

### `unreal.MovieGraphConditionGroupQuery_ComponentTagName`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors by tags on their components.

### `unreal.MovieGraphConditionGroupQuery_ComponentType`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors via the components contained in them.

### `unreal.MovieGraphConditionGroupQuery_DataLayer`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors via World Partition Data Layers.

### `unreal.MovieGraphConditionGroupQuery_EditorFolder`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors via the editor folder that theyâre contained in.

### `unreal.MovieGraphConditionGroupQuery_IsSpawnable`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors by their spawnable status.

### `unreal.MovieGraphConditionGroupQuery_Sublevel`
Inherits: `MovieGraphConditionGroupQueryBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Query type which filters actors via the sublevel that theyâre contained in.

### `unreal.MovieGraphConditionGroupQueryBase`
Inherits: `Object` | Header: `MovieGraphRenderLayerSubsystem.h`

Base class that all condition group queries must inherit from.

**Methods** (10):
  - `evaluate(actors_to_query, world)` -> `Set [ Actor ]` — Determines which of the provided actors (in the given world) match the query. Matches are added to OutMatchingActors. No...
  - `evaluate_actors_and_components()` — Similar to Evaluate(), but returns both actors and components if the query can match both. Note that this method will on...
  - `get_operation_type()` -> `MovieGraphConditionGroupQueryOpType` — Gets the condition group query operation type.
  - `is_editor_only_query()` -> `bool` — Determines if this query is only respected when run within the editor. Used for providing a UI hint.
  - `is_enabled()` -> `bool` — Determines if this query is enabled.
  - `is_first_condition_group_query()` -> `bool` — Determines if this is the first condition group query under the parent condition group.
  - `set_enabled(enabled)` -> `None` — Sets whether this query is enabled.
  - `set_operation_type(operation_type)` -> `None` — Sets how the condition group query interacts with the condition group. This call is ignored for the first query in the c...
  - `should_evaluate_components()` -> `bool` — Determines if this query should additionally match components, rather than just matching actors. Off by default. If this...
  - `should_hide_property_names()` -> `bool` — Determines if the public properties on the query class will have their names hidden in the details panel. Returns false ...

### `unreal.MovieGraphConfig`
Inherits: `Object` | Header: `MovieGraphConfig.h`

This is the runtime representation of the UMoviePipelineEdGraph which contains the actual strongly typed graph network that is read by the MoviePipeline. There is an editor-only representation of this...

**Methods** (35):
  - `add_input(base_name = '')` -> `MovieGraphInput` — Adds a new input member to the graph. Returns the new input on success, else nullptr. The default name of the input is â...
  - `add_labeled_edge(from_node, from_pin_label, to_node, to_pin_label)` -> `bool` — Add a connection in the graph between the given nodes and pin names. Pin name may be empty for basic nodes (if no name i...
  - `add_output(base_name = '')` -> `MovieGraphOutput` — Adds a new output member to the graph. Returns the new output on success, else nullptr. The default name of the output i...
  - `add_variable(custom_base_name = 'None')` -> `MovieGraphVariable` — Adds a new variable member with default values to the graph. The new variable will have a base name of âVariableâ un...
  - `create_flattened_graph(out_error=str)` — Given a user-defined evaluation context, evaluate the graph and build a âflattenedâ list of settings for each branch...
  - `create_node_by_class(class_)` -> `MovieGraphNode` — Creates the given node type in this graph. Does not create any connections, and a node will not be considered during eva...
  - `delete_member(member_to_delete)` -> `bool` — Remove the specified member (input, output, variable) from the graph.
  - `duplicate_variable(variable_to_duplicate)` -> `MovieGraphVariable` — Duplicates the provided variable. Returns the new variable on success, else nullptr.
  - `get_all_contained_subgraphs()` -> `Set [ MovieGraphConfig ]` — Get all subgraphs that this graph contains, recursively (ie, subgraphs of subgraphs are included, etc).
  - `get_branch_names()` -> `Array [ Name ]` — Returns an array of the branch names for the OutputNode on this Graph.
  - `get_downstream_branch_names(from_node, from_pin, stop_at_subgraph = False)` -> `Array [ str ]` — Determines the name(s) of the branches downstream from FromNode, starting at FromPin. Optionally, subgraph nodes can hal...
  - `get_input_node()` -> `MovieGraphNode` — Gets the automatically generated âInputsâ node in the Graph.
  - `get_inputs()` -> `Array [ MovieGraphInput ]` — Gets all inputs that have been defined on the graph.
  - `get_node_for_branch(class_, branch_name, exact_match = False)` -> `MovieGraphNode` — Finds a node (by type) for the given branch name (see GetBranchNames()). Returns the first result of that type found, ev...
  - `get_node_for_tag(script_tag, optional_class_filter = None, optional_branch_filter = 'None', exact_match = False)` -> `MovieGraphNode` — Finds a node by ScriptTag field. THIS SEARCH IS CASE SENSITIVE. Returns the first result of that tag found, even if ther...
  - `get_nodes_for_branch(class_, branch_name, exact_match = False)` -> `Array [ MovieGraphNode ]` — Finds all nodes (by type) for the given branch name (see GetBranchNames()). Returns all results of that type following t...
  - `get_nodes_for_tag(script_tag, optional_class_filter = None, optional_branch_filter = 'None', exact_match = False)` -> `Array [ MovieGraphNode ]` — Finds nodes by ScriptTag field. THIS SEARCH IS CASE SENSITIVE. Returns all results of that tag found, following traversa...
  - `get_output_directory()` -> `str` — Walks the graph backward recursively from the output node searching for a UMovieGraphOutputSettings node. Traverses subg...
  - `get_output_node()` -> `MovieGraphNode` — Gets the automatically generated âOutputsâ node in the Graph.
  - `get_outputs()` -> `Array [ MovieGraphOutput ]` — Gets all outputs that have been defined on the graph.
  - `get_upstream_branch_names(from_node, from_pin, stop_at_subgraph = False)` -> `Array [ str ]` — Determines the name(s) of the branches upstream from FromNode, starting at FromPin. Optionally, subgraph nodes can halt ...
  - `get_variable_by_guid(guid)` -> `MovieGraphVariable` — Gets the variable in the graph with the specified GUID, else nullptr if one could not be found.
  - `get_variable_by_name(variable_name)` -> `MovieGraphVariable` — Gets the variable in the graph with the specified name (including global variables), else nullptr if one could not be fo...
  - `get_variables(include_global = False)` -> `Array [ MovieGraphVariable ]` — Gets all variables that are available to be used in the graph. Global variables can optionally be included if bIncludeGl...
  - `move_category_before(category_to_move, category_before)` -> `None` — Moves one category (InCategoryToMove) and its variables before another category (InCategoryBefore).
  - `move_variable_before(target_variable, before_variable)` -> `None` — Moves one variable (InTargetVariable) before another variable (InBeforeVariable). Takes care of ensuring the variableâ...
  - `move_variable_to_index(target_variable, new_index)` -> `None` — Moves one variable (InTargetVariable) to the specified index among all user graph variables. Note that MoveVariableBefor...
  - `remove_all_inbound_edges(node)` -> `bool` — Convenience function which removes all Inbound (pins on the left side of a node) edges for the given node.
  - `remove_all_outbound_edges(node)` -> `bool` — Convenience function which removes all Outbound (pins on the right side of a node) edges for the given node.
  - `remove_inbound_edges(node, pin_name)` -> `bool` — Convenience function which removes all Inbound (pins on the left side of a node) edges connected to the given inbound pi...
  - `remove_labeled_edge(from_node, from_pin_name, to_node, to_pin_name)` -> `bool` — Like AddLabeledEdge, removes the given connection between Node A and Node B (for the specified pins by name).
  - `remove_node(node)` -> `bool` — Removes the specified node from the graph, disconnecting connected edges as it goes.
  - `remove_nodes(nodes)` -> `bool` — Like RemoveNode but takes an entire array at once for convenience.
  - `remove_outbound_edges(node, pin_name)` -> `bool` — Convenience function which removes all Outbound (pins on the right side of a node) edges connected to the given outbound...
  - `update_global_variable_values(pipeline)` -> `None` — Updates the values of all global variables.

### `unreal.MovieGraphCoreTimeStep`
Inherits: `MovieGraphTimeStepBase` | Header: `MovieGraphCoreTimeStep.h`

Provides common logic for typical time-step functionality.

### `unreal.MovieGraphDataSourceBase`
Inherits: `Object` | Header: `MovieGraphDataTypes.h`

Movie Graph Pipeline is mostly interested in knowing about ranges of time that it should render, and less concerned with the specifics of where that data comes from (ie: a Level Sequence). This lets y...

### `unreal.MovieGraphDebugSettingNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphDebugNode.h`

A node which configures various debug settings that may be useful when debugging an issue.

**Properties** (6):
  - `capture_frames_with_render_doc`: `bool` — [Read-Write] If true, automatically trigger RenderDoc to capture rendering information. RenderDoc pl...
  - `capture_unreal_insights_trace`: `bool` — [Read-Write] If true, automatically capture an Unreal Insights trace file for the duration of the re...
  - `override_b_capture_frames_with_render_doc`: `bool` — [Read-Write] (bool)
  - `override_b_capture_unreal_insights_trace`: `bool` — [Read-Write] (bool)
  - `override_unreal_insights_trace_file_name_format`: `bool` — [Read-Write] (bool)
  - `unreal_insights_trace_file_name_format`: `str` — [Read-Write] If true, automatically capture an Unreal Insights trace file for the duration of the re...

### `unreal.MovieGraphDefaultAudioRenderer`
Inherits: `MovieGraphAudioRendererBase` | Header: `MovieGraphDefaultAudioRenderer.h`

Provides default audio rendering for the pipeline.

### `unreal.MovieGraphDefaultRenderer`
Inherits: `MovieGraphRendererBase` | Header: `MovieGraphDefaultRenderer.h`

This class is the default implementation for the Movie Graph Pipeline renderer. This is split off into a separate class to minimize the complexity of the core UMovieGraphPipeline, and to provide a bet...

### `unreal.MovieGraphEdge`
Inherits: `Object` | Header: `MovieGraphEdge.h`

Movie Graph Edge

### `unreal.MovieGraphEvaluatedConfig`
Inherits: `Object` | Header: `MovieGraphConfig.h`

An evaluated config for the current frame. Each named branch (including Globals) has its own copy of the config, fully resolved (so there is no need to check the Globals branch when looking at a named...

**Methods** (3):
  - `get_branch_names()` -> `Array [ Name ]` — Get Branch Names
  - `get_setting_for_branch(class_, branch_name, include_cd_os = True, exact_match = False)` -> `MovieGraphSettingNode` — Get Setting for Branch
  - `get_settings_for_branch(class_, branch_name, include_cd_os = True, exact_match = False)` -> `Array [ MovieGraphSettingNode ]` — Get Settings for Branch

### `unreal.MovieGraphExecuteScriptNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphExecuteScriptNode.h`

This node allows users to run code before and after a movie graph is being executed. Example use cases would be to run custom logic that overrides the output directory, or to trigger an event after a ...

**Properties** (2):
  - `override_script`: `bool` — [Read-Write] (bool)
  - `script`: `SoftClassPath` — [Read-Write] Which script should this node instantiate? The class will be instantiated when the job ...

### `unreal.MovieGraphFileOutputNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphFileOutputNode.h`

The UMovieGraphFileOutputNode node defines a output file format that MRQ may place produced render data into. At runtime, the individual pieces of render data will be generated by the Movie Graph Rend...

**Properties** (2):
  - `file_name_format`: `str` — [Read-Write] What format string should the final files use? Can include folder prefixes, and format ...
  - `override_file_name_format`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphGlobalGameOverridesNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphGlobalGameOverrides.h`

A node which configures the global game overrides.

**Properties** (36):
  - `b_disable_hlo_ds`: `bool` — âb_disable_hlo_dsâ was renamed to âdisable_hlodsâ. deprecated
  - `disable_hlods`: `bool` — [Read-Write] Determines if hierarchical LODs should be disabled and their real meshes used instead, ...
  - `disable_lo_ds`: `bool` — âdisable_lo_dsâ was renamed to âdisable_lodsâ. deprecated
  - `disable_lods`: `bool` — [Read-Write] Disabling LODs will use the highest quality LOD for meshes and particle systems, regard...
  - `disable_texture_streaming`: `bool` — [Read-Write] Toggles whether texture streaming is disabled. Can solve objects being blurry after cam...
  - `flush_asset_compiler`: `bool` — [Read-Write] This ensures that any asynchronously compiled assets (static meshes, distance fields, e...
  - `flush_grass_streaming`: `bool` — [Read-Write] Flushing grass streaming prevents visible pop-in/culling of grass instances, but may co...
  - `flush_level_streaming`: `bool` — [Read-Write] Flushing level streaming ensures that any pending changes to sub-levels or world partit...
  - `flush_shader_compiler`: `bool` — [Read-Write] This ensures that any asynchronously compiled shader permutations are completed before ...
  - `flush_streaming_managers`: `bool` — [Read-Write] This ensures that we wait on any streaming managers that may have outstanding work to f...
  - `game_mode_override`: `Class` — [Read-Write]
deprecated: Please use the SoftGameModeOverride property instead. (type(Class))
  - `override_b_disable_hlo_ds`: `bool` — âoverride_b_disable_hlo_dsâ was renamed to âoverride_disable_hlodsâ. deprecated
  - `override_b_disable_lo_ds`: `bool` — âoverride_b_disable_lo_dsâ was renamed to âoverride_disable_lodsâ. deprecated
  - `override_b_disable_texture_streaming`: `bool` — âoverride_b_disable_texture_streamingâ was renamed to âoverride_disable_texture_streamingâ. ...
  - `override_b_flush_asset_compiler`: `bool` — âoverride_b_flush_asset_compilerâ was renamed to âoverride_flush_asset_compilerâ. deprecated
  - `override_b_flush_grass_streaming`: `bool` — âoverride_b_flush_grass_streamingâ was renamed to âoverride_flush_grass_streamingâ. deprecat...
  - `override_b_flush_level_streaming`: `bool` — âoverride_b_flush_level_streamingâ was renamed to âoverride_flush_level_streamingâ. deprecat...
  - `override_b_flush_shader_compiler`: `bool` — âoverride_b_flush_shader_compilerâ was renamed to âoverride_flush_shader_compilerâ. deprecat...
  - `override_b_flush_streaming_managers`: `bool` — âoverride_b_flush_streaming_managersâ was renamed to âoverride_flush_streaming_managersâ. de...
  - `override_b_rebuild_lumen_scene_between_render_layers`: `bool` — [Read-Write] (bool)
  - `override_disable_hlods`: `bool` — [Read-Write] (bool)
  - `override_disable_lods`: `bool` — [Read-Write] (bool)
  - `override_disable_texture_streaming`: `bool` — [Read-Write] (bool)
  - `override_flush_asset_compiler`: `bool` — [Read-Write] (bool)
  - `override_flush_grass_streaming`: `bool` — [Read-Write] (bool)
  - `override_flush_level_streaming`: `bool` — [Read-Write] (bool)
  - `override_flush_shader_compiler`: `bool` — [Read-Write] (bool)
  - `override_flush_streaming_managers`: `bool` — [Read-Write] (bool)
  - `override_game_mode_override`: `bool` — [Read-Write]
deprecated: Please use the bOverride_SoftGameModeOverride property instead. (bool)
  - `override_scalability_quality_level`: `bool` — [Read-Write] (bool)
  - `override_soft_game_mode_override`: `bool` — [Read-Write] (bool)
  - `override_virtual_texture_feedback_factor`: `bool` — [Read-Write] (bool)
  - `rebuild_lumen_scene_between_render_layers`: `bool` — [Read-Write] When rendering multiple Render Layers, Lumenâs surface cache may not be able to keep ...
  - `scalability_quality_level`: `MovieGraphScalabilityQualityLevel` — [Read-Write] The scalability quality level that should be used in renders. See the Scalability Refer...
  - `soft_game_mode_override`: `Class` — [Read-Write] Optional game mode to override the mapâs default game mode with. This can be useful i...
  - `virtual_texture_feedback_factor`: `int` — [Read-Write] The virtual texture feedback resolution factor. A lower factor will increase virtual te...

### `unreal.MovieGraphGlobalOutputSettingNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphGlobalOutputSettingNode.h`

Movie Graph Global Output Setting Node

**Properties** (32):
  - `adapt_resolution`: `MovieGraphAspectRatioAdaptBehavior` — [Read-Write] Should the output resolution be automatically adjusted to match the aspect ratio on cam...
  - `custom_playback_range_end`: `MovieGraphSequencePlaybackRangeBound` — [Read-Write] If overwritten, and the Type is set to âCustomâ, then the Value field will override...
  - `custom_playback_range_end_frame`: `int` — [Read-Write] (int32)
  - `custom_playback_range_start`: `MovieGraphSequencePlaybackRangeBound` — [Read-Write] If overwritten, and the Type is set to âCustomâ, then the Value field will override...
  - `custom_playback_range_start_frame`: `int` — [Read-Write] (int32)
  - `custom_timecode_start`: `Timecode` — [Read-Write] Start the timecode at a specific value, rather than the value coming from the Level Seq...
  - `drop_frame_timecode`: `bool` — [Read-Write] Whether the embedded timecode track should be written using drop-frame format. Only app...
  - `flush_disk_writes_per_shot`: `bool` — [Read-Write] If true, the game thread will stall at the end of each shot to flush the rendering queu...
  - `frame_number_offset`: `int` — [Read-Write] How many frames should we offset the output frame number by? This is useful when using ...
  - `handle_frame_count`: `int` — [Read-Write] Top level shot track sections will automatically be expanded by this many frames in bot...
  - `output_directory`: `DirectoryPath` — [Read-Write] What directory should all of our output files be relative to. (DirectoryPath)
  - `output_frame_rate`: `FrameRate` — [Read-Write] What frame rate should the output files be exported at? This overrides the Display Rate...
  - `output_resolution`: `MovieGraphNamedResolution` — [Read-Write] What resolution should our output files be exported at? (MovieGraphNamedResolution)
  - `override_adapt_resolution`: `bool` — [Read-Write] (bool)
  - `override_b_drop_frame_timecode`: `bool` — [Read-Write] (bool)
  - `override_b_flush_disk_writes_per_shot`: `bool` — [Read-Write] (bool)
  - `override_b_overwrite_existing_output`: `bool` — [Read-Write] (bool)
  - `override_custom_playback_range_end`: `bool` — [Read-Write] (bool)
  - `override_custom_playback_range_end_frame`: `bool` — [Read-Write]
deprecated: Use bOverride_CustomPlaybackRangeEnd instead. (bool)
  - `override_custom_playback_range_start`: `bool` — [Read-Write] (bool)
  - `override_custom_playback_range_start_frame`: `bool` — [Read-Write]
deprecated: Use bOverride_CustomPlaybackRangeStart instead. (bool)
  - `override_custom_timecode_start`: `bool` — [Read-Write] (bool)
  - `override_frame_number_offset`: `bool` — [Read-Write] (bool)
  - `override_handle_frame_count`: `bool` — [Read-Write] (bool)
  - `override_output_directory`: `bool` — [Read-Write] (bool)
  - `override_output_frame_rate`: `bool` — [Read-Write] (bool)
  - `override_output_resolution`: `bool` — [Read-Write] (bool)
  - `override_versioning_settings`: `bool` — [Read-Write] (bool)
  - `override_zero_pad_frame_numbers`: `bool` — [Read-Write] (bool)
  - `overwrite_existing_output`: `bool` — [Read-Write] If true, output containers should attempt to override any existing files with the same ...
  - `versioning_settings`: `MovieGraphVersioningSettings` — [Read-Write] Determines how versioning should be handled (Auto Version, Version Number, etc.). (Movi...
  - `zero_pad_frame_numbers`: `int` — [Read-Write] How many digits should all output frame numbers be padded to? MySequence_1.png -> MySeq...

### `unreal.MovieGraphGlobalVariable`
Inherits: `MovieGraphVariable` | Header: `MovieGraphConfig.h`

Similar to normal UMovieGraphVariable instances. However, their values are provided by the graph, they cannot be edited/deleted, and they cannot be overridden at the job level.

### `unreal.MovieGraphGlobalVariable_CameraName`
Inherits: `MovieGraphGlobalVariable` | Header: `MovieGraphConfig.h`

Movie Graph Global Variable Camera Name

### `unreal.MovieGraphGlobalVariable_FrameNumber`
Inherits: `MovieGraphGlobalVariable` | Header: `MovieGraphConfig.h`

Movie Graph Global Variable Frame Number

### `unreal.MovieGraphGlobalVariable_SequenceName`
Inherits: `MovieGraphGlobalVariable` | Header: `MovieGraphConfig.h`

Movie Graph Global Variable Sequence Name

### `unreal.MovieGraphGlobalVariable_ShotName`
Inherits: `MovieGraphGlobalVariable` | Header: `MovieGraphConfig.h`

Movie Graph Global Variable Shot Name

### `unreal.MovieGraphInput`
Inherits: `MovieGraphInterfaceBase` | Header: `MovieGraphConfig.h`

An input exposed on the graph that will be available for nodes to connect to.

### `unreal.MovieGraphInputNode`
Inherits: `MovieGraphNode` | Header: `MovieGraphInputNode.h`

A graph node which displays all input members available in the graph.

### `unreal.MovieGraphInterfaceBase`
Inherits: `MovieGraphMember` | Header: `MovieGraphConfig.h`

Common base class for input/output members on the graph.

### `unreal.MovieGraphLibrary`
Inherits: `BlueprintFunctionLibrary` | Header: `MovieGraphBlueprintLibrary.h`

Movie Graph Blueprint Library

**Methods** (32):
  - `get_backbuffer_resolution(evaluated_graph, default_overscan = 0.000000, camera_aspect_ratio = 0.000000)` -> `IntPoint` [classmethod] — Gets the resolution that frames will actually be rendered at in MRG, which includes factors such as overscan and tiling
  - `get_completion_percentage(pipeline)` -> `float` [classmethod] — Gets the completion percent of the Pipeline in 0-1
  - `get_current_aperture(movie_graph_pipeline, camera_index = -1)` -> `float` [classmethod] — Gets the aperture for the camera currently in use.
  - `get_current_cine_camera(movie_graph_pipeline, camera_index = -1)` -> `CineCameraComponent` [classmethod] — Gets the currently active cine camera, or nullptr if one was not found.
  - `get_current_executor_shot(movie_pipeline)` -> `MoviePipelineExecutorShot` [classmethod] — Gets the current shot being rendered by the graph (could be nullptr if rendering hasnât started or has moved to Finali...
  - `get_current_focal_length(movie_graph_pipeline, camera_index = -1)` -> `float` [classmethod] — Gets the focal length for the camera currently in use.
  - `get_current_focus_distance(movie_graph_pipeline, camera_index = -1)` -> `float` [classmethod] — Gets the focus distance for the camera currently in use.
  - `get_current_segment_name(out_inner_name=Text)` [classmethod] — Gets the name of the segment (shot) currently being rendered.
  - `get_current_segment_state(movie_graph_pipeline)` -> `MovieRenderShotState` [classmethod] — Gets the state of the segment (shot) currently being rendered.
  - `get_current_segment_work_metrics(movie_graph_pipeline)` -> `MoviePipelineSegmentWorkMetrics` [classmethod] — Gets the work metrics for the segment (shot) that is currently being rendered.
  - `get_current_shot_frame_number(movie_graph_pipeline)` -> `FrameNumber` [classmethod] — Gets the frame number of the current render at the shot level.
  - `get_current_shot_timecode(movie_graph_pipeline)` -> `Timecode` [classmethod] — Gets the timecode of the current render at the shot level.
  - `get_current_version_number(movie_graph_pipeline)` -> `int32` [classmethod] — Retrieves the cached version number calculated for the current shot, which depends on where the version token was used i...
  - `get_desired_output_resolution(evaluated_graph, camera_aspect_ratio = 0.000000)` -> `IntPoint` [classmethod] — Gets the desired output resolution for the movie render graph as specified by the user. Does not include overscan, tilin...
  - `get_effective_frame_rate(node, default_rate)` -> `FrameRate` [classmethod] — If InNode is valid, inspects the provided OutputsettingNode to determine if it wants to override the Frame Rate, and if ...
  - `get_effective_output_resolution(evaluated_graph, default_overscan = 0.000000)` -> `IntPoint` [classmethod] — In case of overscan percentage being higher than 0, additional pixels are rendered. This function returns the resolution...
  - `get_estimated_time_remaining(movie_graph_pipeline)` -> `Timespan or None` [classmethod] — Get the estimated amount of time remaining for the current pipeline. Based on looking at the total amount of samples to ...
  - `get_job_author(movie_graph_pipeline)` -> `Text` [classmethod] — Gets the author of the current job, or the logged in userâs username if the job has no specified author.
  - `get_job_initialization_time(movie_graph_pipeline)` -> `DateTime` [classmethod] — Gets the time the job was initialized.
  - `get_job_name(movie_graph_pipeline)` -> `Text` [classmethod] — Gets the name of the current job.
  - `get_overall_output_frames(out_total_count=int32)` [classmethod] — Determines the overall current frame number and total number of frames.
  - `get_overall_segment_counts(out_total_count=int32)` [classmethod] — Gets the number of segments (shots) that will be rendered.
  - `get_overscan_crop_rectangle(evaluated_graph, default_overscan = 0.000000, camera_aspect_ratio = 0.000000)` -> `IntRect` [classmethod] — Gets a rectangle that will crop out any overscan applied to the scene. If there is no overscan, the rectangle returned w...
  - `get_overscanned_resolution(evaluated_graph, default_overscan = 0.000000, camera_aspect_ratio = 0.000000)` -> `IntPoint` [classmethod] — Gets the desired output resolution scaled by any configured overscan for the movie render graph
  - `get_pipeline_state(movie_graph_pipeline)` -> `MovieRenderPipelineState` [classmethod] — Get the current state of the specified pipeline. See EMovieRenderPipelineState for more detail about each state.
  - `get_root_frame_number(movie_graph_pipeline)` -> `FrameNumber` [classmethod] — Gets the frame number of the current render at the root (sequence) level.
  - `get_root_timecode(movie_graph_pipeline)` -> `Timecode` [classmethod] — Gets the timecode of the current render at the root (sequence) level.
  - `is_named_resolution_valid(resolution_profile_name)` -> `bool` [classmethod] — Utility function for checking if a given resolution profile name is valid, since NamedResolutionFromProfile will throw a...
  - `named_resolution_from_profile(resolution_profile_name)` -> `MovieGraphNamedResolution` [classmethod] — Create a Named Resolution from the profile name. Throws a Kismet Exception if the profile name isnât found. The known ...
  - `named_resolution_from_size(res_x, res_y)` -> `MovieGraphNamedResolution` [classmethod] — Create a Named Resolution from the given resolution. Given named resolution will be named âCustomâ.
  - `resolve_filename_format_arguments(format_string, out_merged_format_args=MovieGraphResolveArgs)` [classmethod] — Takes a Movie Graph format string (in the form of {token}), a list of parameters (which normally come from the running U...
  - `resolve_version_number(params, get_next_version = True)` -> `int32` [classmethod] — If the version number is explicitly specified on the Output Setting node, returns that. Otherwise searches the output di...

### `unreal.MovieGraphLinearTimeStep`
Inherits: `MovieGraphCoreTimeStep` | Header: `MovieGraphLinearTimeStep.h`

Advances time forward linearly until the end of the range of time that is being rendered is reached. This is useful for deferred rendering (where thereâs a small number of temporal sub-samples and n...

### `unreal.MovieGraphMaterialModifier`
Inherits: `MovieGraphCollectionModifier` | Header: `MovieGraphRenderLayerSubsystem.h`

Modifies actor materials.

**Methods** (3):
  - `apply_modifier(world)` -> `None` — Apply Modifier
  - `set_material(material)` -> `None` — Set Material
  - `undo_modifier()` -> `None` — Undo Modifier

**Properties** (2):
  - `material`: `MaterialInterface` — [Read-Write] (MaterialInterface)
  - `override_material`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphMember`
Inherits: `MovieGraphValueContainer` | Header: `MovieGraphConfig.h`

Movie Graph Member

**Methods** (6):
  - `can_rename(new_name)` -> `Text or None` — Determines if this member can be renamed to the specified name. If the rename is not possible, returns false and OutErro...
  - `get_guid()` -> `Guid` — Gets the GUID that uniquely identifies this member.
  - `get_member_name()` -> `str` — Gets the name of this member.
  - `is_deletable()` -> `bool` — Determines if this member can be deleted.
  - `is_editable()` -> `bool` — Gets whether this member is editable via the UI.
  - `set_member_name(new_name)` -> `bool` — Sets the name of this member. Returns true if the rename was successful, else false.

**Properties** (1):
  - `description`: `str` — [Read-Write] The optional description of this member, which is user-facing. (str)

### `unreal.MovieGraphMetadataAttributeCollection`
Inherits: `Object` | Header: `MovieGraphSetMetadataAttributesNode.h`

Movie Graph Metadata Attribute Collection

**Properties** (1):
  - `metadata_attributes`: `None` — [Read-Write] An array of metadata attributes to be added to output files. (Array[MovieGraphMetadataA...

### `unreal.MovieGraphModifierNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphModifierNode.h`

A collection node specifies an interface for doing dynamic scene queries for actors in the world. Collections work in tandem with UMovieGraphModifiers to select which actors the modifiers should be ru...

**Methods** (9):
  - `add_collection(collection_name)` -> `None` — Add a collection identified by the given name which will be affected by the modifiers on this node.
  - `add_modifier(modifier_type)` -> `MovieGraphCollectionModifier` — Adds a new modifier of the specified type. Returns a pointer to the new modifier, or nullptr if a modifier of the specif...
  - `get_collections()` -> `Array [ Name ]` — Gets all collections that will be affected by the modifiers on this node.
  - `get_modifier(modifier_type)` -> `MovieGraphCollectionModifier` — Gets the modifier of the specified type, or nullptr if one does not exist on this node.
  - `get_modifiers()` -> `Array [ MovieGraphCollectionModifier ]` — Gets all modifiers currently added to the node.
  - `is_collection_enabled(collection_name)` -> `bool` — Gets the enable state (within this modifier) of the collection with the given name.
  - `remove_collection(collection_name)` -> `bool` — Remove a collection identified by the given name. Returns true if the collection was found and removed successfully, els...
  - `remove_modifier(modifier_type)` -> `bool` — Removes the modifier of the specified type. Returns true on success, or false if a modifier of the specified type does n...
  - `set_collection_enabled(collection_name, is_collection_enabled)` -> `None` — Sets the enable state (within this modifier) of the collection with the given name. Disabled collections will not be mod...

**Properties** (1):
  - `modifier_name`: `str` — [Read-Write] The name of this modifier. (str)

### `unreal.MovieGraphNode`
Inherits: `Object` | Header: `MovieGraphNode.h`

This is a base class for all nodes that can exist in the UMovieGraphConfig network. In the editor, each node in the network will have an editor-only representation too which contains data about itâs...

**Methods** (31):
  - `can_be_added_by_user()` -> `bool` — Determines if this node type can be added to the graph interactively by a user or via the API when constructing a graph.
  - `can_be_disabled()` -> `bool` — Determines if this node can be disabled.
  - `get_branch_restriction()` -> `MovieGraphBranchRestriction` — Determines which types of branches the node can be created in.
  - `get_exposed_properties()` -> `Array [ MovieGraphPropertyInfo ]` — Gets the information about properties which are currently exposed as pins on the node.
  - `get_first_connected_input_pin()` -> `MovieGraphPin` — Gets the first input pin on the node which has a connection, or nullptr if no pins are connected.
  - `get_first_connected_output_pin()` -> `MovieGraphPin` — Gets the first output pin on the node which has a connection, or nullptr if no pins are connected.
  - `get_input_pin(pin_label, pin_requirement = MovieGraphPinQueryRequirement.BUILT_IN_OR_DYNAMIC)` -> `MovieGraphPin` — Gets the input pin with the specified name, or nullptr if one could not be found. Most pins on a node are âbuilt-inâ...
  - `get_input_pin_properties()` -> `Array [ MovieGraphPinProperties ]` — Gets the properties for all input pins.
  - `get_input_pins()` -> `Array [ MovieGraphPin ]` — Gets all input pins on the node. Note that the returned array is const, so input pins cannot be added/removed from the n...
  - `get_keywords()` -> `Text` — Gets the keywords (space-separated) that will be searched in the node creation context menu.
  - `get_menu_category()` -> `Text` — Gets the category that the node belongs under.
  - `get_node_comment()` -> `str` — Get Node Comment
  - `get_node_pos_x()` -> `int32` — Get Node Pos X
  - `get_node_pos_y()` -> `int32` — Get Node Pos Y
  - `get_node_title(get_descriptive = False)` -> `Text` — Gets the nodeâs title. Optionally gets a more descriptive, multi-line title for the node if bGetDescriptive is set to ...
  - `get_output_pin(pin_label)` -> `MovieGraphPin` — Gets the output pin with the specified name, or nullptr if one could not be found.
  - `get_output_pin_properties()` -> `Array [ MovieGraphPinProperties ]` — Gets the properties for all output pins.
  - `get_output_pins()` -> `Array [ MovieGraphPin ]` — Gets all output pins on the node. Note that the returned array is const, so output pins cannot be added/removed from the...
  - `get_overrideable_property_info()` -> `Array [ MovieGraphPropertyInfo ]` — Gets the information about properties which can be exposed as a pin on the node.
  - `is_comment_bubble_pinned()` -> `bool` — Is Comment Bubble Pinned
  - `is_comment_bubble_visible()` -> `bool` — Is Comment Bubble Visible
  - `is_disabled()` -> `bool` — Determines if this node is currently disabled.
  - `promote_property_to_pin(property_name)` -> `None` — Promotes the given PropertyName to a pinned property, if it is found in GetOverrideablePropertyInfo(). Throws a kismet (...
  - `set_disabled(new_disable_state)` -> `None` — Set whether this node is currently disabled.
  - `set_is_comment_bubble_pinned(is_pinned)` -> `None` — Set Is Comment Bubble Pinned
  - `set_is_comment_bubble_visible(is_visible)` -> `None` — Set Is Comment Bubble Visible
  - `set_node_comment(node_comment)` -> `None` — Set Node Comment
  - `set_node_pos_x(node_pos_x)` -> `None` — Set Node Pos X
  - `set_node_pos_y(node_pos_y)` -> `None` — Set Node Pos Y
  - `toggle_promote_property_to_pin(property_name)` -> `None` — Toggles the promotion of the property with the given name to a pin on the node.
  - `unpromote_property_from_pin(property_name)` -> `None` — Unpromotes a given PropertyName from being pinned. Throws a kismet (scripting) exception if itâs not already exposed. ...

**Properties** (1):
  - `script_tags`: `None` — [Read-Write] Tags that can be used to identify this node within a pre/post render script. Tags can b...

### `unreal.MovieGraphOutput`
Inherits: `MovieGraphInterfaceBase` | Header: `MovieGraphConfig.h`

An output exposed on the graph that will be available for nodes to connect to.

### `unreal.MovieGraphOutputNode`
Inherits: `MovieGraphNode` | Header: `MovieGraphOutputNode.h`

A graph node which displays all output members available in the graph.

### `unreal.MovieGraphPin`
Inherits: `Object` | Header: `MovieGraphPin.h`

Movie Graph Pin

**Methods** (12):
  - `allows_multiple_connections()` -> `bool` — Allows Multiple Connections
  - `can_create_connection(other_pin)` -> `bool` — Can Create Connection
  - `edge_count()` -> `int32` — Edge Count
  - `get_all_connected_pins()` -> `Array [ MovieGraphPin ]` — Gets all connected pins.
  - `get_connected_nodes()` -> `Array [ MovieGraphNode ]` — Utility function for scripting which gathers all of the nodes connected to this particular pin. Equivalent to looping th...
  - `get_first_connected_pin(follow_reroute_connections = False)` -> `MovieGraphPin` — Gets the first pin connected to this pin. Returns nullptr if no valid connection exists. If bFollowRerouteConnections is...
  - `is_connected()` -> `bool` — Is Connected
  - `is_connection_to_branch_allowed(other_pin)` -> `Text or None` — Determines if the connection between this pin and OtherPin follows branch restriction rules. OutError is populated with ...
  - `is_input_pin()` -> `bool` — Is Input Pin
  - `is_output_pin()` -> `bool` — Is Output Pin
  - `is_pin_direction_compatible_with(other_pin)` -> `bool` — Determines if this pinâs direction is compatible with another pinâs direction. One needs to be an input, and the oth...
  - `is_type_compatible_with(other_pin)` -> `bool` — Determines if this pinâs type is compatible with another pinâs type. Generally the types have to match exactly in or...

**Properties** (3):
  - `edges`: `None` [Read-Only] — [Read-Only] A list of edges between pins. This is marked as TextExportTransient so that when we copy...
  - `node`: `MovieGraphNode` [Read-Only] — [Read-Only] The node that this pin belongs to. (MovieGraphNode)
  - `properties`: `MovieGraphPinProperties` [Read-Only] — [Read-Only] (MovieGraphPinProperties)

### `unreal.MovieGraphPipeline`
Inherits: `MoviePipelineBase` | Header: `MovieGraphPipeline.h`

Movie Graph Pipeline

**Methods** (9):
  - `get_current_job()` -> `MoviePipelineExecutorJob` — Returns the internal job being used by the Movie Graph Pipeline, which is a duplicate of the job provided originaly by I...
  - `get_current_traversal_context(for_shot = True)` -> `MovieGraphTraversalContext` — Get Current Traversal Context
  - `get_initialization_time()` -> `DateTime` — Returns the time this movie pipeline was initialized at.
  - `get_initialization_time_offset()` -> `Timespan` — The offset that should be applied to the GetInitializationTime() when generating the {time} related filename tokens. Get...
  - `get_renderer_instance()` -> `MovieGraphRendererBase` — Used occasionally to cross-reference other components. Donât call this unless you know what youâre doing.
  - `get_time_step_instance()` -> `MovieGraphTimeStepBase` — Used occasionally to cross-reference other components. Donât call this unless you know what youâre doing.
  - `initialize(job, init_config)` -> `None` — Initialize
  - `on_movie_pipeline_finished_impl()` -> `None` — On Movie Pipeline Finished Impl
  - `set_initialization_time(date_time)` -> `None` — Override the time this movie pipeline was initialized at. This can be used for render farms to ensure that jobs on all m...

### `unreal.MovieGraphProjectSettings`
Inherits: `DeveloperSettings` | Header: `MovieGraphProjectSettings.h`

Settings that apply to the Movie Graph.

### `unreal.MovieGraphQuickRenderModeSettings`
Inherits: `Object` | Header: `MovieGraphQuickRenderSettings.h`

Settings for a specific mode within Quick Render. Note that some modes share settings.

**Methods** (2):
  - `get_variable_assignments_for_graph(graph_config_path)` -> `MovieJobVariableAssignmentContainer` — Gets the variable assignments for the specified graph asset. Note that there will normally only be one variable assignme...
  - `refresh_variable_assignments(settings)` -> `None` [classmethod] — Within the settings object provided, refreshes the variable assignments in the settings to match the settingâs GraphPr...

**Properties** (9):
  - `custom_end_frame`: `int` — [Read-Write] The frame in the level sequence that the render will end on. This frame number is exclu...
  - `custom_start_frame`: `int` — [Read-Write] The frame in the level sequence that the render will begin on. This frame number is inc...
  - `frame_range_type`: `MovieGraphQuickRenderFrameRangeType` — [Read-Write] The frame range that should be used to render from. Only available in modes that render...
  - `graph_preset`: `MovieGraphConfig` — [Read-Write] The graph preset that is used to configure the render. If not specified, the quick rend...
  - `graph_variable_assignments`: `None` — [Read-Write] Values that are set on the variables contained within the graph preset. See note on Ref...
  - `level_sequence_override`: `LevelSequence` — [Read-Write] For sequence-centric modes (like âCurrent Sequenceâ), if this override is set, this...
  - `override_viewport_look_flags`: `bool` — [Read-Write] Enables/disables the ViewportLookFlags property from taking effect. (bool)
  - `post_render_behavior`: `MovieGraphQuickRenderPostRenderActionType` — [Read-Write] The action that quick render should perform after a render is finished. (MovieGraphQuic...
  - `viewport_look_flags`: `int` — [Read-Write] The properties of the viewport that should be applied to the quick render. These will o...

### `unreal.MovieGraphQuickRenderSettings`
Inherits: `Object` | Header: `MovieGraphQuickRenderSettings.h`

Data that specifies user-configurable aspects of a quick render. Settings are stored for each mode separately.

**Methods** (1):
  - `get_saved_quick_render_mode_settings(settings_mode)` -> `MovieGraphQuickRenderModeSettings` [classmethod] — Gets the Quick Render settings (for a specific mode) that have been persisted to disk (Saved/MovieRenderPipeline/QuickRe...

### `unreal.MovieGraphRemoveRenderSettingNode`
Inherits: `MovieGraphNode` | Header: `MovieGraphRemoveRenderSettingNode.h`

A node which can remove other nodes in the graph.

**Properties** (1):
  - `node_type`: `Class` — [Read-Write] The type of node (exact match) that should be removed. (type(Class))

### `unreal.MovieGraphRendererBase`
Inherits: `Object` | Header: `MovieGraphDataTypes.h`

Movie Graph Renderer Base

**Methods** (1):
  - `get_preview_data()` -> `Array [ MovieGraphImagePreviewData ]` — Get an array of image previews that are valid this frame.

### `unreal.MovieGraphRenderLayer`
Inherits: `Object` | Header: `MovieGraphRenderLayerSubsystem.h`

Provides a means of assembling modifiers together to generate a desired view of a scene.

**Methods** (8):
  - `add_modifier(modifier)` -> `None` — Add Modifier
  - `apply(world)` -> `None` — Apply
  - `get_collection_by_name(name)` -> `MovieGraphCollection` — Get Collection by Name
  - `get_modifiers()` -> `Array [ MovieGraphCollectionModifier ]` — Get Modifiers
  - `get_render_layer_name()` -> `Name` — Get Render Layer Name
  - `remove_modifier(modifier)` -> `None` — Remove Modifier
  - `revert()` -> `None` — Revert
  - `set_render_layer_name(new_name)` -> `None` — Set Render Layer Name

### `unreal.MovieGraphRenderLayerNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphRenderLayerNode.h`

Movie Graph Render Layer Node

**Properties** (2):
  - `layer_name`: `str` — [Read-Write] (str)
  - `override_layer_name`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphRenderLayerSubsystem`
Inherits: `WorldSubsystem` | Header: `MovieGraphRenderLayerSubsystem.h`

The primary means of controlling render layers in MRQ. Render layers can be added/registered with the subsystem, then made active in order to view them. Collections and modifiers can also be viewed, b...

**Methods** (9):
  - `add_render_layer(render_layer)` -> `bool` — Adds a render layer to the system, which can later be made active by SetActiveRenderLayer*(). Returns true if the layer ...
  - `clear_active_render_layer()` -> `None` — Clears the currently active render layer and reverts its modifiers.
  - `get_active_render_layer()` -> `MovieGraphRenderLayer` — Gets the currently active render layer (the layer with its modifiers applied).
  - `get_from_world(world)` -> `MovieGraphRenderLayerSubsystem` [classmethod] — Get this subsystem for a specific world. Handy for use from Python.
  - `get_render_layers()` -> `Array [ MovieGraphRenderLayer ]` — Gets all render layers which are currently tracked by the system.
  - `remove_render_layer(render_layer_name)` -> `None` — Removes the render layer with the given name. After removal it can no longer be made active with SetActiveRenderLayerBy*...
  - `reset()` -> `None` — Clear out all tracked render layers and collections.
  - `set_active_render_layer_by_name(render_layer_name)` -> `None` — Applies the layer with the given name. The layer needs to have been registered with AddRenderLayer().
  - `set_active_render_layer_by_obj(render_layer)` -> `None` — Applies the given layer. The layer does not need to have been registered with AddRenderLayer().

### `unreal.MovieGraphRenderPassNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphRenderPassNode.h`

The UMovieGraphRenderPassNode node defines a render pass that MRQ may produce. This node can be implemented in the graph multiple times, and the exact settings it should use can be created out of a mi...

### `unreal.MovieGraphRenderPreviewWidget`
Inherits: `UserWidget` | Header: `MovieRenderDebugWidget.h`

C++ Base Class for the preview widget that is drawn onto the game viewport (but not burned into the output files) that allow us to easily visualize the current state of the pipeline. Used for Graph Ba...

**Methods** (1):
  - `on_initialized_for_pipeline(pipeline)` -> `None` — On Initialized for Pipeline

### `unreal.MovieGraphRenderPropertyModifier`
Inherits: `MovieGraphCollectionModifier` | Header: `MovieGraphRenderLayerSubsystem.h`

Modifies actor visibility.

**Methods** (4):
  - `apply_modifier(world)` -> `None` — Apply Modifier
  - `is_hidden()` -> `bool` — Is Hidden
  - `set_hidden(is_hidden)` -> `None` — ~UObject Interface
  - `undo_modifier()` -> `None` — Undo Modifier

**Properties** (11):
  - `affect_indirect_lighting_while_hidden`: `bool` — [Read-Write] Controls whether the primitive should affect indirect lighting when hidden. (bool)
  - `cast_shadow_while_hidden`: `bool` — [Read-Write] If true, the primitive will cast shadows even if it is hidden. (bool)
  - `casts_shadows`: `bool` — [Read-Write] If true, the primitive will cast shadows. (bool)
  - `holdout`: `bool` — [Read-Write] If true, the primitive will render black with an alpha of 0, but all secondary effects ...
  - `override_b_affect_indirect_lighting_while_hidden`: `bool` — [Read-Write] (bool)
  - `override_b_cast_shadow_while_hidden`: `bool` — [Read-Write] (bool)
  - `override_b_casts_shadows`: `bool` — [Read-Write] (bool)
  - `override_b_holdout`: `bool` — [Read-Write] (bool)
  - `override_b_is_hidden`: `bool` — [Read-Write] (bool)
  - `override_b_process_editor_only_actors`: `bool` — [Read-Write] (bool)
  - `process_editor_only_actors`: `bool` — [Read-Write] If true, this modifier will process editor-only actors (eg, the billboard icons that re...

### `unreal.MovieGraphRerouteNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphRerouteNode.h`

A node which is effectively a no-op/passthrough. Allows a connection to be routed untouched through this node to organize the graph.

**Methods** (2):
  - `get_pin_properties()` -> `MovieGraphPinProperties` — Sets the pin properties for this reroute node. Note that this applies to both the input and output pin.
  - `set_pin_properties(pin_properties)` -> `None` — Sets the pin properties for this reroute node (both the input and output pin have the same properties). This generally s...

### `unreal.MovieGraphSamplingMethodNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphSamplingMethodNode.h`

A node which configures sampling properties for renderers.

**Properties** (3):
  - `override_sampling_method_class`: `bool` — [Read-Write] (bool)
  - `override_temporal_sample_count`: `bool` — [Read-Write] (bool)
  - `temporal_sample_count`: `int` — [Read-Write] The number of temporal samples which should be taken on one frame. Applies only to line...

### `unreal.MovieGraphScriptBase`
Inherits: `Object` | Header: `MovieGraphExecuteScriptNode.h`

This is the base class used for writing scripts that can be executed as part of a Movie Graph Pipeline. After creating a UMovieGraphExecuteScriptNode (named âExecute Scriptâ in the UI) in your Mov...

**Methods** (5):
  - `is_per_shot_callback_needed()` -> `bool` — If you return true here the system will stall at the end of every shot and wait until all files have been written to dis...
  - `on_job_finished(job_copy, output_data)` -> `None` — This callback is called at the very end of the Movie Graph Pipeline, right before shutting down. The files specified by ...
  - `on_job_start(job_copy)` -> `None` — This callback is called very early on in the Movie Graph Pipeline, before data is read from the job. The provided job is...
  - `on_shot_finished(job_copy, shot_copy, output_data)` -> `None` — This is similar to OnJobFinished but called after a shot is completed. This is only called if IsPerShotCallbackNeeded() ...
  - `on_shot_start(job_copy, shot_copy)` -> `None` — This is similar to OnJobStart but called before a shot is set up. This is only called if IsPerShotCallbackNeeded() retur...

### `unreal.MovieGraphSelectNode`
Inherits: `MovieGraphNode` | Header: `MovieGraphSelectNode.h`

A node which creates a condition that selects from a set of input branches.

### `unreal.MovieGraphSequenceDataSource`
Inherits: `MovieGraphDataSourceBase` | Header: `MovieGraphSequenceDataSource.h`

The UMovieGraphSequenceDataSource allows using a ULevelSequence as the external datasource for the Movie Graph. It will build the range of shots based on the contents of the level sequence (one shot p...

### `unreal.MovieGraphSetCVarValueNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphSetCVarValueNode.h`

A node which can set a specific console variableâs value.

**Properties** (4):
  - `name`: `str` — [Read-Write] The name of the CVar having its value set. (str)
  - `override_name`: `bool` — [Read-Write] (bool)
  - `override_value`: `bool` — [Read-Write] (bool)
  - `value`: `float` — [Read-Write] The new value of the CVar. (float)

### `unreal.MovieGraphSetMetadataAttributesNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphSetMetadataAttributesNode.h`

A node which can set a specific metadata attributes.

**Properties** (2):
  - `metadata_attribute_collection`: `MovieGraphMetadataAttributeCollection` [Read-Only] — [Read-Only] A container of metadata attributes to be added to the file. (MovieGraphMetadataAttribute...
  - `override_metadata_attribute_collection`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphSetStartEndConsoleCommandsNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphSetStartEndConsoleCommandsNode.h`

A node which can set console commands to run at the start and end of a render.

**Properties** (2):
  - `console_commands`: `MovieGraphStartEndConsoleCommands` [Read-Only] — [Read-Only] (MovieGraphStartEndConsoleCommands)
  - `override_console_commands`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphSettingNode`
Inherits: `MovieGraphNode` | Header: `MovieGraphNode.h`

Nodes representing user settings should derive from this. This is the only node type copied into flattened eval.

### `unreal.MovieGraphShowFlags`
Inherits: `Object` | Header: `MovieGraphShowFlags.h`

Stores show flag enable/disable state, as well as per-flag override state.

### `unreal.MovieGraphStartEndConsoleCommands`
Inherits: `Object` | Header: `MovieGraphSetStartEndConsoleCommandsNode.h`

Console commands that can execute within the UMovieGraphSetStartEndConsoleCommandsNode.

**Properties** (4):
  - `add_end_commands`: `None` — [Read-Write] Console commands to execute when this shot finishes rendering. Used to restore changes ...
  - `add_start_commands`: `None` — [Read-Write] Console commands to execute when this shot starts rendering. If the command(s) need to ...
  - `remove_end_commands`: `None` — [Read-Write] End commands that should be removed from upstream nodes. Commands entered here must be ...
  - `remove_start_commands`: `None` — [Read-Write] Start commands that should be removed from upstream nodes. Commands entered here must b...

### `unreal.MovieGraphSubgraphNode`
Inherits: `MovieGraphNode` | Header: `MovieGraphSubgraphNode.h`

A node which represents another graph asset. Inputs/outputs on this subgraph will update if the underlying graph assetâs inputs/outputs change.

**Methods** (2):
  - `get_subgraph_asset()` -> `MovieGraphConfig` — Gets the graph asset this subgraph points to.
  - `set_sub_graph_asset(subgraph_asset)` -> `None` — Sets the graph asset this subgraph points to.

### `unreal.MovieGraphTimeStepBase`
Inherits: `Object` | Header: `MovieGraphDataTypes.h`

Movie Graph Time Step Base

**Methods** (1):
  - `get_calculated_time_data()` -> `MovieGraphTimeStepData` — TickProducingFrames will be called for a frame (before the frame starts) and then this will be called at the end of the ...

### `unreal.MovieGraphUIRendererNode`
Inherits: `MovieGraphWidgetRendererBaseNode` | Header: `MovieGraphUIRendererNode.h`

A node which renders the viewportâs UMG widget to a standalone image, or composited on top of a render layer.

### `unreal.MovieGraphValueContainer`
Inherits: `Object` | Header: `MovieGraphValueContainer.h`

Holds a generic value, with an API for getting/setting the value, as well as getting/setting its type and container (eg, array).

**Methods** (32):
  - `get_value_bool()` -> `bool or None` — Gets the bool value of the held data. Returns true on success, else false.
  - `get_value_byte()` -> `uint8 or None` — Gets the byte value of the held data. Returns true on success, else false.
  - `get_value_class()` -> `type(Class) or None` — Gets the UClass value of the held data. Returns true on success, else false.
  - `get_value_container_type()` -> `MovieGraphContainerType` — Gets the container type of the stored value.
  - `get_value_double()` -> `double or None` — Gets the double value of the held data. Returns true on success, else false.
  - `get_value_enum(requested_enum = None)` -> `uint8 or None` — Gets the enum value (for a specific enum) of the held data. Returns true on success, else false.
  - `get_value_float()` -> `float or None` — Gets the float value of the held data. Returns true on success, else false.
  - `get_value_int32()` -> `int32 or None` — Gets the int32 value of the held data. Returns true on success, else false.
  - `get_value_int64()` -> `int64 or None` — Gets the int64 value of the held data. Returns true on success, else false.
  - `get_value_name()` -> `Name or None` — Gets the FName value of the held data. Returns true on success, else false.
  - `get_value_object(out_value, requested_class = None)` -> `bool` — Gets the object value (for a specific class) of the held data. Returns true on success, else false.
  - `get_value_serialized_string()` -> `str` — Gets the serialized string value of the held data.
  - `get_value_string()` -> `str or None` — Gets the FString value of the held data. Returns true on success, else false.
  - `get_value_text()` -> `Text or None` — Gets the FText value of the held data. Returns true on success, else false.
  - `get_value_type()` -> `MovieGraphValueType` — Gets the type of the stored data.
  - `get_value_type_object()` -> `Object` — Gets the object that defines the enum, struct, or class.
  - `set_value_bool(value)` -> `bool` — Sets the bool value of the held data. Returns true on success, else false.
  - `set_value_byte(value)` -> `bool` — Sets the byte value of the held data. Returns true on success, else false.
  - `set_value_class(value)` -> `bool` — Sets the class value of the held data. Returns true on success, else false.
  - `set_value_container_type(container_type)` -> `None` — Sets the container type of the stored value.
  - `set_value_double(value)` -> `bool` — Sets the double value of the held data. Returns true on success, else false.
  - `set_value_enum(value, enum)` -> `bool` — Sets the enum value of the held data. Returns true on success, else false.
  - `set_value_float(value)` -> `bool` — Sets the float value of the held data. Returns true on success, else false.
  - `set_value_int32(value)` -> `bool` — Sets the int32 value of the held data. Returns true on success, else false.
  - `set_value_int64(value)` -> `bool` — Sets the int64 value of the held data. Returns true on success, else false.
  - `set_value_name(value)` -> `bool` — Sets the FName value of the held data. Returns true on success, else false.
  - `set_value_object(value)` -> `bool` — Sets the object value of the held data. Returns true on success, else false.
  - `set_value_serialized_string(new_value)` -> `bool` — Sets the serialized value of the held data. The string should be the serialized representation of the value. Returns tru...
  - `set_value_string(value)` -> `bool` — Sets the FString value of the held data. Returns true on success, else false.
  - `set_value_text(value)` -> `bool` — Sets the FText value of the held data. Returns true on success, else false.
  - `set_value_type(value_type, value_type_object = None)` -> `None` — Sets the type of the stored data. Enums, structs, and classes must specify a value type object.
  - `set_value_type_object(value_type_object)` -> `None` — Sets the object that defines the enum, struct, or class.

### `unreal.MovieGraphVariable`
Inherits: `MovieGraphMember` | Header: `MovieGraphConfig.h`

A variable that can be used inside the graph. Most variables are created by the user, and can have their value changed at the job level. Global variables, however, are not user-created and their value...

**Methods** (3):
  - `get_category()` -> `str` — Gets the category (if any) assigned to this variable.
  - `is_global()` -> `bool` — Returns true if this variable is a global variable.
  - `set_category(new_category)` -> `None` — Sets the variable to the provided category. Be aware that the category provided here may not be the final category set o...

### `unreal.MovieGraphVariableNode`
Inherits: `MovieGraphNode` | Header: `MovieGraphVariableNode.h`

A node which gets the value of a variable which has been defined on the graph.

**Methods** (2):
  - `get_variable()` -> `MovieGraphVariable` — Gets the variable that this node represents.
  - `set_variable(variable)` -> `None` — Sets the variable that this node represents.

### `unreal.MovieGraphVideoOutputNode`
Inherits: `MovieGraphFileOutputNode` | Header: `MovieGraphVideoOutputNode.h`

A base node for nodes that generate video in the Movie Render Graph.

### `unreal.MovieGraphWarmUpSettingNode`
Inherits: `MovieGraphSettingNode` | Header: `MovieGraphWarmUpSettingNode.h`

Movie Graph Warm Up Setting Node

**Properties** (4):
  - `emulate_motion_blur`: `bool` — [Read-Write] If true, we will evaluate frame 0, and then wait NumWarmUpFrames frames, rendering each...
  - `num_warm_up_frames`: `int` — [Read-Write] At the start of each shot, how many frames should we run the engine (without writing re...
  - `override_b_emulate_motion_blur`: `bool` — [Read-Write] (bool)
  - `override_num_warm_up_frames`: `bool` — [Read-Write] (bool)

### `unreal.MovieGraphWidgetRendererBaseNode`
Inherits: `MovieGraphRenderPassNode` | Header: `MovieGraphWidgetRendererBaseNode.h`

Base node containing common logic for nodes that render widgets.

**Properties** (2):
  - `composite_onto_final_image`: `bool` — [Read-Write] If true, the pass will be composited onto each render. Does not apply to multi-layer EX...
  - `override_b_composite_onto_final_image`: `bool` — [Read-Write] (bool)

### `unreal.MovieJobVariableAssignmentContainer`
Inherits: `Object` | Header: `MovieJobVariableAssignmentContainer.h`

Holds a group of properties which override variable values on the jobâs associated graph (if any). Overrides are not added manually. Instead, UpdateGraphVariableOverrides() should be called which wi...

**Methods** (34):
  - `get_graph_config()` -> `MovieGraphConfig` — Gets the graph that is associated with this container.
  - `get_value_bool(graph_variable)` -> `bool or None` — Gets the bool value of the specified property. Returns true on success, else false.
  - `get_value_byte(graph_variable)` -> `uint8 or None` — Gets the byte value of the specified property. Returns true on success, else false.
  - `get_value_class(graph_variable)` -> `type(Class) or None` — Gets the UClass value of the specified property. Returns true on success, else false.
  - `get_value_container_type(graph_variable)` -> `MovieGraphContainerType` — Gets the container type of the stored value in the specified property.
  - `get_value_double(graph_variable)` -> `double or None` — Gets the double value of the specified property. Returns true on success, else false.
  - `get_value_enum(graph_variable, requested_enum = None)` -> `uint8 or None` — Gets the enum value (for a specific enum) of the specified property. Returns true on success, else false.
  - `get_value_float(graph_variable)` -> `float or None` — Gets the float value of the specified property. Returns true on success, else false.
  - `get_value_int32(graph_variable)` -> `int32 or None` — Gets the int32 value of the specified property. Returns true on success, else false.
  - `get_value_int64(graph_variable)` -> `int64 or None` — Gets the int64 value of the specified property. Returns true on success, else false.
  - `get_value_name(graph_variable)` -> `Name or None` — Gets the FName value of the specified property. Returns true on success, else false.
  - `get_value_object(graph_variable, out_value, requested_class = None)` -> `bool` — Gets the object value (for a specific class) of the specified property. Returns true on success, else false.
  - `get_value_serialized_string(graph_variable)` -> `str` — Gets the serialized string value of the specified property.
  - `get_value_string(graph_variable)` -> `str or None` — Gets the FString value of the specified property. Returns true on success, else false.
  - `get_value_text(graph_variable)` -> `Text or None` — Gets the FText value of the specified property. Returns true on success, else false.
  - `get_value_type(graph_variable)` -> `MovieGraphValueType` — Gets the type of the value stored in the specified property.
  - `get_value_type_object(graph_variable)` -> `Object` — Gets the object that defines the enum, struct, or class stored in the specified property.
  - `get_variable_assignment_enable_state(graph_variable)` -> `bool or None` — Gets the enable state of the variable assignment for the provided graph variable. The enable state is provided via bOutI...
  - `set_graph_config(graph_config)` -> `None` — Sets the graph config associated with the variable assignments.
  - `set_value_bool(graph_variable, value)` -> `bool` — Sets the bool value of the specified property. Returns true on success, else false.
  - `set_value_byte(graph_variable, value)` -> `bool` — Sets the byte value of the specified property. Returns true on success, else false.
  - `set_value_class(graph_variable, value)` -> `bool` — Sets the class value of the specified property. Returns true on success, else false.
  - `set_value_double(graph_variable, value)` -> `bool` — Sets the double value of the specified property. Returns true on success, else false.
  - `set_value_enum(graph_variable, value, enum)` -> `bool` — Sets the enum value of the specified property. Returns true on success, else false.
  - `set_value_float(graph_variable, value)` -> `bool` — Sets the float value of the specified property. Returns true on success, else false.
  - `set_value_int32(graph_variable, value)` -> `bool` — Sets the int32 value of the specified property. Returns true on success, else false.
  - `set_value_int64(graph_variable, value)` -> `bool` — Sets the int64 value of the specified property. Returns true on success, else false.
  - `set_value_name(graph_variable, value)` -> `bool` — Sets the FName value of the specified property. Returns true on success, else false.
  - `set_value_object(graph_variable, value)` -> `bool` — Sets the object value of the specified property. Returns true on success, else false.
  - `set_value_serialized_string(graph_variable, new_value)` -> `bool` — Sets the serialized value of this member. The string should be the serialized representation of the value. Returns true ...
  - `set_value_string(graph_variable, value)` -> `bool` — Sets the FString value of the specified property. Returns true on success, else false.
  - `set_value_text(graph_variable, value)` -> `bool` — Sets the FText value of the specified property. Returns true on success, else false.
  - `set_variable_assignment_enable_state(graph_variable, is_enabled)` -> `bool` — Updates an existing variable assignment for the provided graph variable to a new enable state, or adds a new assignment ...
  - `update_graph_variable_overrides()` -> `None` — Updates the stored variable overrides to reflect the graph preset. Existing overrides will be updated to match the graph...

### `unreal.MoviePipeline`
Inherits: `MoviePipelineBase` | Header: `MoviePipeline.h`

Movie Pipeline

**Methods** (8):
  - `get_current_job()` -> `MoviePipelineExecutorJob` — Get Current Job
  - `get_initialization_time()` -> `DateTime` — Returns the time this movie pipeline was initialized at.
  - `get_initialization_time_offset()` -> `Timespan` — The offset that should be applied to the GetInitializationTime() when generating the {time} related filename tokens. Get...
  - `get_pipeline_primary_config()` -> `MoviePipelinePrimaryConfig` — Get the Primary Configuration used to render this shot. This contains the global settings for the shot, as well as per-s...
  - `get_preview_texture()` -> `Texture` — Get Preview Texture
  - `initialize(job)` -> `None` — Initialize the movie pipeline with the specified settings. This kicks off the rendering process.
  - `on_movie_pipeline_finished_impl()` -> `None` — This function should be called by the Executor when execution has finished (this should still be called in the event of ...
  - `set_initialization_time(date_time)` -> `None` — Override the time this movie pipeline was initialized at. This can be used for render farms to ensure that jobs on all m...

### `unreal.MoviePipelineAntiAliasingSetting`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineAntiAliasingSetting.h`

Movie Pipeline Anti Aliasing Setting

**Properties** (8):
  - `anti_aliasing_method`: `AntiAliasingMethod` — [Read-Write] If we are overriding the AA method, what do we use? None will turn off anti-aliasing. (...
  - `engine_warm_up_count`: `int` — [Read-Write] The number of frames at the start of each shot that the engine will run without renderi...
  - `override_anti_aliasing`: `bool` — [Read-Write] Should we override the Projectâs anti-aliasing setting during a movie render? This ca...
  - `render_warm_up_count`: `int` — [Read-Write] The number of frames at the start of each shot that the engine will render and then dis...
  - `render_warm_up_frames`: `bool` — [Read-Write] Should we submit the warm-up frames to the GPU? Generally you want this disabled (as it...
  - `spatial_sample_count`: `int` — [Read-Write] How many frames should we accumulate together before contributing to one overall sample...
  - `temporal_sample_count`: `int` — [Read-Write] The number of frames we should combine together to produce each output frame. This blen...
  - `use_camera_cut_for_warm_up`: `bool` — [Read-Write] Should we use the excess in the camera cut track to determine engine warmup? When disab...

### `unreal.MoviePipelineBase`
Inherits: `Object` | Header: `MoviePipelineBase.h`

Movie Pipeline Base

**Methods** (4):
  - `get_pipeline_state()` -> `MovieRenderPipelineState` — Get Pipeline State
  - `is_shutdown_requested()` -> `bool` — Has RequestShutdown() been called?
  - `request_shutdown(is_error = False)` -> `None` — Request the movie pipeline to shut down at the next available time. The pipeline will attempt to abandon the current fra...
  - `shutdown(is_error = False)` -> `None` — Abandons any future work on this Movie Pipeline and runs through the shutdown flow to ensure already completed work is w...

**Properties** (2):
  - `on_movie_pipeline_shot_work_finished_delegate`: `MoviePipelineWorkFinished` — [Read-Write] This callback will not be called by default due to performance reasons. You need to opt...
  - `on_movie_pipeline_work_finished_delegate`: `MoviePipelineWorkFinished` — [Read-Write] Called when we have completely finished this pipeline. This means that all frames have ...

### `unreal.MoviePipelineCameraSetting`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineCameraSetting.h`

Movie Pipeline Camera Setting

**Properties** (4):
  - `override_camera_overscan`: `bool` — [Read-Write] If true, the camera settings overscan value will override any overscan on the cameras w...
  - `overscan_percentage`: `float` — [Read-Write] Overscan percent allows to render additional pixels beyond the set resolution and can b...
  - `render_all_cameras`: `bool` — [Read-Write] If true, when a Camera Cut section is found we will also render any other cameras withi...
  - `shutter_timing`: `MoviePipelineShutterTiming` — [Read-Write] Shutter Timing allows you to bias the timing of your shutter angle to either be before,...

### `unreal.MoviePipelineColorSetting`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineColorSetting.h`

Movie Pipeline Color Setting

**Properties** (2):
  - `disable_tone_curve`: `bool` — [Read-Write] If true the Filmic Tone Curve will not be applied. Disabling this will allow you to exp...
  - `ocio_configuration`: `OpenColorIODisplayConfiguration` — [Read-Write] OCIO config to be passed to OCIO View Extension. If this is enabled the Tone Curve will...

### `unreal.MoviePipelineCommandLineEncoder`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineCommandLineEncoder.h`

Movie Pipeline Command Line Encoder

**Properties** (6):
  - `additional_command_line_args`: `str` — [Read-Write] Any additional arguments to pass to the CLI encode for this particular job. (str)
  - `delete_source_files`: `bool` — [Read-Write] Should we delete the source files from disk after encoding? (bool)
  - `file_name_format_override`: `str` — [Read-Write] File name format string override. If specified it will override the FileNameFormat from...
  - `quality`: `MoviePipelineEncodeQuality` — [Read-Write] What encoding quality to use for this job? Exact command line arguments for each one ar...
  - `skip_encode_on_render_canceled`: `bool` — [Read-Write] If a render was canceled (via hitting escape mid render) should we skip trying to encod...
  - `write_each_frame_duration`: `bool` — [Read-Write] Write the duration for each frame into the generated text file. Needed for some input t...

### `unreal.MoviePipelineCommandLineEncoderSettings`
Inherits: `DeveloperSettings` | Header: `MoviePipelineCommandLineEncoderSettings.h`

Movie Pipeline Command Line Encoder Settings

**Properties** (11):
  - `audio_codec`: `str` — [Read-Write] Which audio codec should we use? Run âMovieRenderPipeline.DumpCLIEncoderCodecsâ for...
  - `audio_input_string_format`: `str` — [Read-Write] Format string used for each audio input. (str)
  - `command_line_format`: `str` — [Read-Write] The format string used when building the final command line argument to launch. (str)
  - `encode_settings_epic`: `str` — [Read-Write] The flags used for epic quality encoding. (str)
  - `encode_settings_high`: `str` — [Read-Write] The flags used for high quality encoding. (str)
  - `encode_settings_low`: `str` — [Read-Write] The flags used for low quality encoding. (str)
  - `encode_settings_med`: `str` — [Read-Write] The flags used for medium quality encoding. (str)
  - `executable_path`: `str` — [Read-Write] Path to the executable (including extension). Can just be âffmpeg.exeâ if it can be...
  - `output_file_extension`: `str` — âwebmâ. (str) [Read-Write] Extension for the output files. Many encoders use this to determine t...
  - `video_codec`: `str` — [Read-Write] Which video codec should we use? Run âMovieRenderPipeline.DumpCLIEncoderCodecsâ for...
  - `video_input_string_format`: `str` — [Read-Write] Format string used for each video input. (str)

### `unreal.MoviePipelineConfigBase`
Inherits: `Object` | Header: `MoviePipelineConfigBase.h`

Movie Pipeline Config Base

**Methods** (8):
  - `copy_from(config)` -> `None` — Copy this configuration from another existing configuration.
  - `find_or_add_setting_by_class(class_, include_disabled_settings = False, exact_match = False)` -> `MoviePipelineSetting` — Finds a setting of a particular type for this pipeline config, adding it if it doesnât already exist.
  - `find_setting_by_class(class_, include_disabled_settings = False, exact_match = False)` -> `MoviePipelineSetting` — Find a setting of a particular type for this config.
  - `find_settings_by_class(class_, include_disabled_settings = False, exact_match = False)` -> `Array [ MoviePipelineSetting ]` — Find all settings of a particular type for this config.
  - `get_config_origin()` -> `MoviePipelineConfigBase` — Gets the config that this config was originally based on (if any). The origin will only be set on transient configs; the...
  - `get_user_settings()` -> `Array [ MoviePipelineSetting ]` — Returns an array of all settings in this config that the user has added via the UI or via Scripting.
  - `remove_setting(setting)` -> `None` — Removes the specific instance from our Setting list.
  - `set_config_origin(config)` -> `None` — Sets the config that this config originated from (if any). The origin should only be set for transient configs.

### `unreal.MoviePipelineDebugSettings`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineDebugSettings.h`

Movie Pipeline Debug Settings

**Properties** (4):
  - `capture_frame`: `int` — [Read-Write] Used when capturing rendering information with RenderDoc. In Display Rate frames. (int3...
  - `capture_frames_with_render_doc`: `bool` — [Read-Write] If true, automatically trigger RenderDoc to capture rendering information for frames fr...
  - `capture_unreal_insights_trace`: `bool` — [Read-Write] If true, automatically capture an Unreal Insights trace file for the duration of the re...
  - `write_all_samples`: `bool` — [Read-Write] If true, we will write all samples that get generated to disk individually. This can be...

### `unreal.MoviePipelineExecutorBase`
Inherits: `Object` | Header: `MoviePipelineExecutor.h`

A Movie Pipeline Executor is responsible for executing an array of Movie Pipelines, and (optionally) reporting progress back for the movie pipelines. The entire array is passed at once to allow the im...

**Methods** (17):
  - `cancel_all_jobs()` -> `None` — Abort the currently executing job and skip all other jobs.
  - `cancel_current_job()` -> `None` — Abort the currently executing job.
  - `connect_socket(host_name, port)` -> `bool` — Attempts to connect a socket to the specified host and port. This is a blocking call.
  - `disconnect_socket()` -> `None`
  - `execute(pipeline_queue)` -> `None` — Execute the provided Queue. You are responsible for deciding how to handle each job in the queue and processing them. On...
  - `get_status_message()` -> `str` — Get the current status message for this job. May be an empty string. For C++ implementations override virtual FString Ge...
  - `get_status_progress()` -> `float` — Get the current progress as last set by SetStatusProgress. 0 by default. For C++ implementations override virtual float ...
  - `is_rendering()` -> `bool` — Report the current state of the executor. This is used to know if we can call Execute again. For C++ implementations ove...
  - `is_socket_connected()` -> `bool` — Returns true if the socket is currently connected, false otherwise. Call ConnectSocket to attempt a connection.
  - `on_begin_frame()` -> `None` — Called once at the beginning of each engine frame. For C++ implementations override virtual bool OnBeginFrame_Implementa...
  - `on_executor_errored_impl(errored_pipeline, fatal, error_reason)` -> `None` — On Executor Errored Impl
  - `on_executor_finished_impl()` -> `None` — This should be called when the Executor has finished executing all of the things it has been asked to execute. This shou...
  - `send_http_request(url, verb, message, headers)` -> `int32` — Sends a asynchronous HTTP request. Responses will be returned in the the OnHTTPResponseRecieved event.
  - `send_socket_message(message)` -> `bool` — Sends a socket message if the socket is currently connected. Messages back will happen in the OnSocketMessageRecieved ev...
  - `set_movie_pipeline_class(pipeline_class)` -> `None` — Specify which MoviePipeline class type should be created by this executor when processing jobs.
  - `set_status_message(status)` -> `None` — Set the status of this Executor. Does nothing in default implementations, but a useful shorthand for implementations to ...
  - `set_status_progress(progress)` -> `None` — Set the progress of this Executor. Does nothing in default implementations, but a useful shorthand for implementations t...

**Properties** (7):
  - `debug_widget_class`: `Class` — [Read-Write] (type(Class))
  - `http_response_recieved_delegate`: `MoviePipelineHttpResponseRecieved` — [Read-Write] If an HTTP Request has been made and a response returned, the returned response will be...
  - `on_executor_errored_delegate`: `OnMoviePipelineExecutorErrored` — [Read-Write] Called when an individual job reports a warning/error. Jobs are considered fatal
if the...
  - `on_executor_finished_delegate`: `OnMoviePipelineExecutorFinished` — [Read-Write] Called when the Executor has finished all jobs. Reports success if no jobs
had fatal er...
  - `socket_message_recieved_delegate`: `MoviePipelineSocketMessageRecieved` — [Read-Write] If this executor has been configured to connect to a socket, this event will be called ...
  - `target_pipeline_class`: `Class` — [Read-Write] Which Pipeline Class should be created by this Executor. May be null. (type(Class))
  - `user_data`: `str` — [Read-Write] Arbitrary data that can be associated with the executor. Not used by default implementa...

### `unreal.MoviePipelineExecutorJob`
Inherits: `Object` | Header: `MoviePipelineQueue.h`

A particular job within the Queue

**Methods** (17):
  - `get_configuration()` -> `MoviePipelinePrimaryConfig` — Gets the configuration for the job. This configuration is either standalone (not associated with any preset), or contain...
  - `get_graph_preset()` -> `MovieGraphConfig` — Gets the graph-style preset that this job is using. If the job is not using a graph-style preset, returns nullptr. see: ...
  - `get_or_create_variable_overrides(graph)` -> `MovieJobVariableAssignmentContainer` — This method will return you the object which contains variable overrides for the Primary Graph assigned to this job. You...
  - `get_preset_origin()` -> `MoviePipelinePrimaryConfig` — Gets the preset for this job, but only if the preset has not been modified. If it has been modified, or the preset no lo...
  - `get_status_message()` -> `str` — Get the current status message for this job. May be an empty string. For C++ implementations override virtual FString Ge...
  - `get_status_progress()` -> `float` — Get the current progress as last set by SetStatusProgress. 0 by default. For C++ implementations override virtual float ...
  - `is_consumed()` -> `bool` — Gets whether or not the job has been marked as being consumed. A consumed job is not editable in the UI and should not b...
  - `is_enabled()` -> `bool` — Gets whether or not the job has been marked as being enabled. For C++ implementations override virtual bool IsEnabled_Im...
  - `is_using_graph_configuration()` -> `bool` — Returns true if this job is using graph-style configuration, else false.
  - `on_duplicated()` -> `None` — Should be called to clear status and user data after duplication so that jobs stay unique and donât pick up ids or oth...
  - `set_configuration(preset)` -> `None` — Set Configuration
  - `set_consumed(consumed)` -> `None` — Set the job to be consumed. A consumed job is disabled in the UI and should not be submitted for rendering again. This a...
  - `set_graph_preset(graph_preset, update_variable_assignments = True)` -> `None` — Sets the graph-style preset that this job will use. Note that this will cause the graph to switch over to using graph-st...
  - `set_is_enabled(enabled)` -> `None` — Set the job to be enabled/disabled. This is exposed to the user in the Queue UI so they can disable a job after loading ...
  - `set_preset_origin(preset)` -> `None` — Set Preset Origin
  - `set_status_message(status)` -> `None` — Set the status of this job to the given value. This will be shown on the UI if progress is set to a value less than zero...
  - `set_status_progress(progress)` -> `None` — Set the progress of this job to the given value. If a positive value is provided the UI will show the progress bar, whil...

**Properties** (8):
  - `author`: `str` — [Read-Write] (Optional) If left blank, will default to system username. Can be shown in burn in as a...
  - `comment`: `str` — [Read-Write] (Optional) If specified, will be shown in the burn in to allow users to keep track of n...
  - `console_variable_overrides`: `None` — [Read-Write] (Optional) Console variable overrides which are applied after cvars set via nodes. Only...
  - `job_name`: `str` — [Read-Write] (Optional) Name of the job. Shown on the default burn-in. (str)
  - `map`: `SoftObjectPath` — [Read-Write] Which map should this job render on (SoftObjectPath)
  - `sequence`: `SoftObjectPath` — [Read-Write] Which sequence should this job render? (SoftObjectPath)
  - `shot_info`: `None` — [Read-Write] (Optional) Shot specific information. If a shot is missing from this list it will assum...
  - `user_data`: `str` — [Read-Write] Arbitrary data that can be associated with the job. Not used by default implementations...

### `unreal.MoviePipelineExecutorShot`
Inherits: `Object` | Header: `MoviePipelineQueue.h`

This class represents a segment of work within the Executor Job. This should be owned by the UMoviePipelineExecutorJob and can be created before the movie pipeline starts to configure some aspects abo...

**Methods** (15):
  - `allocate_new_shot_override_config(config_type = None)` -> `MoviePipelineShotConfig` — Allocate New Shot Override Config
  - `get_camera_name(camera_index)` -> `str` — Get Camera Name
  - `get_graph_preset()` -> `MovieGraphConfig` — Gets the graph-style preset that this job is using. If the job is not using a graph-style preset, returns nullptr.
  - `get_or_create_variable_overrides(graph, is_for_primary_overrides = False)` -> `MovieJobVariableAssignmentContainer` — This method will return you the object which contains variable overrides for either the Jobâs Primary or the Shotâs ...
  - `get_shot_override_configuration()` -> `MoviePipelineShotConfig` — Get Shot Override Configuration
  - `get_shot_override_preset_origin()` -> `MoviePipelineShotConfig` — Get Shot Override Preset Origin
  - `get_status_message()` -> `str` — Get the current status message for this shot. May be an empty string. For C++ implementations override virtual FString G...
  - `get_status_progress()` -> `float` — Get the current progress as last set by SetStatusProgress. 0 by default. For C++ implementations override virtual float ...
  - `is_using_graph_configuration()` -> `bool` — Returns true if this job is using graph-style configuration, else false.
  - `set_graph_preset(graph_preset, update_variable_assignments = True)` -> `None` — Sets the graph-style preset that this job will use. Note that this will cause the graph to switch over to using graph-st...
  - `set_shot_override_configuration(preset)` -> `None` — Set Shot Override Configuration
  - `set_shot_override_preset_origin(preset)` -> `None` — Set Shot Override Preset Origin
  - `set_status_message(status)` -> `None` — Set the status of this shot to the given value. This will be shown on the UI if progress is set to a value less than zer...
  - `set_status_progress(progress)` -> `None` — Set the progress of this shot to the given value. If a positive value is provided the UI will show the progress bar, whi...
  - `should_render()` -> `bool` — Returns whether this should should be rendered

**Properties** (5):
  - `console_variable_overrides`: `None` — [Read-Write] (Optional) Console variable overrides which are applied after cvars set via nodes. Only...
  - `enabled`: `bool` — [Read-Write] Does the user want to render this shot? (bool)
  - `inner_name`: `str` — [Read-Write] The name of the camera cut section that this shot represents. Can be empty. (str)
  - `outer_name`: `str` — [Read-Write] The name of the shot section that contains this shot. Can be empty. (str)
  - `sidecar_cameras`: `None` — [Read-Write] List of cameras to render for this shot. Only used if the setting flag is set in the Ca...

### `unreal.MoviePipelineFCPXMLExporter`
Inherits: `MoviePipelineOutputBase` | Header: `MoviePipelineFCPXMLExporterSetting.h`

Movie Pipeline FCPXMLExporter

**Properties** (2):
  - `data_source`: `FCPXMLExportDataSource` — [Read-Write] Whether to build the FCPXML from sequence data directly (for reimporting) or from actua...
  - `file_name_format_override`: `str` — [Read-Write] File name format string override. If specified it will override the FileNameFormat from...

### `unreal.MoviePipelineGameMode`
Inherits: `GameModeBase` | Header: `MoviePipelineGameMode.h`

Movie Pipeline Game Mode

### `unreal.MoviePipelineGameOverrideSetting`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineGameOverrideSetting.h`

Movie Pipeline Game Override Setting

**Properties** (16):
  - `cinematic_quality_settings`: `bool` — [Read-Write] If true, automatically set the engine to the Cinematic Scalability quality settings dur...
  - `disable_hlo_ds`: `bool` — âdisable_hlo_dsâ was renamed to âdisable_hlodsâ. deprecated
  - `disable_hlods`: `bool` — [Read-Write] Should we disable Hierarchical LODs and instead use their real meshes regardless of dis...
  - `flush_grass_streaming`: `bool` — [Read-Write] Flushing grass streaming (combined with override view distance scale) prevents visible ...
  - `flush_streaming_managers`: `bool` — [Read-Write] Experimental. If true flush the streaming managers (Texture Streaming) each frame. Allo...
  - `game_mode_override`: `Class` — [Read-Write]
deprecated: Please use the SoftGameModeOverride property instead. (type(Class))
  - `override_view_distance_scale`: `bool` — [Read-Write] Should we override the View Distance Scale? Can be used in situations where MaxDrawDist...
  - `override_virtual_texture_feedback_factor`: `bool` — [Read-Write] If true then override the virtual texture feedback resolution factor. Otherwise the val...
  - `shadow_distance_scale`: `int` — [Read-Write] Scalability option to trade shadow distance versus performance for directional lights (...
  - `shadow_radius_threshold`: `float` — [Read-Write] Cull shadow casters if they are too small, value is the minimal screen space bounding s...
  - `soft_game_mode_override`: `Class` — [Read-Write] Optional Game Mode to override the mapâs default game mode with. This can be useful i...
  - `texture_streaming`: `MoviePipelineTextureStreamingMethod` — [Read-Write] Defines which If true, when using texture streaming fully load the required textures ea...
  - `use_high_quality_shadows`: `bool` — [Read-Write] Should we override shadow-related CVars with some high quality preset settings? (bool)
  - `use_lod_zero`: `bool` — [Read-Write] Should we try to use the highest quality LOD for meshes and particle systems regardless...
  - `view_distance_scale`: `int` — [Read-Write] Controls the view distance scale. A primitiveâs MaxDrawDistance is scaled by this val...
  - `virtual_texture_feedback_factor`: `int` — [Read-Write] The virtual texture feedback resolution factor. A lower factor will increase virtual te...

### `unreal.MoviePipelineHighResSetting`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineHighResSetting.h`

Movie Pipeline High Res Setting

**Properties** (7):
  - `allocate_history_per_tile`: `bool` — [Read-Write] * If true, allocate a unique history for each tile. This is needed to make some render ...
  - `burley_sample_count`: `int` — [Read-Write] * How many samples should the Burley Sub Surface Scattering use? (int32)
  - `overlap_ratio`: `float` — [Read-Write] How much should each tile overlap each other (0-0.5). Decreasing the overlap will resul...
  - `override_sub_surface_scattering`: `bool` — [Read-Write] Sub Surface Scattering relies on history which is not available when using tiling. This...
  - `page_to_system_memory`: `bool` — [Read-Write] * If true, persistent GPU data per high resolution tile is mirrored to system memory, a...
  - `texture_sharpness_bias`: `float` — [Read-Write] This bias encourages the engine to use a higher detail texture when it would normally u...
  - `tile_count`: `int` — [Read-Write] How many tiles should the resulting movie render be broken into? A tile should be no la...

### `unreal.MoviePipelineInProcessExecutor`
Inherits: `MoviePipelineLinearExecutorBase` | Header: `MoviePipelineInProcessExecutor.h`

This executor implementation can process an array of movie pipelines and run them inside the currently running process. This is intended for usage outside of the editor (ie. -game mode) as it will tak...

**Properties** (1):
  - `use_current_level`: `bool` — [Read-Write] Use current level instead of opening new level (bool)

### `unreal.MoviePipelineInProcessExecutorSettings`
Inherits: `DeveloperSettings` | Header: `MoviePipelineInProcessExecutorSettings.h`

This is the implementation responsible for executing the rendering of multiple movie pipelines after being launched via the command line.

**Properties** (4):
  - `additional_command_line_arguments`: `str` — [Read-Write] A list of additional command line arguments to be appended to the new process startup. ...
  - `close_editor`: `bool` — [Read-Write] If enabled the editor will close itself when a new process is started. This can be used...
  - `inherited_command_line_arguments`: `str` [Read-Only] — [Read-Only] A list of command line arguments which are inherited from the currently running Editor i...
  - `initial_delay_frame_count`: `int` — [Read-Write] How long should we wait after being initialized to start doing any work? This can be us...

### `unreal.MoviePipelineLibrary`
Inherits: `BlueprintFunctionLibrary` | Header: `MoviePipelineBlueprintLibrary.h`

Movie Pipeline Blueprint Library

**Methods** (34):
  - `duplicate_sequence(outer, sequence)` -> `MovieSceneSequence` [classmethod] — Duplicates the specified sequence using a medium depth copy. Standard duplication will only duplicate the top level Sequ...
  - `find_or_get_default_setting_for_shot(setting_type, primary_config, shot)` -> `MoviePipelineSetting` [classmethod] — Allows access to a setting of provided type for specific shot.
  - `get_backbuffer_resolution(primary_config, pipeline_executor_shot, default_overscan = 0.000000)` -> `IntPoint` [classmethod] — Gets the resolution that will be used by the engine when rendering a frame. Includes overscan and tiling
  - `get_completion_percentage(pipeline)` -> `float` [classmethod] — Gets the completion percent of the Pipeline in 0-1
  - `get_current_aperture(movie_pipeline)` -> `float` [classmethod] — Get Current Aperture
  - `get_current_executor_shot(movie_pipeline)` -> `MoviePipelineExecutorShot` [classmethod] — Get Current Executor Shot
  - `get_current_focal_length(movie_pipeline)` -> `float` [classmethod] — Get Current Focal Length
  - `get_current_focus_distance(movie_pipeline)` -> `float` [classmethod] — Get Current Focus Distance
  - `get_current_segment_name(out_inner_name=Text)` [classmethod] — Get Current Segment Name
  - `get_current_segment_state(movie_pipeline)` -> `MovieRenderShotState` [classmethod] — Get Current Segment State
  - `get_current_segment_work_metrics(movie_pipeline)` -> `MoviePipelineSegmentWorkMetrics` [classmethod] — Get Current Segment Work Metrics
  - `get_current_sequence(movie_pipeline)` -> `LevelSequence` [classmethod] — Get Current Sequence
  - `get_current_shot_frame_number(movie_pipeline)` -> `FrameNumber` [classmethod] — Get Current Shot Frame Number
  - `get_current_shot_timecode(movie_pipeline)` -> `Timecode` [classmethod] — Get Current Shot Timecode
  - `get_current_version_number(movie_pipeline)` -> `int32` [classmethod] — Retrieves the cached version number calculated for the current shot, which depends on where the version token was used i...
  - `get_desired_output_resolution(primary_config)` -> `IntPoint` [classmethod] — Gets the desired output resolution for the movie render queue as specified by the user. Does not include overscan, tilin...
  - `get_effective_output_resolution(primary_config, pipeline_executor_shot, default_overscan = 0.000000)` -> `IntPoint` [classmethod] — In case of Overscan percentage being higher than 0 we render additional pixels. This function returns the resolution wit...
  - `get_estimated_time_remaining(pipeline)` -> `Timespan or None` [classmethod] — Get the estimated amount of time remaining for the current pipeline. Based on looking at the total amount of samples to ...
  - `get_job_author(movie_pipeline)` -> `Text` [classmethod] — Get Job Author
  - `get_job_initialization_time(movie_pipeline)` -> `DateTime` [classmethod] — Get Job Initialization Time
  - `get_job_name(movie_pipeline)` -> `Text` [classmethod] — Get Job Name
  - `get_map_package_name(job)` -> `str` [classmethod] — Get the package name for the map in this job. The level travel command requires the package path and not the asset path.
  - `get_movie_pipeline_engine_changelist_label(movie_pipeline)` -> `Text` [classmethod] — Get a string to represent the Changelist Number for the burn in. This can be driven by a Modular Feature if you want to ...
  - `get_overall_output_frames(out_total_count=int32)` [classmethod] — Get Overall Output Frames
  - `get_overall_segment_counts(out_total_count=int32)` [classmethod] — Get Overall Segment Counts
  - `get_overscan_crop_rectangle(primary_config, pipeline_executor_shot, default_overscan = 0.000000)` -> `IntRect` [classmethod] — Gets a rectangle that will crop out any overscan applied to the scene. If there is no overscan, the rectangle returned w...
  - `get_overscanned_resolution(primary_config, pipeline_executor_shot, default_overscan = 0.000000)` -> `IntPoint` [classmethod] — Gets the overscanned resolution, which is the target output resolution scaled by any configured overscan amount
  - `get_pipeline_state(pipeline)` -> `MovieRenderPipelineState` [classmethod] — Get the current state of the specified Pipeline. See EMovieRenderPipelineState for more detail about each state.
  - `get_root_frame_number(movie_pipeline)` -> `FrameNumber` [classmethod] — Get Root Frame Number
  - `get_root_timecode(movie_pipeline)` -> `Timecode` [classmethod] — Get Root Timecode
  - `load_manifest_file_from_string(manifest_file_path)` -> `MoviePipelineQueue` [classmethod] — Loads the specified manifest file and converts it into an UMoviePipelineQueue. Use in combination with SaveQueueToManife...
  - `resolve_filename_format_arguments(format_string, out_merged_format_args=MoviePipelineFormatArgs)` [classmethod] — Resolves the provided InFormatString by converting {format_strings} into settings provided by the primary config.
  - `resolve_version_number(params, get_next_version = True)` -> `int32` [classmethod] — If version number is manually specified by the Job, returns that. Otherwise search the Output Directory for the highest ...
  - `update_job_shot_list_from_sequence(sequence, job)` -> `bool` [classmethod] — Scan the provided sequence in the job to see which camera cut sections we would try to render and update the jobâs sho...

### `unreal.MoviePipelineLinearExecutorBase`
Inherits: `MoviePipelineExecutorBase` | Header: `MoviePipelineLinearExecutor.h`

This is an abstract base class designed for executing an array of movie pipelines in linear fashion. It is generally the case that you only want to execute one at a time on a local machine and a diffe...

### `unreal.MoviePipelineOutputBase`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineOutputBase.h`

A base class for different output containers for the data generated by the Movie Pipeline, i.e: image sequences, video containers, etc.

### `unreal.MoviePipelineOutputSetting`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineOutputSetting.h`

Movie Pipeline Output Setting

**Properties** (16):
  - `auto_version`: `bool` — [Read-Write] If true, version tokens will automatically be incremented with each local render. If fa...
  - `custom_end_frame`: `int` — [Read-Write] Used when overriding the playback range. In Display Rate frames. If bUseCustomPlaybackR...
  - `custom_start_frame`: `int` — [Read-Write] Used when overriding the playback range. In Display Rate frames. If bUseCustomPlaybackR...
  - `file_name_format`: `str` — [Read-Write] What format string should the final files use? Can include folder prefixes, and format ...
  - `flush_disk_writes_per_shot`: `bool` — [Read-Write] If true, the game thread will stall at the end of each shot to flush the rendering queu...
  - `frame_number_offset`: `int` — [Read-Write] How many frames should we offset the output frame number by? This is useful when using ...
  - `handle_frame_count`: `int` — [Read-Write] Top level shot track sections will automatically be expanded by this many frames in bot...
  - `output_directory`: `DirectoryPath` — [Read-Write] What directory should all of our output files be relative to. (DirectoryPath)
  - `output_frame_rate`: `FrameRate` — [Read-Write] What frame rate should the output files be exported at? This overrides the Display Rate...
  - `output_frame_step`: `int` — Setting this value to 2 renders every other frame. Game Thread is still evaluated on âskippedâ f...
  - `output_resolution`: `IntPoint` — [Read-Write] What resolution should our output files be exported at? (IntPoint)
  - `override_existing_output`: `bool` — [Read-Write] If true, output containers should attempt to override any existing files with the same ...
  - `use_custom_frame_rate`: `bool` — [Read-Write] Should we use the custom frame rate specified by OutputFrameRate? Otherwise defaults to...
  - `use_custom_playback_range`: `bool` — [Read-Write] If true, override the Playback Range start/end bounds with the bounds specified below. ...
  - `version_number`: `int` — [Read-Write] The value to use for the version token if versions are not automatically incremented. (...
  - `zero_pad_frame_numbers`: `int` — [Read-Write] How many digits should all output frame numbers be padded to? MySequence_1.png -> MySeq...

### `unreal.MoviePipelinePrimaryConfig`
Inherits: `MoviePipelineConfigBase` | Header: `MoviePipelinePrimaryConfig.h`

This class describes the main configuration for a Movie Render Pipeline. Only settings that apply to the entire output should be stored here, anything that is changed on a per-shot basis should be sto...

**Methods** (4):
  - `get_all_settings(include_disabled_settings = False, include_transient_settings = False)` -> `Array [ MoviePipelineSetting ]` — Get All Settings
  - `get_effective_frame_rate(sequence)` -> `FrameRate` — Returns the frame rate override from the Primary Configuration (if any) or the Sequence frame rate if no override is spe...
  - `get_transient_settings()` -> `Array [ MoviePipelineSetting ]` — Get Transient Settings
  - `initialize_transient_settings()` -> `None` — Initializes a single instance of every setting so that even non-user-configured settings have a chance to apply their de...

### `unreal.MoviePipelinePythonHostExecutor`
Inherits: `MoviePipelineExecutorBase` | Header: `MoviePipelinePythonHostExecutor.h`

This is a dummy executor that is designed to host a executor implemented in python. Python defined UClasses are not available when the executor is initialized and not all callbacks are available in Py...

**Methods** (3):
  - `execute_delayed(pipeline_queue)` -> `None` — Python/Blueprint API
  - `get_last_loaded_world()` -> `World` — ~Python/Blueprint API
  - `on_map_load(world)` -> `None` — On Map Load

**Properties** (2):
  - `executor_class`: `Class` — [Read-Write] You should override this class type on the CDO of the object with your Python type when...
  - `pipeline_queue`: `MoviePipelineQueue` — [Read-Write] (MoviePipelineQueue)

### `unreal.MoviePipelineQueue`
Inherits: `Object` | Header: `MoviePipelineQueue.h`

A queue is a list of jobs that have been executed, are executing and are waiting to be executed. These can be saved to specific assets to allow

**Methods** (11):
  - `allocate_new_job(job_type = None)` -> `MoviePipelineExecutorJob` — Allocates a new Job in this Queue. The Queue owns the jobs for memory management purposes, and this will handle that for...
  - `copy_from(queue)` -> `MoviePipelineQueue` — Replace the contents of this queue with a copy of the contents from another queue. Returns a pointer to this queue if th...
  - `delete_all_jobs()` -> `None` — Delete all jobs from the Queue.
  - `delete_job(job)` -> `None` — Deletes the specified job from the Queue.
  - `duplicate_job(job)` -> `MoviePipelineExecutorJob` — Duplicate the specific job and return the duplicate. Configurations are duplicated and not shared.
  - `get_jobs()` -> `Array [ MoviePipelineExecutorJob ]` — Get all of the Jobs contained in this Queue.
  - `get_queue_origin()` -> `MoviePipelineQueue` — Gets the queue that this queue was originally based on (if any). The origin will only be set on transient queues; the or...
  - `is_dirty()` -> `bool` — Gets the dirty state of this queue. Note that dirty state is only tracked when the editor is active.
  - `set_is_dirty(new_dirty_state)` -> `None` — Sets the dirty state of this queue. Generally the queue will correctly track the dirty state; however, there are situati...
  - `set_job_index(job, index)` -> `None` — Set the index of the given job
  - `set_queue_origin(config)` -> `None` — Sets the queue that this queue originated from (if any). The origin should only be set for transient queues.

### `unreal.MoviePipelineQueueEngineSubsystem`
Inherits: `EngineSubsystem` | Header: `MoviePipelineQueueEngineSubsystem.h`

This subsystem is intended for use when rendering in a shipping game (but can also be used in PIE during development/testing). See UMoviePipelineQueueSubsystem for the Editor-only queue which is bound...

**Methods** (8):
  - `allocate_job(sequence)` -> `MoviePipelineExecutorJob` — Convenience function for creating a UMoviePipelineExecutorJob out of the given Level Sequence asset. The newly created j...
  - `get_active_executor()` -> `MoviePipelineExecutorBase` — Returns the active executor (if there is one). This can be used to subscribe to events on an already in-progress render....
  - `get_queue()` -> `MoviePipelineQueue` — Returns the queue of Movie Pipelines that need to be rendered.
  - `is_rendering()` -> `bool` — Returns true if there is an active executor working on producing a movie. This could be actively rendering frames, or wo...
  - `render_job(job)` -> `None` — Convenience function for rendering the specified job. Calling this will render the specified job (if it was allocated us...
  - `render_queue_with_executor(executor_type)` -> `MoviePipelineExecutorBase` — Starts processing the current queue with the supplied executor class. This starts an async process which may or may not ...
  - `render_queue_with_executor_instance(executor)` -> `None` — Starts processing the current queue with the supplied executor. This starts an async process which may or may not run in...
  - `set_configuration(progress_widget_class = None, render_player_viewport = False)` -> `None` — Sets some advanced configuration options that are used only occasionally to have better control over integrating it into...

**Properties** (1):
  - `on_render_finished`: `MoviePipelineWorkFinished` — [Read-Write] Assign a function to this delegate to get notified when each individual job is finished...

### `unreal.MoviePipelineRenderPass`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineRenderPass.h`

Movie Pipeline Render Pass

### `unreal.MoviePipelineSetting`
Inherits: `Object` | Header: `MoviePipelineSetting.h`

A base class for all Movie Render Pipeline settings.

**Methods** (3):
  - `build_new_process_command_line_args()` — When rendering in a new process some settings may need to provide command line arguments to affect engine settings that ...
  - `is_enabled()` -> `bool` — Is this setting enabled by the user in the UI?
  - `set_is_enabled(enabled)` -> `None` — Set Is Enabled

### `unreal.MoviePipelineSetting_BlueprintBase`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineSettingBlueprintBase.h`

A base class for all Movie Render Pipeline settings which can be implemented in Blueprints. This features a slightly different API than the regular UMoviePipelineSetting to make the Blueprint integrat...

**Methods** (5):
  - `on_engine_tick_begin_frame()` -> `None` — Tick
  - `receive_get_footer_text(job)` -> `Text` — Receive Get Footer Text
  - `receive_get_format_arguments(out_format_args=MoviePipelineFormatArgs)` — Receive Get Format Arguments
  - `receive_setup_for_pipeline_impl(pipeline)` -> `None` — Receive Setup for Pipeline Impl
  - `receive_teardown_for_pipeline_impl(pipeline)` -> `None` — Receive Teardown for Pipeline Impl

### `unreal.MoviePipelineShotConfig`
Inherits: `MoviePipelineConfigBase` | Header: `MoviePipelineShotConfig.h`

Movie Pipeline Shot Config

### `unreal.MoviePipelineVideoOutputBase`
Inherits: `MoviePipelineOutputBase` | Header: `MoviePipelineVideoOutputBase.h`

A base class for video codec outputs for the Movie Pipeline system. To simplify encoder implementations this handles multi-threading for you and will call all of the encoding functions on a dedicated ...

### `unreal.MoviePipelineViewFamilySetting`
Inherits: `MoviePipelineSetting` | Header: `MoviePipelineViewFamilySetting.h`

Movie Pipeline View Family Setting

### `unreal.MovieRenderDebugWidget`
Inherits: `UserWidget` | Header: `MovieRenderDebugWidget.h`

C++ Base Class for the debug widget that is drawn onto the game viewport (but not burned into the output files) that allow us to easily visualize the current state of the pipeline.

**Methods** (1):
  - `on_initialized_for_pipeline(for_pipeline)` -> `None` — On Initialized for Pipeline

### `unreal.MovieRenderGraphEditorSettings`
Inherits: `DeveloperSettings` | Header: `MovieRenderGraphEditorSettings.h`

Movie Render Graph Editor Settings

### `unreal.FCPXMLExportDataSource`
Inherits: `EnumBase` | Header: `MoviePipelineFCPXMLExporterSetting.h`

FCPXMLExport Data Source

**Properties** (2):
  - `OUTPUT_METADATA`: `FCPXMLExportDataSource = Ellipsis` — 0
  - `SEQUENCE_DATA`: `FCPXMLExportDataSource = Ellipsis` — 1

### `unreal.MovieGraphAspectRatioAdaptBehavior`
Inherits: `EnumBase` | Header: `MovieGraphGlobalOutputSettingNode.h`

EMovie Graph Aspect Ratio Adapt Behavior

**Properties** (4):
  - `AUTOMATIC`: `MovieGraphAspectRatioAdaptBehavior = Ellipsis` — If the camera has Constrain Aspect Ratio enabled, then this should match Disabled, except the letter...
  - `DISABLED`: `MovieGraphAspectRatioAdaptBehavior = Ellipsis` — Output resolution will not be modified. If the camera has Constrain Aspect Ratio enabled, then
lette...
  - `SCALE_TO_HEIGHT`: `MovieGraphAspectRatioAdaptBehavior = Ellipsis` — If the camera has Constrain Aspect Ratio enabled, then the height of the output resolution will be p...
  - `SCALE_TO_WIDTH`: `MovieGraphAspectRatioAdaptBehavior = Ellipsis` — If the camera has Constrain Aspect Ratio enabled, then the width of the output resolution will be pr...

### `unreal.MovieGraphBranchRestriction`
Inherits: `EnumBase` | Header: `MovieGraphNode.h`

Describes a restriction on what kind of branch a node can be created in within the graph.

**Properties** (3):
  - `ANY`: `MovieGraphBranchRestriction = Ellipsis` — 0
  - `GLOBALS`: `MovieGraphBranchRestriction = Ellipsis` — < The node can be created in any type of branch 1
  - `RENDER_LAYER`: `MovieGraphBranchRestriction = Ellipsis` — < The node must be created in the Globals branch 2

### `unreal.MovieGraphConditionGroupOpType`
Inherits: `EnumBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Operation types available on condition groups.

**Properties** (3):
  - `ADD`: `MovieGraphConditionGroupOpType = Ellipsis` — Adds the contents of the condition group to the results from the previous condition group (if any). ...
  - `AND`: `MovieGraphConditionGroupOpType = Ellipsis` — Replaces the results of the previous condition group(s) with only the elements that exist in both th...
  - `SUBTRACT`: `MovieGraphConditionGroupOpType = Ellipsis` — Removes the contents of the condition group from the result of the previous condition group (if any)...

### `unreal.MovieGraphConditionGroupQueryOpType`
Inherits: `EnumBase` | Header: `MovieGraphRenderLayerSubsystem.h`

Operation types available on condition group queries.

**Properties** (3):
  - `ADD`: `MovieGraphConditionGroupQueryOpType = Ellipsis` — Adds the results of the query to the results from the previous query (if any). 0
  - `AND`: `MovieGraphConditionGroupQueryOpType = Ellipsis` — Replaces the results of the previous queries with only the items that exist in both those queries, a...
  - `SUBTRACT`: `MovieGraphConditionGroupQueryOpType = Ellipsis` — Removes the results of the query from the results of the previous query (if any). Any items in this ...

### `unreal.MovieGraphContainerType`
Inherits: `EnumBase` | Header: `MovieGraphCommon.h`

The container type of a graph memberâs value.

**Properties** (2):
  - `ARRAY`: `MovieGraphContainerType = Ellipsis` — 1
  - `SET`: `MovieGraphContainerType = Ellipsis` — 2

### `unreal.MovieGraphFrameRangeNotation`
Inherits: `EnumBase` | Header: `MoviePipelinePostRenderSettings.h`

The symbol that should be used to substitute a frame number within image paths.

**Properties** (4):
  - `DOLLAR_F`: `MovieGraphFrameRangeNotation = Ellipsis` — $F. Includes the rendered frame range before the file paths(s) are given on the command line.
For ex...
  - `HASH`: `MovieGraphFrameRangeNotation = Ellipsis` — Hash symbol. Does not include the rendered frame range on the command line.
For example, âLayer1.#...
  - `HASH_WITH_START_END_FRAME`: `MovieGraphFrameRangeNotation = Ellipsis` — Hash symbol. Includes the rendered frame range after the file path(s) are given on the command line....
  - `START_FRAME`: `MovieGraphFrameRangeNotation = Ellipsis` — Use the path for the first rendered frame only.
For example, âLayer1.100.exrâ, if frame 100 was ...

### `unreal.MovieGraphImageSequencePlaybackRange`
Inherits: `EnumBase` | Header: `MoviePipelinePostRenderSettings.h`

The frame range for image sequences that should be used during playback.

**Properties** (2):
  - `FIRST_FRAME_ONLY`: `MovieGraphImageSequencePlaybackRange = Ellipsis` — Only play back the first frame. 0
  - `FULL_RANGE`: `MovieGraphImageSequencePlaybackRange = Ellipsis` — Play back all available frames. 1

### `unreal.MovieGraphJobPlaybackRange`
Inherits: `EnumBase` | Header: `MoviePipelinePostRenderSettings.h`

Dictates which jobs should be used to get media from for playback if more than one job generated media.

**Properties** (2):
  - `ALL_JOBS`: `MovieGraphJobPlaybackRange = Ellipsis` — Play back media from all of the jobs that were rendered. 1
  - `FIRST_JOB_ONLY`: `MovieGraphJobPlaybackRange = Ellipsis` — Only play back media from the first job that was rendered. 0

### `unreal.MovieGraphOutputTypePlayback`
Inherits: `EnumBase` | Header: `MoviePipelinePostRenderSettings.h`

How media should be selected for playback when more than one type was generated by a render.

**Properties** (2):
  - `PLAY_ALL_OUTPUT_TYPES`: `MovieGraphOutputTypePlayback = Ellipsis` — Play all media types that were generated from the render. Note that the highest-priority media dicta...
  - `USE_PRIORITY_ORDER`: `MovieGraphOutputTypePlayback = Ellipsis` — Use the priority order when determining the media to play (the media with the extension that has the...

### `unreal.MovieGraphPinQueryRequirement`
Inherits: `EnumBase` | Header: `MovieGraphPin.h`

Specifies a restriction on pin properties when searching for a pin on a node.

**Properties** (3):
  - `BUILT_IN`: `MovieGraphPinQueryRequirement = Ellipsis` — The pin must be built-in, meaning that it is always present on the node. 0
  - `BUILT_IN_OR_DYNAMIC`: `MovieGraphPinQueryRequirement = Ellipsis` — The pin can be either built-in or dynamic. 2
  - `DYNAMIC`: `MovieGraphPinQueryRequirement = Ellipsis` — The pin must be dynamic, meaning that it may not always exist on the node. These are typically user-...

### `unreal.MovieGraphPlaybackMethod`
Inherits: `EnumBase` | Header: `MoviePipelinePostRenderSettings.h`

The type of viewer that should be used to play back media.

**Properties** (2):
  - `CUSTOM_VIEWER`: `MovieGraphPlaybackMethod = Ellipsis` — Use a specific application for playing back media. 1
  - `OPERATING_SYSTEM`: `MovieGraphPlaybackMethod = Ellipsis` — Use the operating systemâs default application for playing back media of the type thatâs being v...

### `unreal.MovieGraphQuickRenderButtonMode`
Inherits: `EnumBase` | Header: `MovieGraphQuickRenderSettings.h`

The type of render that the Quick Render toolbar button performs.

**Properties** (2):
  - `NORMAL_MOVIE_RENDER_QUEUE`: `MovieGraphQuickRenderButtonMode = Ellipsis` — Uses the active queue in the Movie Render Queue editor to do a render. 0
  - `QUICK_RENDER`: `MovieGraphQuickRenderButtonMode = Ellipsis` — Use the Quick Render configuration to do a render. 1

### `unreal.MovieGraphQuickRenderFrameRangeType`
Inherits: `EnumBase` | Header: `MovieGraphQuickRenderSettings.h`

The frame range within the active level sequence that a Quick Render should use.

**Properties** (3):
  - `CUSTOM`: `MovieGraphQuickRenderFrameRangeType = Ellipsis` — Use a custom frame range. 2
  - `PLAYBACK_RANGE`: `MovieGraphQuickRenderFrameRangeType = Ellipsis` — Use the frame range specified by the sequenceâs start and end frame. 0
  - `SELECTION_RANGE`: `MovieGraphQuickRenderFrameRangeType = Ellipsis` — Use the sequenceâs selection as the start and end frame. 1

### `unreal.MovieGraphQuickRenderMode`
Inherits: `EnumBase` | Header: `MovieGraphQuickRenderSettings.h`

The available modes for Quick Render to be put into.

**Properties** (4):
  - `CURRENT_SEQUENCE`: `MovieGraphQuickRenderMode = Ellipsis` — Renders the level sequence that is currently active in Sequencer. 0
  - `CURRENT_VIEWPORT`: `MovieGraphQuickRenderMode = Ellipsis` — Renders a single frame using the current viewportâs camera. 1
  - `SELECTED_CAMERAS`: `MovieGraphQuickRenderMode = Ellipsis` — Renders one frame for each camera actor selected in the Outliner. 2
  - `USE_VIEWPORT_CAMERA_IN_SEQUENCE`: `MovieGraphQuickRenderMode = Ellipsis` — Renders the level sequence active in Sequencer, but uses the viewportâs camera instead (for the en...

### `unreal.MovieGraphQuickRenderPostRenderActionType`
Inherits: `EnumBase` | Header: `MovieGraphQuickRenderSettings.h`

The action that quick render should take after finishing the render.

**Properties** (3):
  - `DO_NOTHING`: `MovieGraphQuickRenderPostRenderActionType = Ellipsis` — Do nothing after the render. 0
  - `OPEN_OUTPUT_DIRECTORY`: `MovieGraphQuickRenderPostRenderActionType = Ellipsis` — Open the directory that contains the files generated by Quick Render. 2
  - `PLAY_RENDER_OUTPUT`: `MovieGraphQuickRenderPostRenderActionType = Ellipsis` — Open the media that was generated from Quick Render. The application used for this is defined in Edi...

### `unreal.MovieGraphQuickRenderViewportLookFlags`
Inherits: `EnumBase` | Header: `MovieGraphQuickRenderSettings.h`

The aspects of the viewport that should be applied to the quick render.

**Properties** (5):
  - `EDITOR_ONLY_ACTORS`: `MovieGraphQuickRenderViewportLookFlags = Ellipsis` — 16
  - `OCIO`: `MovieGraphQuickRenderViewportLookFlags = Ellipsis` — 1
  - `SHOW_FLAGS`: `MovieGraphQuickRenderViewportLookFlags = Ellipsis` — 2
  - `VIEW_MODE`: `MovieGraphQuickRenderViewportLookFlags = Ellipsis` — 4
  - `VISIBILITY`: `MovieGraphQuickRenderViewportLookFlags = Ellipsis` — 8

### `unreal.MovieGraphRenderLayerPlaybackRange`
Inherits: `EnumBase` | Header: `MoviePipelinePostRenderSettings.h`

Dictates the selection of render layers that should be used in playback if more than one was generated.

**Properties** (2):
  - `ALL_RENDER_LAYERS`: `MovieGraphRenderLayerPlaybackRange = Ellipsis` — Play back media from all render layers that were rendered. 1
  - `FIRST_RENDER_LAYER_ONLY`: `MovieGraphRenderLayerPlaybackRange = Ellipsis` — Only play back media from the first render layer that was rendered. 0

### `unreal.MovieGraphScalabilityQualityLevel`
Inherits: `EnumBase` | Header: `MovieGraphGlobalGameOverrides.h`

Scalability quality levels available for use in renders generated from the graph.

**Properties** (5):
  - `CINEMATIC`: `MovieGraphScalabilityQualityLevel = Ellipsis` — 4
  - `EPIC`: `MovieGraphScalabilityQualityLevel = Ellipsis` — 3
  - `HIGH`: `MovieGraphScalabilityQualityLevel = Ellipsis` — 2
  - `LOW`: `MovieGraphScalabilityQualityLevel = Ellipsis` — 0
  - `MEDIUM`: `MovieGraphScalabilityQualityLevel = Ellipsis` — 1

### `unreal.MovieGraphSequenceRangeType`
Inherits: `EnumBase` | Header: `MovieGraphGlobalOutputSettingNode.h`

EMovie Graph Sequence Range Type

**Properties** (2):
  - `CUSTOM`: `MovieGraphSequenceRangeType = Ellipsis` — Override the Playback Range value from the Level Sequence and instead override it to use a custom Fr...
  - `SEQUENCE_DEFAULT`: `MovieGraphSequenceRangeType = Ellipsis` — Use the Playback Range value from the Level Sequence (without overriding it.) 0

### `unreal.MovieGraphValueType`
Inherits: `EnumBase` | Header: `MovieGraphCommon.h`

The type of a graph memberâs value.

**Properties** (17):
  - `BOOL`: `MovieGraphValueType = Ellipsis` — 1
  - `BYTE`: `MovieGraphValueType = Ellipsis` — 2
  - `CLASS`: `MovieGraphValueType = Ellipsis` — 14
  - `DOUBLE`: `MovieGraphValueType = Ellipsis` — 6
  - `ENUM`: `MovieGraphValueType = Ellipsis` — 10
  - `FLOAT`: `MovieGraphValueType = Ellipsis` — 5
  - `INT32`: `MovieGraphValueType = Ellipsis` — 3
  - `INT64`: `MovieGraphValueType = Ellipsis` — 4
  - `NAME`: `MovieGraphValueType = Ellipsis` — 7
  - `OBJECT`: `MovieGraphValueType = Ellipsis` — 12
  - `SOFT_CLASS`: `MovieGraphValueType = Ellipsis` — 15
  - `SOFT_OBJECT`: `MovieGraphValueType = Ellipsis` — 13
  - `STRING`: `MovieGraphValueType = Ellipsis` — 8
  - `STRUCT`: `MovieGraphValueType = Ellipsis` — 11
  - `TEXT`: `MovieGraphValueType = Ellipsis` — 9
  - `U_INT32`: `MovieGraphValueType = Ellipsis` — 16
  - `U_INT64`: `MovieGraphValueType = Ellipsis` — Type not fully supported at UI, will work with restrictions to type editing 17

### `unreal.MoviePipelineEncodeQuality`
Inherits: `EnumBase` | Header: `MoviePipelineCommandLineEncoder.h`

EMovie Pipeline Encode Quality

**Properties** (4):
  - `EPIC`: `MoviePipelineEncodeQuality = Ellipsis` — 3
  - `HIGH`: `MoviePipelineEncodeQuality = Ellipsis` — 2
  - `LOW`: `MoviePipelineEncodeQuality = Ellipsis` — 0
  - `MEDIUM`: `MoviePipelineEncodeQuality = Ellipsis` — 1

### `unreal.MoviePipelineShutterTiming`
Inherits: `EnumBase` | Header: `MovieRenderPipelineDataTypes.h`

EMovie Pipeline Shutter Timing

**Properties** (3):
  - `FRAME_CENTER`: `MoviePipelineShutterTiming = Ellipsis` — 1
  - `FRAME_CLOSE`: `MoviePipelineShutterTiming = Ellipsis` — 2
  - `FRAME_OPEN`: `MoviePipelineShutterTiming = Ellipsis` — 0

### `unreal.MoviePipelineTextureStreamingMethod`
Inherits: `EnumBase` | Header: `MoviePipelineGameOverrideSetting.h`

EMovie Pipeline Texture Streaming Method

**Properties** (3):
  - `DISABLED`: `MoviePipelineTextureStreamingMethod = Ellipsis` — Disable the Texture Streaming system. Requires the highest amount of VRAM, but helps if Fully Load U...
  - `FULLY_LOAD`: `MoviePipelineTextureStreamingMethod = Ellipsis` — Fully load used textures instead of progressively streaming them in over multiple frames. Requires l...
  - `NONE`: `MoviePipelineTextureStreamingMethod = Ellipsis` — This will not change the texture streaming method / cvars the users has set. 0

### `unreal.MovieRenderPipelineState`
Inherits: `EnumBase` | Header: `MovieRenderPipelineDataTypes.h`

What is the current overall state of the Pipeline? States are processed in order from first to last and all states will be hit (though there is no guarantee the state will not be transitioned away fro...

**Properties** (5):
  - `EXPORT`: `MovieRenderPipelineState = Ellipsis` — All outputs have finished writing to disk or otherwise processing. Additional exports that may have ...
  - `FINALIZE`: `MovieRenderPipelineState = Ellipsis` — All desired frames have been produced. Audio is already finalized. Outputs have a chance to finish l...
  - `FINISHED`: `MovieRenderPipelineState = Ellipsis` — The pipeline has been shut down. It is an error to shut it down again. 4
  - `PRODUCING_FRAMES`: `MovieRenderPipelineState = Ellipsis` — The pipeline has been initialized and is now controlling time and working on producing frames. 1
  - `UNINITIALIZED`: `MovieRenderPipelineState = Ellipsis` — The pipeline has not been initialized yet. Only valid operation is to call Initialize. 0

### `unreal.MovieRenderShotState`
Inherits: `EnumBase` | Header: `MovieRenderPipelineDataTypes.h`

What is the current state of a shot? States are processed in order from first to last but not all states are required, ie: WarmUp and MotionBlur can be disabled and the shot will never pass through th...

**Properties** (6):
  - `COOLING_DOWN`: `MovieRenderShotState = Ellipsis` — The shot is cooling down. Engine ticks are passing and frames are being rendered, but
not saved to d...
  - `FINISHED`: `MovieRenderShotState = Ellipsis` — The shot has produced all frames it will produce. No more evaluation should be done for this shot on...
  - `MOTION_BLUR`: `MovieRenderShotState = Ellipsis` — The shot is doing additional pre-roll for motion blur. No frames are being produced, but the renderi...
  - `RENDERING`: `MovieRenderShotState = Ellipsis` — The shot is working on producing frames and may be currently doing a sub-frame or a whole frame. 3
  - `UNINITIALIZED`: `MovieRenderShotState = Ellipsis` — The shot has not been initialized yet. 0
  - `WARMING_UP`: `MovieRenderShotState = Ellipsis` — The shot is warming up. Engine ticks are passing but no frames are being produced. 1

### `unreal.MoviePipelineFinished`
Inherits: `MulticastDelegateBase` | Header: `MoviePipeline.h`

Movie Pipeline Finished Delegate Signature

### `unreal.MoviePipelineHttpResponseRecieved`
Inherits: `MulticastDelegateBase` | Header: `MoviePipelineExecutor.h`

Called when the response for an HTTP request comes in.

### `unreal.MoviePipelineSocketMessageRecieved`
Inherits: `MulticastDelegateBase` | Header: `MoviePipelineExecutor.h`

Called when a socket message is recieved. String is UTF8 encoded and has had the length byte stripped from it.

### `unreal.MoviePipelineWorkFinished`
Inherits: `MulticastDelegateBase` | Header: `MoviePipelineBase.h`

Movie Pipeline Work Finished Delegate Signature

### `unreal.OnMoviePipelineExecutorErrored`
Inherits: `MulticastDelegateBase` | Header: `MoviePipelineExecutor.h`

On Movie Pipeline Executor Errored Delegate Signature

### `unreal.OnMoviePipelineExecutorFinished`
Inherits: `MulticastDelegateBase` | Header: `MoviePipelineExecutor.h`

On Movie Pipeline Executor Finished Delegate Signature
