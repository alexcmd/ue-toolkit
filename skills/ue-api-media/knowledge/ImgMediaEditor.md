# UE Python API — ImgMediaEditor Module

**2 types** from the `ImgMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ImgMediaSourceFactory`, `ImgMediaSourceFactoryNew`

---

## Classes

### `unreal.ImgMediaSourceFactory`
Inherits: `Factory` | Header: `ImgMediaSourceFactory.h`

Implements a factory for UImgMediaSource objects.

### `unreal.ImgMediaSourceFactoryNew`
Inherits: `Factory` | Header: `ImgMediaSourceFactoryNew.h`

Implements a factory for UImgMediaSource objects.
