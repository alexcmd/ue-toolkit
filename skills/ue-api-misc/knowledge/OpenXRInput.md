# UE Python API — OpenXRInput Module

**1 types** from the `OpenXRInput` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `OpenXRInputFunctionLibrary`

---

## Classes

### `unreal.OpenXRInputFunctionLibrary`
Inherits: `BlueprintFunctionLibrary` | Header: `OpenXRInputFunctionLibrary.h`

Open XRInput Function Library

**Methods** (2):
  - `begin_xr_session(input_mapping_contexts)` -> `bool` [classmethod] — Begin an XR session with the provided input mapping. This will make the tracking system aware of the actions and binding...
  - `end_xr_session()` -> `None` [classmethod] — End XRSession
