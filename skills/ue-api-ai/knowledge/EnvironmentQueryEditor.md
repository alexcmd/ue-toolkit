# UE Python API — EnvironmentQueryEditor Module

**1 types** from the `EnvironmentQueryEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `EnvironmentQueryFactory`

---

## Classes

### `unreal.EnvironmentQueryFactory`
Inherits: `Factory` | Header: `EnvironmentQueryFactory.h`

Environment Query Factory
