# UE Python API — SoundModImporter Module

**1 types** from the `SoundModImporter` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SoundModImporterFactory`

---

## Classes

### `unreal.SoundModImporterFactory`
Inherits: `Factory` | Header: `SoundModImporterFactory.h`

Imports a sound module file
