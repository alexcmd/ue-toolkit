# UE Python API — ProxyTable Module

**4 types** from the `ProxyTable` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ProxyEntry`, `ProxyStructOutput`, `ProxyAsset`, `ProxyTable`

---

## Classes

### `unreal.ProxyEntry`
Inherits: `StructBase` | Header: `ProxyTable.h`

Proxy Entry

### `unreal.ProxyStructOutput`
Inherits: `StructBase` | Header: `ProxyTable.h`

Proxy Struct Output

### `unreal.ProxyAsset`
Inherits: `Object` | Header: `ProxyAsset.h`

EXPERIMENTAL: An indirect mapping to an asset. The direct mapping is defined in a Proxy Table.

### `unreal.ProxyTable`
Inherits: `Object` | Header: `ProxyTable.h`

EXPERIMENTAL: Table mapping of proxy assets to a specific asset.
