# UE Python API — SVGImporterEditor Module

**2 types** from the `SVGImporterEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SVGFactory`, `SVGReimportFactory`

---

## Classes

### `unreal.SVGFactory`
Inherits: `Factory` | Header: `SVGFactory.h`

### `unreal.SVGReimportFactory`
Inherits: `SVGFactory` | Header: `SVGReimportFactory.h`

SVGReimport Factory
