# UE Python API — VertexDeltaModelEditor Module

**1 types** from the `VertexDeltaModelEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `VertexDeltaTrainingModel`

---

## Classes

### `unreal.VertexDeltaTrainingModel`
Inherits: `MLDeformerGeomCacheTrainingModel` | Header: `VertexDeltaTrainingModel.h`

The training model for the vertex delta model. This class is our link to the Python training.

**Methods** (1):
  - `train()` -> `int32` — Main training function, with implementation in python. You need to implement this method. See the UMLDeformerTrainingMod...
