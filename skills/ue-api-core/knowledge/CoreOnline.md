# UE Python API — CoreOnline Module

**2 types** from the `CoreOnline` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `UniqueNetIdWrapper`, `CoreOnlineDummy`

---

## Classes

### `unreal.UniqueNetIdWrapper`
Inherits: `StructBase` | Header: `CoreOnline.h`

UE::Online

### `unreal.CoreOnlineDummy`
Inherits: `EnumBase` | Header: `CoreOnlinePrivate.h`

This exists purely to guarantee a package is created, or the engine will not boot.

**Properties** (1):
  - `DUMMY`: `CoreOnlineDummy = Ellipsis` — 0
