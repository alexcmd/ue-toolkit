# UE Python API — SoundscapeEditor Module

**2 types** from the `SoundscapeEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SoundscapeColorFactory`, `SoundscapePaletteFactory`

---

## Classes

### `unreal.SoundscapeColorFactory`
Inherits: `Factory` | Header: `SoundscapeColorFactory.h`

Soundscape Color Factory

### `unreal.SoundscapePaletteFactory`
Inherits: `Factory` | Header: `SoundScapePaletteFactory.h`

Soundscape Palette Factory
