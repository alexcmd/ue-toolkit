# UE Python API — BehaviorTreeEditor Module

**2 types** from the `BehaviorTreeEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `BehaviorTreeFactory`, `BlackboardDataFactory`

---

## Classes

### `unreal.BehaviorTreeFactory`
Inherits: `Factory` | Header: `BehaviorTreeFactory.h`

Behavior Tree Factory

### `unreal.BlackboardDataFactory`
Inherits: `Factory` | Header: `BlackboardDataFactory.h`

Blackboard Data Factory
