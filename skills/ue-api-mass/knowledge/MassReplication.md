# UE Python API — MassReplication Module

**3 types** from the `MassReplication` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MassReplicationParameters`, `MassClientBubbleInfoBase`, `MassReplicationTrait`

---

## Classes

### `unreal.MassReplicationParameters`
Inherits: `MassConstSharedFragment` | Header: `MassReplicationFragments.h`

Mass Replication Parameters

### `unreal.MassClientBubbleInfoBase`
Inherits: `Info` | Header: `MassClientBubbleInfoBase.h`

The info actor base class that provides the actual replication

### `unreal.MassReplicationTrait`
Inherits: `MassEntityTraitBase` | Header: `MassReplicationTrait.h`

Mass Replication Trait
