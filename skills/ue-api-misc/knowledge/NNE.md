# UE Python API — NNE Module

**1 types** from the `NNE` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `NNEModelData`

---

## Classes

### `unreal.NNEModelData`
Inherits: `Object` | Header: `NNEModelData.h`

This class represents assets that store neural network model data.
