# UE Python API — GeometryCacheUSD Module

**2 types** from the `GeometryCacheUSD` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `GeometryCacheTrackUsd`, `GeometryCacheUsdComponent`

---

## Classes

### `unreal.GeometryCacheTrackUsd`
Inherits: `GeometryCacheTrack` | Header: `GeometryCacheTrackUSD.h`

GeometryCacheTrack for querying USD

### `unreal.GeometryCacheUsdComponent`
Inherits: `GeometryCacheComponent` | Header: `GeometryCacheUSDComponent.h`

GeometryCacheUSDComponent, encapsulates a transient GeometryCache asset instance that fetches its data from a USD file and implements functionality for rendering and playback
