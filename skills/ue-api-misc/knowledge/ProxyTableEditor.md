# UE Python API — ProxyTableEditor Module

**2 types** from the `ProxyTableEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ProxyAssetFactory`, `ProxyTableFactory`

---

## Classes

### `unreal.ProxyAssetFactory`
Inherits: `Factory` | Header: `ProxyAssetFactory.h`

Proxy Asset Factory

### `unreal.ProxyTableFactory`
Inherits: `Factory` | Header: `ProxyTableFactory.h`

Proxy Table Factory
