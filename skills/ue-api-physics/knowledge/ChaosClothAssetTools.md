# UE Python API — ChaosClothAssetTools Module

**1 types** from the `ChaosClothAssetTools` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ChaosClothAssetFactory`

---

## Classes

### `unreal.ChaosClothAssetFactory`
Inherits: `Factory` | Header: `ClothAssetFactory.h`

Having a cloth factory allows the cloth asset to be created from the Editorâs menus.
