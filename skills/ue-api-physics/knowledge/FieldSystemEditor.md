# UE Python API — FieldSystemEditor Module

**1 types** from the `FieldSystemEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `FieldSystemFactory`

---

## Classes

### `unreal.FieldSystemFactory`
Inherits: `Factory` | Header: `FieldSystemFactory.h`

Factory for Simple Cube
