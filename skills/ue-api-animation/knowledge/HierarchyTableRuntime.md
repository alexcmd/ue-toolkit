# UE Python API — HierarchyTableRuntime Module

**1 types** from the `HierarchyTableRuntime` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `HierarchyTable`

---

## Classes

### `unreal.HierarchyTable`
Inherits: `Object` | Header: `HierarchyTable.h`

A general-purpose container asset for storing typed hierarchical data.
