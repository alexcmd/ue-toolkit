# UE Python API — HarmonixMetasoundEditor Module

**1 types** from the `HarmonixMetasoundEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MidiStepSequenceFactory`

---

## Classes

### `unreal.MidiStepSequenceFactory`
Inherits: `Factory` | Header: `MidiStepSequenceFactory.h`

Midi Step Sequence Factory
