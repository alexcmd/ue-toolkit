# UE Python API — AudioCaptureTimecodeProvider Module

**1 types** from the `AudioCaptureTimecodeProvider` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AudioCaptureTimecodeProvider`

---

## Classes

### `unreal.AudioCaptureTimecodeProvider`
Inherits: `GenlockedTimecodeProvider` | Header: `AudioCaptureTimecodeProvider.h`

Read the LTC from the audio capture device.
