# UE Python API — MassNavigationEditor Module

**2 types** from the `MassNavigationEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MassNavigationTestingActor`, `MassNavigationTestingComponent`

---

## Classes

### `unreal.MassNavigationTestingActor`
Inherits: `Actor` | Header: `MassNavigationTestingActor.h`

Debug actor to visually test zone graph.

**Methods** (2):
  - `clear_pinned_lane()` -> `None` — Clear Pinned Lane
  - `pin_lane()` -> `None` — Pin Lane

### `unreal.MassNavigationTestingComponent`
Inherits: `DebugDrawComponent` | Header: `MassNavigationTestingActor.h`

Component for testing MassMovement functionality.
