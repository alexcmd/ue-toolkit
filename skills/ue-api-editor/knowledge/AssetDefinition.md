# UE Python API — AssetDefinition Module

**1 types** from the `AssetDefinition` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `RevisionInfo`

---

## Classes

### `unreal.RevisionInfo`
Inherits: `StructBase` | Header: `AssetDefinition.h`

Revision information for a single revision of a file in source control

**Properties** (3):
  - `changelist`: `int` — [Read-Write] (int32)
  - `date`: `DateTime` — [Read-Write] (DateTime)
  - `revision`: `str` — [Read-Write] (str)
