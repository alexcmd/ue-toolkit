# UE Python API — ParametricSurface Module

**1 types** from the `ParametricSurface` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ParametricSceneParameters`

---

## Classes

### `unreal.ParametricSceneParameters`
Inherits: `StructBase` | Header: `DatasmithParametricSurfaceData.h`

Parametric Scene Parameters
