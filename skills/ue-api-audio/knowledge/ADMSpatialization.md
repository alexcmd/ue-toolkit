# UE Python API — ADMSpatialization Module

**2 types** from the `ADMSpatialization` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ADMEngineSubsystem`, `ADMSpatializationSourceSettings`

---

## Classes

### `unreal.ADMEngineSubsystem`
Inherits: `AudioEngineSubsystem` | Header: `ADMSpatialization.h`

ADMEngine Subsystem

**Methods** (1):
  - `set_send_ip_address(ip_address, port)` -> `None` — Set the IP Address to communicate ADM updates over OSC to

### `unreal.ADMSpatializationSourceSettings`
Inherits: `SpatializationPluginSourceSettingsBase` | Header: `ADMSpatialization.h`

ADMSpatialization Source Settings
