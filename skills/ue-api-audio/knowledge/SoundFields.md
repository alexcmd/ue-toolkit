# UE Python API — SoundFields Module

**1 types** from the `SoundFields` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AmbisonicsEncodingSettings`

---

## Classes

### `unreal.AmbisonicsEncodingSettings`
Inherits: `SoundfieldEncodingSettingsBase` | Header: `SoundFields.h`

Ambisonics Encoding Settings
