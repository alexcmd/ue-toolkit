# UE Python API — PaperTiledImporter Module

**1 types** from the `PaperTiledImporter` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `PaperTiledImporterFactory`

---

## Classes

### `unreal.PaperTiledImporterFactory`
Inherits: `Factory` | Header: `PaperTiledImporterFactory.h`

Imports a tile map (and associated textures & tile sets) exported from Tiled ( http:www.mapeditor.org/ )
