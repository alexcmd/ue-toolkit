# UE Python API — ReplayTracksEditor Module

**1 types** from the `ReplayTracksEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `ReplayTracksCameraModifier`

---

## Classes

### `unreal.ReplayTracksCameraModifier`
Inherits: `CameraModifier` | Header: `ReplayTracksEditorModule.h`

Camera modifier that lets us set the correct FOV and post-process settings on the spectator pawn when we want to lock into a camera.
