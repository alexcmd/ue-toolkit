# UE Python API — WaveTableEditor Module

**1 types** from the `WaveTableEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `WaveTableBankFactory`

---

## Classes

### `unreal.WaveTableBankFactory`
Inherits: `Factory` | Header: `WaveTableBankFactory.h`

Wave Table Bank Factory
