# UE Python API — TemplateSequenceEditor Module

**2 types** from the `TemplateSequenceEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `CameraAnimationSequenceFactoryNew`, `TemplateSequenceFactoryNew`

---

## Classes

### `unreal.CameraAnimationSequenceFactoryNew`
Inherits: `Factory` | Header: `CameraAnimationSequenceFactoryNew.h`

Implements a factory for UCameraAnimationSequence objects.

### `unreal.TemplateSequenceFactoryNew`
Inherits: `Factory` | Header: `TemplateSequenceFactoryNew.h`

Implements a factory for UTemplateSequence objects.
