# UE Python API — SkeletalMeshEditor Module

**2 types** from the `SkeletalMeshEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SkeletalMeshEditorSubsystem`, `SkelSubSysQueryCurvesMetatdataNamesFilter`

---

## Classes

### `unreal.SkeletalMeshEditorSubsystem`
Inherits: `EditorSubsystem` | Header: `SkeletalMeshEditorSubsystem.h`

USkeletalMeshEditorSubsystem Subsystem for exposing skeletal mesh functionality to scripts

**Methods** (32):
  - `assign_physics_asset(target_mesh, physics_asset)` -> `bool` [classmethod] — Assigns a PhysicsAsset to the given SkeletalMesh if it is compatible. Passing nullptr / None as the physics asset will a...
  - `create_physics_asset(skeletal_mesh)` -> `PhysicsAsset` [classmethod] — This function creates a PhysicsAsset for the given SkeletalMesh with the same settings as if it were created through FBX...
  - `get_lod_build_settings(skeletal_mesh, lod_index)` -> `SkeletalMeshBuildSettings` [classmethod] — Copy the build options with the specified LOD build settings.
  - `get_lod_count(skeletal_mesh)` -> `int32` [classmethod] — Retrieve the number of LOD contain in the specified skeletal mesh.
  - `get_lod_material_slot(skeletal_mesh, lod_index, section_index)` -> `int32` — Gets the material slot used for a specific LOD section.
  - `get_material_slot_overlay_material(skeletal_mesh, slot_index)` -> `MaterialInterface` — Get the overlay material for a material slot in a Skeletal Mesh.
  - `get_num_sections(skeletal_mesh, lod_index)` -> `int32` — Get number of sections for a LOD of a Skeletal Mesh
  - `get_num_verts(skeletal_mesh, lod_index)` -> `int32` — Get number of mesh vertices for an LOD of a Skeletal Mesh
  - `get_section_cast_shadow(skeletal_mesh, lod_index, section_index)` -> `bool or None` — Get bCastShadow from a section of a LOD of a Skeletal Mesh
  - `get_section_recompute_tangent(skeletal_mesh, lod_index, section_index)` -> `bool or None` — Get bRecomputeTangent from a section of a LOD of a Skeletal Mesh
  - `get_section_recompute_tangents_vertex_mask_channel(skeletal_mesh, lod_index, section_index)` -> `uint8 or None` — Get RecomputeTangentsVertexMaskChannel from a section of a LOD of a Skeletal Mesh
  - `get_section_visible_in_ray_tracing(skeletal_mesh, lod_index, section_index)` -> `bool or None` — Get bVisibleInRayTracing from a section of a LOD of a Skeletal Mesh
  - `get_skeletal_mesh_overlay_material(skeletal_mesh)` -> `MaterialInterface` — Get the overlay material for a Skeletal Mesh.
  - `get_skeleton_curve_meta_data_names(target_skeleton, filter)` -> `Array [ Name ]` [classmethod] — Retrieve all curve metadata name from the skeleton.
  - `import_lod(base_mesh, lod_index, source_filename)` -> `int32` [classmethod] — Import or re-import a LOD into the specified base mesh. If the LOD do not exist it will import it and add it to the base...
  - `is_physics_asset_compatible(target_mesh, physics_asset)` -> `bool` [classmethod] — Checks whether a physics asset is compatible with the given SkeletalMesh
  - `regenerate_lod(skeletal_mesh, new_lod_count = 0, regenerate_even_if_imported = False, generate_base_lod = False)` -> `bool` [classmethod] — Regenerate LODs of the mesh
  - `reimport_all_custom_lo_ds(skeletal_mesh: SkeletalMesh)` -> `bool` [classmethod] — deprecated: âreimport_all_custom_lo_dsâ was renamed to âreimport_all_custom_lodsâ.
  - `reimport_all_custom_lods(skeletal_mesh)` -> `bool` [classmethod] — Re-import the specified skeletal mesh and all the custom LODs.
  - `remove_lo_ds(base_mesh: SkeletalMesh, to_remove_lo_ds: None)` -> `bool` [classmethod] — deprecated: âremove_lo_dsâ was renamed to âremove_lodsâ.
  - `remove_lods(base_mesh, to_remove_lo_ds)` -> `bool` [classmethod] — Remove all the specified LODs. This function will remove all the valid LODs in the list. Valid LOD is any LOD greater th...
  - `rename_socket(skeletal_mesh, old_name, new_name)` -> `bool` [classmethod] — Rename a socket within a skeleton
  - `set_lod_build_settings(skeletal_mesh, lod_index, build_options)` -> `None` [classmethod] — Set the LOD build options for the specified LOD index.
  - `set_material_slot_overlay_material(skeletal_mesh, slot_index, new_section_overlay_material)` -> `bool` — Set the overlay material for a material slot in a Skeletal Mesh.
  - `set_morph_targets_to_generated_by_engine(target_mesh, optional_names)` -> `bool` [classmethod] — Set morph targets has generated by engine.
  - `set_morph_targets_to_generated_by_engine_for_all_skeletal_mesh(optional_names, optional_paths)` -> `None` [classmethod] — Set morph targets has generated by engine for all skeletalmesh in the content.
  - `set_section_cast_shadow(skeletal_mesh, lod_index, section_index, cast_shadow)` -> `bool` — Set bCastShadow for a section of a LOD of a Skeletal Mesh.
  - `set_section_recompute_tangent(skeletal_mesh, lod_index, section_index, recompute_tangent)` -> `bool` — Set bRecomputeTangent for a section of a LOD of a Skeletal Mesh.
  - `set_section_recompute_tangents_vertex_mask_channel(skeletal_mesh, lod_index, section_index, recompute_tangents_vertex_mask_channel)` -> `bool` — Set RecomputeTangentsVertexMaskChannel for a section of a LOD of a Skeletal Mesh.
  - `set_section_visible_in_ray_tracing(skeletal_mesh, lod_index, section_index, visible_in_ray_tracing)` -> `bool` — Set bVisibleInRayTracing for a section of a LOD of a Skeletal Mesh.
  - `set_skeletal_mesh_overlay_material(skeletal_mesh, new_overlay_material)` -> `bool` — Set the overlay material for a Skeletal Mesh.
  - `strip_lod_geometry(skeletal_mesh, lod_index, texture_mask, threshold)` -> `bool` [classmethod] — This function will strip all triangle in the specified LOD that donât have any UV area pointing on a black pixel in th...

### `unreal.SkelSubSysQueryCurvesMetatdataNamesFilter`
Inherits: `EnumBase` | Header: `SkeletalMeshEditorSubsystem.h`

Blueprint enum to query skeleton curves

**Properties** (3):
  - `ALL`: `SkelSubSysQueryCurvesMetatdataNamesFilter = Ellipsis` — Query all metadata curves 0
  - `MATERIAL_ONLY`: `SkelSubSysQueryCurvesMetatdataNamesFilter = Ellipsis` — Query only material metadata curves 2
  - `MORPH_TARGET_ONLY`: `SkelSubSysQueryCurvesMetatdataNamesFilter = Ellipsis` — Query only morph target metadata curves 1
