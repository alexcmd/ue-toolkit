# UE Python API — AvfMediaEditor Module

**1 types** from the `AvfMediaEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AvfFileMediaSourceFactory`

---

## Classes

### `unreal.AvfFileMediaSourceFactory`
Inherits: `Factory` | Header: `AvfFileMediaSourceFactory.h`

Implements a factory for UFileMediaSource objects.
