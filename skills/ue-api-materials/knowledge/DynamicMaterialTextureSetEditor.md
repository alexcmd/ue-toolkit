# UE Python API — DynamicMaterialTextureSetEditor Module

**2 types** from the `DynamicMaterialTextureSetEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `DMTextureSetBlueprintFunctionLibrary`, `DMTextureSetFactory`

---

## Classes

### `unreal.DMTextureSetBlueprintFunctionLibrary`
Inherits: `BlueprintFunctionLibrary` | Header: `DMTextureSetBlueprintFunctionLibrary.h`

Material Designer Texture Set Blueprint Function Library

**Methods** (1):
  - `create_texture_set_from_assets(assets)` -> `DMTextureSet` [classmethod] — Uses the filters from the Texture Set Settings to create a Texture Set based on the given assets.

### `unreal.DMTextureSetFactory`
Inherits: `Factory` | Header: `DMTextureSetFactory.h`

DMTexture Set Factory
