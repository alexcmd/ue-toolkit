# UE Python API — Qos Module

**2 types** from the `Qos` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `QosBeaconClient`, `QosBeaconHost`

---

## Classes

### `unreal.QosBeaconClient`
Inherits: `OnlineBeaconClient` | Header: `QosBeaconClient.h`

A beacon client used for quality timings to a specified session

### `unreal.QosBeaconHost`
Inherits: `OnlineBeaconHostObject` | Header: `QosBeaconHost.h`

A beacon host listening for Qos requests from a potential client
