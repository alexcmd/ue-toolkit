# UE Python API — MassSpawner Module

**13 types** from the `MassSpawner` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MassEntityConfig`, `MassSpawnDataGenerator`, `MassSpawnedEntityType`, `MassAssortedFragmentsTrait`, `MassEntityConfigAsset`, `MassEntityEQSSpawnPointsGenerator`, `MassEntitySpawnDataGeneratorBase`, `MassEntityTraitBase`, `MassEntityZoneGraphSpawnPointsGenerator`, `MassSpawner`, `MassTranslationDirection`, `MassSpawnerOnDespawningFinishedEvent`, `MassSpawnerOnSpawningFinishedEvent`

---

## Classes

### `unreal.MassEntityConfig`
Inherits: `StructBase` | Header: `MassEntityConfigAsset.h`

Describes a Mass agent to spawn. The struct can be embedded to allow last minute changes to the agent (i.e. for debugging). The agent config describes a unique list of features which are used to creat...

### `unreal.MassSpawnDataGenerator`
Inherits: `StructBase` | Header: `MassSpawnerTypes.h`

Mass Spawn Data Generator

**Properties** (2):
  - `generator_instance`: `MassEntitySpawnDataGeneratorBase` — [Read-Write] (MassEntitySpawnDataGeneratorBase)
  - `proportion`: `float` — [Read-Write] Proportion of the spawn points that should be generated by this generator, (the proport...

### `unreal.MassSpawnedEntityType`
Inherits: `StructBase` | Header: `MassSpawnerTypes.h`

Describes an entity type to spawn.

**Properties** (2):
  - `entity_config`: `MassEntityConfigAsset` — [Read-Write] Asset that describes the entity (MassEntityConfigAsset)
  - `proportion`: `float` — [Read-Write] Proportion of the count that should be this agent type, (the proportions will be normal...

### `unreal.MassAssortedFragmentsTrait`
Inherits: `MassEntityTraitBase` | Header: `MassAssortedFragmentsTrait.h`

Mass Agent Feature which appends a list of specified fragments.

### `unreal.MassEntityConfigAsset`
Inherits: `DataAsset` | Header: `MassEntityConfigAsset.h`

Agent Config asset allows to create shared configs that can be used as base for derived configs. The asset can be used as is i.e. on a spawner, or you can use FMassEntityConfig to allow last minute ch...

### `unreal.MassEntityEQSSpawnPointsGenerator`
Inherits: `MassEntitySpawnDataGeneratorBase` | Header: `MassEntityEQSSpawnPointsGenerator.h`

Describes the SpawnPoints Generator when we want to leverage the points given by an EQS Query

### `unreal.MassEntitySpawnDataGeneratorBase`
Inherits: `Object` | Header: `MassEntitySpawnDataGeneratorBase.h`

Base class for Mass Entity Spawn Points Generator. A Mass Spawn Points Generator can be of several type (EQS, ZoneGraph, Volume, Area, etc.) The concept is to override the GenerateSpawnPoints() method...

### `unreal.MassEntityTraitBase`
Inherits: `Object` | Header: `MassEntityTraitBase.h`

Base class for Mass Entity Traits. An entity trait is a set of fragments that create a logical trait tha makes sense to end use (i.e. replication, visualization). The template building method allows t...

### `unreal.MassEntityZoneGraphSpawnPointsGenerator`
Inherits: `MassEntitySpawnDataGeneratorBase` | Header: `MassEntityZoneGraphSpawnPointsGenerator.h`

Describes the SpawnPoints Generator when we want to spawn directly on Zone Graph

### `unreal.MassSpawner`
Inherits: `Actor` | Header: `MassSpawner.h`

A spawner you can put on a map and configure it to spawn different things

**Methods** (9):
  - `clear_templates()` -> `None` — Clear Templates
  - `debug_clear()` -> `None` — Remove all the entities
  - `debug_spawn()` -> `None` — DEBUG Spawn
  - `do_despawning()` -> `None` — Despawn all mass agent that was spawned by this spawner
  - `do_spawning()` -> `None` — Starts the spawning of all the agent types of this spawner
  - `get_count()` -> `int32` — Get Count
  - `get_spawning_count_scale()` -> `float` — Get Spawning Count Scale
  - `scale_spawning_count(scale)` -> `None` — Scales the spawning counts
  - `unload_config()` -> `None` — Unload Config

**Properties** (3):
  - `entity_types`: `None` — [Read-Write] Array of entity types to spawn. These define which entities to spawn. (Array[MassSpawne...
  - `on_despawning_finished_event`: `MassSpawnerOnDespawningFinishedEvent` — [Read-Write] Called once DoDespawning completes and all mass agents spawned by this spawner have bee...
  - `on_spawning_finished_event`: `MassSpawnerOnSpawningFinishedEvent` — [Read-Write] Called once DoSpawning completes and all entities have been spawned. (MassSpawnerOnSpaw...

### `unreal.MassTranslationDirection`
Inherits: `EnumBase` | Header: `MassTranslator.h`

EMass Translation Direction

**Properties** (5):
  - `ACTOR_TO_MASS`: `MassTranslationDirection = Ellipsis` — 2
  - `BOTH_WAYS`: `MassTranslationDirection = Ellipsis` — 6
  - `INITIALIZATION_ONLY`: `MassTranslationDirection = Ellipsis` — 1
  - `MASS_TO_ACTOR`: `MassTranslationDirection = Ellipsis` — 4
  - `NONE`: `MassTranslationDirection = Ellipsis` — 0

### `unreal.MassSpawnerOnDespawningFinishedEvent`
Inherits: `MulticastDelegateBase` | Header: `MassSpawner.h`

Mass Spawner on Despawning Finished Event Delegate Signature

### `unreal.MassSpawnerOnSpawningFinishedEvent`
Inherits: `MulticastDelegateBase` | Header: `MassSpawner.h`

Mass Spawner on Spawning Finished Event Delegate Signature
