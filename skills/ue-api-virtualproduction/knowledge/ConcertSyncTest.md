# UE Python API — ConcertSyncTest Module

**2 types** from the `ConcertSyncTest` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `TestReflectionObject`, `TestReflectionDelegate`

---

## Classes

### `unreal.TestReflectionObject`
Inherits: `Object` | Header: `TestReflectionObject.h`

Test Reflection Object

**Properties** (1):
  - `delegate_test`: `TestReflectionDelegate` — [Read-Write] These properties are invalid to replicate (TestReflectionDelegate)

### `unreal.TestReflectionDelegate`
Inherits: `MulticastDelegateBase` | Header: `TestReflectionObject.h`

Test Reflection Delegate Delegate Signature
