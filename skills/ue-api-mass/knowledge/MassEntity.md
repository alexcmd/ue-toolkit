# UE Python API — MassEntity Module

**4 types** from the `MassEntity` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MassConstSharedFragment`, `MassProcessingContext_DEPRECATED`, `MassSharedFragment`, `MassTickableSubsystemBase`

---

## Classes

### `unreal.MassConstSharedFragment`
Inherits: `StructBase` | Header: `MassEntityElementTypes.h`

Mass Const Shared Fragment

### `unreal.MassProcessingContext_DEPRECATED`
Inherits: `StructBase` | Header: `MassProcessingTypes.h`

Mass Processing Context DEPRECATED

### `unreal.MassSharedFragment`
Inherits: `StructBase` | Header: `MassEntityElementTypes.h`

Mass Shared Fragment

### `unreal.MassTickableSubsystemBase`
Inherits: `TickableWorldSubsystem` | Header: `MassSubsystemBase.h`

The sole responsibility of this tickable world subsystem class is to serve functionality common to all Mass-related UTickableWorldSubsystem-based subsystems, like whether the subsystems should get cre...
