# UE Python API — VirtualCameraEditor Module

**1 types** from the `VirtualCameraEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `MovieSceneSpawnableVCamBinding`

---

## Classes

### `unreal.MovieSceneSpawnableVCamBinding`
Inherits: `MovieSceneSpawnableActorBinding` | Header: `MovieSceneSpawnableVCamBinding.h`

Allows the AVCamBaseActor to be recorded as ACineCameraActor instead.
