# UE Python API — EditorScriptableToolsFramework Module

**5 types** from the `EditorScriptableToolsFramework` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `EditorScriptableClickDragTool`, `EditorScriptableInteractiveTool`, `EditorScriptableInteractiveToolPropertySet`, `EditorScriptableModularBehaviorTool`, `EditorScriptableSingleClickTool`

---

## Classes

### `unreal.EditorScriptableClickDragTool`
Inherits: `ScriptableClickDragTool` | Header: `EditorScriptableClickDragTool.h`

Editor-Only variant of UScriptableClickDragTool, which gives access to Editor-Only BP functions

### `unreal.EditorScriptableInteractiveTool`
Inherits: `ScriptableInteractiveTool` | Header: `EditorScriptableInteractiveTool.h`

Editor-Only variant of UScriptableInteractiveTool, which gives access to Editor-Only BP functions

### `unreal.EditorScriptableInteractiveToolPropertySet`
Inherits: `ScriptableInteractiveToolPropertySet` | Header: `EditorScriptableInteractiveTool.h`

Editor Scriptable Interactive Tool Property Set

### `unreal.EditorScriptableModularBehaviorTool`
Inherits: `ScriptableModularBehaviorTool` | Header: `EditorScriptableModularBehaviorTool.h`

Editor-Only variant of UScriptableModularBehaviorTool, which gives access to Editor-Only BP functions

### `unreal.EditorScriptableSingleClickTool`
Inherits: `ScriptableSingleClickTool` | Header: `EditorScriptableSingleClickTool.h`

Editor-Only variant of UScriptableSingleClickTool, which gives access to Editor-Only BP functions
