# UE Python API — LogVisualizer Module

**1 types** from the `LogVisualizer` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `VisualLoggerRenderingActorBase`

---

## Classes

### `unreal.VisualLoggerRenderingActorBase`
Inherits: `Actor` | Header: `VisualLoggerRenderingActorBase.h`

Transient actor used to draw visual logger data on level Base class shared between RewindDebugger and Visual Logger implementations
