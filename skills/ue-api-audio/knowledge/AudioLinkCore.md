# UE Python API — AudioLinkCore Module

**1 types** from the `AudioLinkCore` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `AudioLinkSettingsAbstract`

---

## Classes

### `unreal.AudioLinkSettingsAbstract`
Inherits: `Object` | Header: `AudioLinkSettingsAbstract.h`

This opaque class should be used for specifying settings for how audio should be send to an external audio link.
