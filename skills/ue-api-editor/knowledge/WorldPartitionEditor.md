# UE Python API — WorldPartitionEditor Module

**1 types** from the `WorldPartitionEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `HLODLayerFactory`

---

## Classes

### `unreal.HLODLayerFactory`
Inherits: `Factory` | Header: `HLODLayerFactory.h`

HLODLayer Factory
