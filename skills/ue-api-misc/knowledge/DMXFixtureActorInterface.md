# UE Python API — DMXFixtureActorInterface Module

**1 types** from the `DMXFixtureActorInterface` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `DMXMVRFixtureActorInterface`

---

## Classes

### `unreal.DMXMVRFixtureActorInterface`
Inherits: `Interface` | Header: `DMXMVRFixtureActorInterface.h`

DMXMVRFixture Actor Interface

**Methods** (1):
  - `on_mvr_get_supported_dmx_attributes()` — Should return the DMX Attributes the Actor supports
