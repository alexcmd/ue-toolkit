# UE Python API — PBIK Module

**8 types** from the `PBIK` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `PBIKBoneSetting`, `PBIKDebug`, `PBIKEffector`, `PBIKSolverSettings`, `RigUnit_PBIK`, `RootPrePullSettings`, `PBIKLimitType`, `PBIKRootBehavior`

---

## Classes

### `unreal.PBIKBoneSetting`
Inherits: `StructBase` | Header: `PBIK_Shared.h`

PBIKBone Setting

**Properties** (14):
  - `bone`: `Name` — [Read-Write] The Bone that these settings will be applied to. (Name)
  - `max_x`: `float` — [Read-Write] Range is 0 to 180 (Default is 0). Degrees of rotation in the positive X direction to al...
  - `max_y`: `float` — [Read-Write] Range is 0 to 180 (Default is 0). Degrees of rotation in the positive Y direction to al...
  - `max_z`: `float` — [Read-Write] Range is 0 to 180 (Default is 0). Degrees of rotation in the positive Z direction to al...
  - `min_x`: `float` — [Read-Write] Range is -180 to 0 (Default is 0). Degrees of rotation in the negative X direction to a...
  - `min_y`: `float` — [Read-Write] Range is -180 to 0 (Default is 0). Degrees of rotation in the negative Y direction to a...
  - `min_z`: `float` — [Read-Write] Range is -180 to 0 (Default is 0). Degrees of rotation in the negative Z direction to a...
  - `position_stiffness`: `float` — [Read-Write] Range is 0 to 1 (Default is 0). At higher values, the bone will resist translational mo...
  - `preferred_angles`: `Vector` — [Read-Write] The local Euler angles (in degrees) used to rotate this bone when the chain it belongs ...
  - `rotation_stiffness`: `float` — [Read-Write] Range is 0 to 1 (Default is 0). At higher values, the bone will resist rotating (forcin...
  - `use_preferred_angles`: `bool` — [Read-Write] When true, this bone will âpreferâ to rotate in the direction specified by the Pref...
  - `x`: `PBIKLimitType` — [Read-Write] Limit the rotation angle of the bone on the X axis.
Free: can rotate freely in this axi...
  - `y`: `PBIKLimitType` — [Read-Write] Limit the rotation angle of the bone on the Y axis.
Free: can rotate freely in this axi...
  - `z`: `PBIKLimitType` — [Read-Write] Limit the rotation angle of the bone on the Z axis.
Free: can rotate freely in this axi...

### `unreal.PBIKDebug`
Inherits: `StructBase` | Header: `RigUnit_PBIK.h`

### `unreal.PBIKEffector`
Inherits: `StructBase` | Header: `RigUnit_PBIK.h`

**Properties** (1):
  - `chain_depth`: `int` — [Read-Write] Range 0-inf (default is 0). Explicitly set the number of bones up the hierarchy to cons...

### `unreal.PBIKSolverSettings`
Inherits: `StructBase` | Header: `PBIKSolver.h`

PBIKSolver Settings

**Properties** (9):
  - `allow_stretch`: `bool` — [Read-Write] If true, joints will translate to reach the effectors; causing bones to lengthen if nec...
  - `global_pull_chain_alpha`: `float` — [Read-Write] A global multiplier for all Pull Chain Alpha values on all effectors. Range is 0.0 to 1...
  - `iterations`: `int` — [Read-Write] High iteration counts can help solve complex joint configurations with competing constr...
  - `mass_multiplier`: `float` — [Read-Write] A global mass multiplier; higher values will make the joints more stiff, but require mo...
  - `max_angle`: `float` — [Read-Write] Maximum angle that a joint can be rotated per constraint iteration. Lower this value if...
  - `over_relaxation`: `float` — [Read-Write] Pushes constraints beyond their normal amount to speed up convergence. Increasing this ...
  - `pre_pull_root_settings`: `RootPrePullSettings` — [Read-Write] Settings only applicable when Root Behavior is set to âPrePullâ. Use these values t...
  - `root_behavior`: `PBIKRootBehavior` — [Read-Write] (Default is PrePull) Set the behavior of the solver root.
Pre Pull: translates and rota...
  - `sub_iterations`: `int` — [Read-Write] Iterations used for sub-chains defined by the Chain Depth of the effectors. These are s...

### `unreal.RigUnit_PBIK`
Inherits: `RigUnit_HighlevelBaseMutable` | Header: `RigUnit_PBIK.h`

Based on a Position Based solver at core, this node can solve multi chains within a root using multi effectors

**Properties** (6):
  - `bone_settings`: `None` — [Read-Write] Per-bone settings to control the resulting pose. Includes limits and preferred angles. ...
  - `debug`: `PBIKDebug` — [Read-Write] Debug drawing options. (PBIKDebug)
  - `effectors`: `None` — [Read-Write] An array of effectors. These specify target transforms for different parts of the skele...
  - `excluded_bones`: `None` — [Read-Write] These bones will be excluded from the solver. They will not bend and will not contribut...
  - `root`: `Name` — [Read-Write] This is usually the top-most skinned bone; often the âPelvisâ or âHipsâ, but ca...
  - `settings`: `PBIKSolverSettings` — [Read-Write] Global solver settings. (PBIKSolverSettings)

### `unreal.RootPrePullSettings`
Inherits: `StructBase` | Header: `PBIKSolver.h`

Root Pre Pull Settings

**Properties** (8):
  - `position_alpha`: `float` — [Read-Write] Range 0-1, default is 1. Apply a large scale position offset to the entire body prior t...
  - `position_alpha_x`: `float` — [Read-Write] Range 0-1, default is 1. Blend contribution to position offset in the X axis in compone...
  - `position_alpha_y`: `float` — [Read-Write] Range 0-1, default is 1. Blend contribution to position offset in the Y axis in compone...
  - `position_alpha_z`: `float` — [Read-Write] Range 0-1, default is 1. Blend contribution to position offset in the Z axis in compone...
  - `rotation_alpha`: `float` — [Read-Write] Range 0-1, default is 0. Apply a large scale rotation offset to the entire body prior t...
  - `rotation_alpha_x`: `float` — [Read-Write] Range 0-1, default is 1. Blend contribution to rotation offset in the X axis in compone...
  - `rotation_alpha_y`: `float` — [Read-Write] Range 0-1, default is 1. Blend contribution to rotation offset in the Y axis in compone...
  - `rotation_alpha_z`: `float` — [Read-Write] Range 0-1, default is 1. Blend contribution to rotation offset in the Z axis in compone...

### `unreal.PBIKLimitType`
Inherits: `EnumBase` | Header: `PBIK_Shared.h`

EPBIKLimit Type

**Properties** (3):
  - `FREE`: `PBIKLimitType = Ellipsis` — 0
  - `LIMITED`: `PBIKLimitType = Ellipsis` — 1
  - `LOCKED`: `PBIKLimitType = Ellipsis` — 2

### `unreal.PBIKRootBehavior`
Inherits: `EnumBase` | Header: `PBIKSolver.h`

namespace

**Properties** (3):
  - `FREE`: `PBIKRootBehavior = Ellipsis` — 2
  - `PIN_TO_INPUT`: `PBIKRootBehavior = Ellipsis` — 1
  - `PRE_PULL`: `PBIKRootBehavior = Ellipsis` — 0
