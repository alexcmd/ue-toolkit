# UE Python API — SlateScreenReader Module

**1 types** from the `SlateScreenReader` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `SlateScreenReaderEngineSubsystem`

---

## Classes

### `unreal.SlateScreenReaderEngineSubsystem`
Inherits: `EngineSubsystem` | Header: `SlateScreenReaderEngineSubsystem.h`

The engine subsystem for the Slate screen reader. A screen reader is a framework that provides vision accessibility services for screen reader useres. A screen reader user is a single user of the scre...

**Methods** (19):
  - `activate_screen_reader()` -> `None` — Activates the underlying screen reader. Use this to allow screen reader users to register with the screen reader and rec...
  - `activate_user(user_id)` -> `ScreenReaderReply` — Activates a screen reader user and fulfill requests for accessibility services such as text to speech that clients can m...
  - `deactivate_screen_reader()` -> `None` — Deactivates the underlying screen reader and prevents screen reader users from getting any accessible feedback via text ...
  - `deactivate_user(user_id)` -> `ScreenReaderReply` — Deactivates the screen reader and disables all announcement and text to speech services making them do nothing. If the p...
  - `get_speech_rate(user_id)` -> `float` — Returns the rate text to speech will be speaking at for a screen reader user. Value is between 0.0f and 1.0f. If the pro...
  - `get_speech_volume(user_id)` -> `float` — Returns the volume text to speech will be speaking at for a screen reader user. Value is between 0.0f and 1.0f. If the p...
  - `is_screen_reader_active()` -> `bool` — Returns true if the screen reader is currently active and accessibility services such as text to speech can be used by t...
  - `is_speaking(user_id)` -> `bool` — Returns true if the screen reader is speaking text to a particular user. Returns false if no no announcements are being ...
  - `is_speech_muted(user_id)` -> `bool` — Returns true if text to speech for a screen reader user is muted. Otherwise false is returned. If the provided user Id d...
  - `is_user_registered(user_id)` -> `bool` — Returns true if the passed in screen reader user Id is already registered. Else false is returned.
  - `mute_speech(user_id)` -> `ScreenReaderReply` — Mutes the text to speech for a screen reader user. If the provided user Id doesnât exist, nothing will happen.
  - `register_user(user_id)` -> `ScreenReaderReply` — Registers a provided user Id to the screen reader framework and allows the screen reader user to receive and respond to ...
  - `request_speak(user_id, announcement)` -> `ScreenReaderReply` — Requests an announcement to be spoken to the screen reader user. This is the main mechanism to provide text to speech au...
  - `request_speak_focused_widget(user_id)` -> `ScreenReaderReply` — Requests the information about the accessibility widget a user is focused on to be read out. If nothing is currently bei...
  - `set_speech_rate(user_id, rate)` -> `ScreenReaderReply` — Sets the rate text to speech will be speaking at for a screen reader user. If the provided user Id doesnât exist, noth...
  - `set_speech_volume(user_id, volume)` -> `ScreenReaderReply` — Sets the volume text to speech will be speaking at for a screen reader user. If the provided user Id doesnât exist, no...
  - `stop_speaking(user_id)` -> `ScreenReaderReply` — Immediately stops speaking any currently spoken announcement for a particular screen reader user. Does nothing if there ...
  - `unmute_speech(user_id)` -> `ScreenReaderReply` — Unmutes the text to speech for a screen reader user. If the provided user Id doesnât exist, nothing will happen.
  - `unregister_user(user_id)` -> `ScreenReaderReply` — Unregisters a provided user Id from the screen reader framework and deactivates the user. The unregistered user will no ...
