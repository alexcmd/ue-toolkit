# UE Python API — EditorInteractiveToolsFramework Module

**3 types** from the `EditorInteractiveToolsFramework` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `GizmosParameters`, `TransformGizmoEditorSettings`, `AxisRotateMode`

---

## Classes

### `unreal.GizmosParameters`
Inherits: `StructBase` | Header: `TransformGizmo.h`

Gizmos Parameters

### `unreal.TransformGizmoEditorSettings`
Inherits: `DeveloperSettings` | Header: `TransformGizmoEditorSettings.h`

Transform Gizmo Editor Settings

### `unreal.AxisRotateMode`
Inherits: `EnumBase` | Header: `TransformGizmo.h`

EAxis Rotate Mode

**Properties** (2):
  - `ARC`: `AxisRotateMode = Ellipsis` — 1
  - `PULL`: `AxisRotateMode = Ellipsis` — 0
