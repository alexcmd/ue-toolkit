# UE Python API — TimecodeSynchronizerEditor Module

**1 types** from the `TimecodeSynchronizerEditor` UE module.
All accessible via `import unreal` in the UE Python environment.

**Classes**: `TimecodeSynchronizerFactory`

---

## Classes

### `unreal.TimecodeSynchronizerFactory`
Inherits: `Factory` | Header: `TimecodeSynchronizerFactory.h`

Timecode Synchronizer Factory
